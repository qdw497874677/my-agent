---
phase: 06-java-extension-surface-spi-and-spring
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiTool.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiEventListener.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedToolExtensionSourceFactory.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedEventListenerExtensionSourceFactory.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfiguration.java
  - pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedSpringExtensionTest.java
autonomous: true
requirements: [EXT-02, EXT-05]
must_haves:
  truths:
    - "Spring users can register lightweight tools with an annotation."
    - "Spring users can register lightweight event listeners with an annotation."
    - "Annotations do not attempt to define model providers, policies, workspace providers, or memory providers."
  artifacts:
    - path: "pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiTool.java"
      provides: "Lightweight Spring tool annotation"
    - path: "pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiEventListener.java"
      provides: "Lightweight Spring event listener annotation"
    - path: "pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedSpringExtensionTest.java"
      provides: "Annotation registration proof"
  key_links:
    - from: "AnnotatedToolExtensionSourceFactory"
      to: "ToolRegistry"
      via: "ToolExtensionCapability descriptor plus binding"
      pattern: "ToolExtensionCapability"
---

<objective>
Add lightweight Spring annotations for tools and event listeners.

Purpose: Provide ergonomic Spring registration while preserving explicit Bean registration for complex providers/policies/workspace/memory capabilities.
Output: `@PiTool`, `@PiEventListener`, annotation scanners/factories inside starter auto-configuration, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-CONTEXT.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-04-SUMMARY.md
@docs/phase-04-governed-tool-contracts.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define limited Spring extension annotations</name>
  <files>pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiTool.java, pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/annotation/PiEventListener.java</files>
  <behavior>
    - `@PiTool` captures id, name, description, version, scopes, risk, sideEffect, timeoutMs, inputSchema JSON string, and optional metadata.
    - `@PiEventListener` captures id, event types, order, version, and metadata.
    - No annotations are created for model providers, policy engines, workspace/resource providers, or memory providers per D-10.
  </behavior>
  <action>Create runtime-retained annotations targeted to methods or beans as appropriate. Keep annotation attributes strings/primitives/enums only; do not import Jackson or JSON Schema libraries. Document in Javadoc that complex capabilities must use explicit `ExtensionSource` Spring Beans per D-09 and D-10.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am test</automated>
  </verify>
  <done>Starter exposes exactly two lightweight annotation types: tools and event listeners.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert annotated Spring methods into extension sources</name>
  <files>pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedToolExtensionSourceFactory.java, pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedEventListenerExtensionSourceFactory.java, pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfiguration.java</files>
  <behavior>
    - Annotated tool methods become `ToolDescriptor` plus `ToolExecutorBinding` and execute only through `ToolExecutionGateway` once registered per D-07 and D-19.
    - Annotated event listeners become event listener capabilities only; they do not replace `EventSink` persistence/fanout semantics.
    - Annotation discovery is explicit starter bean logic, not broad unbounded component-scan magic per D-13.
  </behavior>
  <action>Use Spring bean factory/application context inspection in the starter to find annotations on already-registered beans. For tool methods, support a conservative signature accepting either `ToolExecutionRequest` or no args returning `ToolExecutionResult`/`Map`. Convert annotation schema JSON string into existing `ToolSchema` raw map minimally; if parsing is not available without Jackson, require a simple default object schema and document metadata. Provenance should be `SPRING_BEAN` with source id `spring-annotations` and binding ref beanName#methodName.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am -Dtest=AnnotatedSpringExtensionTest test</automated>
  </verify>
  <done>Annotated methods contribute normalized capabilities alongside explicit Spring Beans and ServiceLoader sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test annotation registration and boundary behavior</name>
  <files>pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/AnnotatedSpringExtensionTest.java</files>
  <behavior>
    - Annotated tool appears in `ToolRegistry.listTools()` with `SPRING_BEAN` provenance and can be resolved to a binding.
    - Duplicate annotated and bean tool IDs fail by default.
    - Annotated event listener appears in extension governance with capability type `EVENT_LISTENER`.
  </behavior>
  <action>Add ApplicationContextRunner tests with a fixture bean containing one `@PiTool` method and one `@PiEventListener` method. Assert the tool is represented as a normal registry entry and governance reports both capabilities. Do not invoke tool methods directly in tests except through resolved `ToolExecutorBinding`; gateway-level conformance is covered later.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am -Dtest=AnnotatedSpringExtensionTest test</automated>
  </verify>
  <done>Spring annotation path works for lightweight tools/listeners and stays within the locked Phase 6 annotation scope.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am -Dtest=AnnotatedSpringExtensionTest test`.
</verification>

<success_criteria>
EXT-02 includes both explicit Spring Bean and limited annotation registration, with no annotation-driven scope creep into providers/policies/workspace/memory.
</success_criteria>

<output>
After completion, create `.planning/phases/06-java-extension-surface-spi-and-spring/06-05-SUMMARY.md`.
</output>
