---
phase: 06-java-extension-surface-spi-and-spring
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - pom.xml
  - pi-agent-spring-boot-starter/pom.xml
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfiguration.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionProperties.java
  - pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/SpringExtensionSourceFactory.java
  - pi-agent-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
  - pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfigurationTest.java
autonomous: true
requirements: [EXT-02, EXT-03, EXT-05, WORK-06]
must_haves:
  truths:
    - "External Spring Boot applications can include a starter and get extension discovery/registration beans."
    - "The Cloud Server can consume the same starter path later instead of bespoke internal wiring."
    - "Spring Bean extension sources merge deterministically with ServiceLoader sources."
  artifacts:
    - path: "pi-agent-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports"
      provides: "Boot 3.5 auto-configuration registration"
    - path: "pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfiguration.java"
      provides: "Starter auto-configuration"
    - path: "pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfigurationTest.java"
      provides: "ApplicationContextRunner starter tests"
  key_links:
    - from: "PiAgentExtensionAutoConfiguration"
      to: "DefaultExtensionContributionRegistry"
      via: "auto-configured bean"
      pattern: "@AutoConfiguration"
---

<objective>
Create the Spring Boot starter/autoconfiguration for extension registration.

Purpose: Validate the Java enterprise integration path and prepare Cloud Server to consume the same external starter path.
Output: Boot 3.5 starter module using `@AutoConfiguration`, `@ConditionalOnMissingBean`, properties, AutoConfiguration.imports, and ApplicationContextRunner tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-CONTEXT.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-01-SUMMARY.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-02-SUMMARY.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-03-SUMMARY.md

<interfaces>
Spring Boot 3.5 auto-configuration pattern verified by Context7:
```java
@AutoConfiguration
@EnableConfigurationProperties(PiAgentExtensionProperties.class)
public class PiAgentExtensionAutoConfiguration {
    @Bean
    @ConditionalOnMissingBean
    SomeBean someBean(...) { ... }
}
```
Register with `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`, one class name per line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Spring Boot starter module and imports file</name>
  <files>pom.xml, pi-agent-spring-boot-starter/pom.xml, pi-agent-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</files>
  <behavior>
    - Starter is a separate module from the framework-free SDK per D-01.
    - Starter depends on extension API and infrastructure-extension plus Spring Boot autoconfigure/test only; Domain/App remain free of starter dependencies per D-21.
    - Auto-configuration is registered via Boot 3.5 `AutoConfiguration.imports` per researched Spring Boot docs.
  </behavior>
  <action>Add `pi-agent-spring-boot-starter` to the reactor after `pi-agent-infrastructure-extension`. Create its POM with dependencies on `pi-agent-extension-api`, `pi-agent-infrastructure-extension`, `pi-agent-app`, `spring-boot-autoconfigure`, `spring-boot-configuration-processor` optional, and test-scoped `spring-boot-test`. Add the `AutoConfiguration.imports` file pointing to `io.github.pi_java.agent.spring.autoconfigure.PiAgentExtensionAutoConfiguration`.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am test</automated>
  </verify>
  <done>Starter module is present and Boot can discover its auto-configuration metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-configure ServiceLoader and Spring Bean extension sources</name>
  <files>pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfiguration.java, pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionProperties.java, pi-agent-spring-boot-starter/src/main/java/io/github/pi_java/agent/spring/autoconfigure/SpringExtensionSourceFactory.java</files>
  <behavior>
    - Configuration uses `@AutoConfiguration`, `@ConfigurationProperties`, `@EnableConfigurationProperties`, and `@ConditionalOnMissingBean` per D-13.
    - Discovers both ServiceLoader sources and explicit Spring `ExtensionSource` beans per D-09 and D-11.
    - Properties allow disabling sources/capabilities and duplicate override opt-in per D-12 and D-15.
  </behavior>
  <action>Implement `PiAgentExtensionProperties` under prefix `pi.extensions` with enabled flag, platformApiVersion default `1.0`, disabled sources/capabilities, and allowDuplicateCapabilityOverrides default false. Auto-configure `ServiceLoaderExtensionDiscovery`, `DefaultExtensionContributionRegistry`, `ExtensionToolRegistry`, `ExtensionModelProviderRegistry`, and `ExtensionGovernanceCatalog` if missing. Merge `ObjectProvider&lt;ExtensionSource&gt;` Spring beans with ServiceLoader sources using deterministic order; do not rely on broad component scanning.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am -Dtest=PiAgentExtensionAutoConfigurationTest test</automated>
  </verify>
  <done>Spring Boot applications get deterministic extension registries from ServiceLoader and Spring Bean extension sources with override-friendly bean conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test starter behavior with ApplicationContextRunner</name>
  <files>pi-agent-spring-boot-starter/src/test/java/io/github/pi_java/agent/spring/autoconfigure/PiAgentExtensionAutoConfigurationTest.java</files>
  <behavior>
    - ContextRunner creates auto-configured beans when starter is on classpath.
    - User-provided beans override auto-configured beans via `@ConditionalOnMissingBean`.
    - Duplicate capability IDs fail by default; configured disabled capability remains visible in governance and absent from usable ToolRegistry.
  </behavior>
  <action>Add ApplicationContextRunner tests. Include a simple Spring `ExtensionSource` bean with one tool capability and assert `ToolRegistry.listTools()` includes it with `SPRING_BEAN` provenance. Add tests for `pi.extensions.disabled-capabilities` and duplicate ID behavior. Keep tests no-key/no-Docker.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am -Dtest=PiAgentExtensionAutoConfigurationTest test</automated>
  </verify>
  <done>Starter behavior is proven through Spring Boot context tests, not just unit construction.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-spring-boot-starter -am test`.
</verification>

<success_criteria>
EXT-02 has a production starter path for external Spring Boot applications, with deterministic merge, config-driven disable, and no hidden component-scan magic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-java-extension-surface-spi-and-spring/06-04-SUMMARY.md`.
</output>
