---
phase: 06-java-extension-surface-spi-and-spring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - pi-agent-extension-api/pom.xml
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionSource.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionMetadata.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionApiVersion.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionCompatibility.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionLifecycleState.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionHealth.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ToolExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ModelProviderExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/PolicyExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/EventListenerExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/WorkspaceProviderExtensionCapability.java
  - pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/MemoryProviderExtensionCapability.java
  - pi-agent-extension-api/src/test/java/io/github/pi_java/agent/extension/api/ExtensionApiContractTest.java
  - pi-agent-extension-api/src/test/java/io/github/pi_java/agent/extension/api/ExtensionApiArchitectureTest.java
autonomous: true
requirements: [EXT-01, EXT-03, WORK-06]
must_haves:
  truths:
    - "Developer can depend on one framework-free extension API JAR to describe extension metadata and capabilities."
    - "Extension API covers tools, model providers, policies, event listeners/sinks, workspace providers, and memory placeholders."
    - "The extension API has version, compatibility, lifecycle, health, and source/capability identity contracts."
  artifacts:
    - path: "pi-agent-extension-api/pom.xml"
      provides: "Framework-free public extension API module"
    - path: "pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/ExtensionSource.java"
      provides: "Extension-source-with-capabilities entry contract"
    - path: "pi-agent-extension-api/src/test/java/io/github/pi_java/agent/extension/api/ExtensionApiArchitectureTest.java"
      provides: "Architecture gate for SDK boundaries"
  key_links:
    - from: "pi-agent-extension-api"
      to: "pi-agent-domain/pi-agent-app contracts"
      via: "compile dependencies only on existing Domain/App modules"
      pattern: "<artifactId>pi-agent-(domain|app)</artifactId>"
---

<objective>
Create the framework-free public Java extension API/SPI module.

Purpose: Establish the stable SDK surface before ServiceLoader, Spring starter, MCP, or PF4J work builds on it.
Output: A Maven module containing pure Java extension metadata, version/compatibility, lifecycle/health, and typed capability contracts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-java-extension-surface-spi-and-spring/06-CONTEXT.md
@docs/phase-03-model-provider-contracts.md
@docs/phase-04-governed-tool-contracts.md

<interfaces>
Existing contracts to reuse, not re-invent:

From `ToolDescriptor`:
```java
public record ToolDescriptor(String id, String name, String description, ToolSchema inputSchema,
        Optional<ToolSchema> outputSchema, ToolProvenance provenance, String version,
        Set<String> scopes, ToolRiskLevel riskLevel, ToolSideEffect sideEffect,
        Duration defaultTimeout, Map<String, Object> metadata) {}
```

From `ToolExecutorBinding`:
```java
@FunctionalInterface
public interface ToolExecutorBinding {
    ToolExecutionResult execute(ToolExecutionRequest request, CancellationToken cancellationToken);
}
```

From `ModelProviderRegistry`:
```java
public interface ModelProviderRegistry {
    List<ProviderDescriptor> listProviders();
    Optional<ModelProviderResolution> resolve(ProviderModelRef modelRef);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public extension API Maven module</name>
  <files>pom.xml, pi-agent-extension-api/pom.xml</files>
  <behavior>
    - Reactor includes `pi-agent-extension-api` before Spring/Adapter modules.
    - The module has compile dependencies only on `pi-agent-domain` and `pi-agent-app` per D-01 and D-02.
    - The module has no Spring, Vaadin, PF4J, MCP, JDBC, provider SDK, Adapter, or Infrastructure production dependency per D-21.
  </behavior>
  <action>Add `pi-agent-extension-api` to the parent `<modules>` list after `pi-agent-app`. Create its POM with parent version, artifactId `pi-agent-extension-api`, name `Pi Agent Extension API`, compile dependencies on `pi-agent-domain` and `pi-agent-app`, and test-scoped JUnit/AssertJ/ArchUnit. Do not add Spring Boot starter dependencies here; those belong to a later dedicated starter module per D-01.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-extension-api -am test</automated>
  </verify>
  <done>`pi-agent-extension-api` participates in Maven reactor and compiles with only allowed framework-free dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define extension metadata and capability contracts</name>
  <files>pi-agent-extension-api/src/main/java/io/github/pi_java/agent/extension/api/*.java, pi-agent-extension-api/src/test/java/io/github/pi_java/agent/extension/api/ExtensionApiContractTest.java</files>
  <behavior>
    - `ExtensionSource` exposes `ExtensionMetadata metadata()` and `List&lt;ExtensionCapability&gt; capabilities()` per D-06.
    - Metadata includes extension id/name/version/vendor, platform API version range, lifecycle state, health, enabled flag, and redacted metadata per D-14 through D-18.
    - Capability contracts cover tools, model providers, policies, event listeners/sinks, workspace/resource providers, and minimal memory placeholder per D-05 and D-08.
    - Tool capability carries a complete `ToolDescriptor` plus `ToolExecutorBinding` per D-07.
  </behavior>
  <action>Create immutable Java records/interfaces under `io.github.pi_java.agent.extension.api`. Use sealed or marker interfaces where useful, but keep contracts simple plain Java. Use `ExtensionCapability.Type` values `TOOL`, `MODEL_PROVIDER`, `POLICY`, `EVENT_LISTENER`, `WORKSPACE_PROVIDER`, `MEMORY_PROVIDER`. Model memory provider as metadata-only placeholder with no runtime/RAG wiring. Tests should assert validation rejects blank IDs, capability IDs are stable, tool capability requires descriptor+binding, and lifecycle/compatibility values are machine-checkable.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-extension-api -Dtest=ExtensionApiContractTest test</automated>
  </verify>
  <done>Extension SDK exposes the full Phase 6 capability family while keeping memory minimal and tool capabilities descriptor-first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add extension API architecture gate</name>
  <files>pi-agent-extension-api/src/test/java/io/github/pi_java/agent/extension/api/ExtensionApiArchitectureTest.java</files>
  <behavior>
    - API package does not depend on Spring, Jakarta, Vaadin, PF4J, MCP, Spring AI, Jackson annotations, JDBC, provider SDKs, Adapter, or Infrastructure per D-02 and D-21.
    - Domain/App still do not depend back on `io.github.pi_java.agent.extension..` per D-21.
  </behavior>
  <action>Add ArchUnit tests importing `io.github.pi_java.agent.extension.api`, `io.github.pi_java.agent.domain`, and `io.github.pi_java.agent.app`. Assert extension API has no forbidden framework/implementation dependencies and Domain/App do not depend on extension packages. Keep this test focused on boundaries; do not enforce package naming beyond current project conventions.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-extension-api -Dtest=ExtensionApiArchitectureTest test</automated>
  </verify>
  <done>Architecture tests fail if the SDK pulls in Spring/PF4J/MCP/Adapter/Infrastructure or if Domain/App depend back on the extension SDK.</done>
</task>

</tasks>

<verification>
Run the full focused module gate: `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-extension-api -am test`.
</verification>

<success_criteria>
The repository has a public framework-free extension API module that implements decisions D-01, D-02, D-05, D-06, D-07, D-08, D-14, D-16, D-18, and D-21 without adding Spring or implementation dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/06-java-extension-surface-spi-and-spring/06-01-SUMMARY.md`.
</output>
