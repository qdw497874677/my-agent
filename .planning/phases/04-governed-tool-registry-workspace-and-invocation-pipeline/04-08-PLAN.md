---
phase: 04-governed-tool-registry-workspace-and-invocation-pipeline
plan: 08
type: execute
wave: 7
depends_on: [04-05, 04-07]
files_modified:
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerGovernedToolE2ETest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/GovernedToolSecurityRedactionE2ETest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/InMemoryCloudE2EConfiguration.java
  - docs/phase-04-governed-tool-contracts.md
  - .planning/REQUIREMENTS.md
requirements: [E2E-02, E2E-03, E2E-06, TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05, TOOL-06, TOOL-07, WORK-03, WORK-07, WORK-08, OPS-02, OPS-03, OPS-05]
autonomous: true
must_haves:
  truths:
    - "Headless E2E proves successful model-to-tool-to-model execution through ToolExecutionGateway."
    - "Headless E2E proves policy deny and approval-required paths persist events/audit and prevent unauthorized execution."
    - "Security E2E proves fake sensitive values do not appear in REST responses, RunEvents, audit records, or persisted payloads by default."
    - "Phase 4 downstream docs explain contracts, APIs, events, built-ins, local-temp limitations, and deferrals."
  artifacts:
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerGovernedToolE2ETest.java"
      provides: "No-key E2E for success, deny, approval, preview, workspace-bound execution"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/GovernedToolSecurityRedactionE2ETest.java"
      provides: "E2E-06 secret/sensitive payload absence assertions"
    - path: "docs/phase-04-governed-tool-contracts.md"
      provides: "Downstream contract index for phases 5-8"
  key_links:
    - from: "CloudServerGovernedToolE2ETest"
      to: "ToolExecutionGateway"
      via: "runtime-created tool call"
      pattern: "toolExecutionGateway|tool.gateway|tool.policy_decided"
    - from: "GovernedToolSecurityRedactionE2ETest"
      to: "run_events/audit_records"
      via: "event history and audit assertions"
      pattern: "fake-secret|redacted|audit"
---

<objective>
Add final Phase 4 headless E2E, security-redaction E2E, downstream contract documentation, and requirement status updates.

Purpose: Phase 4 is not complete until product-path tests prove successful, denied, approval-required, preview, workspace, audit/event, and secret-redaction behavior through Cloud Server/runtime paths.
Output: No-key E2E tests, docs, and requirement traceability updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-CONTEXT.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-05-SUMMARY.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-07-SUMMARY.md
@docs/phase-02-cloud-server-api.md
@docs/phase-03-model-provider-contracts.md

<interfaces>
Reuse Phase 3 no-Docker pattern if Docker is unavailable:
```java
// InMemoryCloudE2EConfiguration provides test-only repositories, queue, audit, and transaction template.
```
E2E must create runs through REST/worker activation where possible and assert persisted event history/audit, not only direct gateway unit behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add governed tool Cloud Server E2E</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerGovernedToolE2ETest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/InMemoryCloudE2EConfiguration.java</files>
  <behavior>
    - Test 1: fake model requests safe read-only tool, runtime invokes through ToolExecutionGateway, emits proposed/policy/started/completed events, persists audit, returns to model, and completes run.
    - Test 2: deny/block policy path emits denied/policy events and audit and proves executor side effect counter remains zero.
    - Test 3: approval-required side-effectful tool emits preview and approval-required events/audit and proves side effect does not execute.
    - Test 4: workspace write/allowlisted command path is workspace-bound and emits redacted/summarized result events.
  </behavior>
  <action>Add no-key E2E tests using existing fake provider/testkit patterns and built-in tools. Prefer in-memory Cloud E2E fallback when Docker/Testcontainers are unavailable, but still exercise REST create-run, worker activation/gateway runtime path, event history replay, and audit repository assertions. Do not require real provider keys, real secrets, broad shell access, or Docker-only success.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=CloudServerGovernedToolE2ETest test</automated>
  </verify>
  <done>E2E-02/E2E-03 paths pass without real keys and prove all runtime tool calls go through ToolExecutionGateway.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add security redaction E2E</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/GovernedToolSecurityRedactionE2ETest.java</files>
  <behavior>
    - Test 1: configured fake sensitive values in tool args/results are absent from REST run detail, event history, and audit details.
    - Test 2: persisted run_events and audit_records contain redaction/truncation metadata instead of raw fake secrets.
    - Test 3: exception messages and normalized errors expose safe categories/messages only.
  </behavior>
  <action>Create explicit fake sensitive values such as `PI_PHASE4_FAKE_SECRET_DO_NOT_LEAK` in test config only. Assert absence across API DTOs, RunEvent payloads, audit details, and persistence JSON strings. Do not use real secrets or environment variables per D-22/D-26.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=GovernedToolSecurityRedactionE2ETest test</automated>
  </verify>
  <done>E2E-06 passes and proves default event/audit/API paths do not expose raw secrets or sensitive payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 4 contracts and update requirement status</name>
  <files>docs/phase-04-governed-tool-contracts.md, .planning/REQUIREMENTS.md</files>
  <action>Write downstream contract documentation covering ToolDescriptor, ToolRegistry, ToolExecutionGateway, validation library boundary, policy decisions, preview/approval/sandbox semantics, lifecycle events, audit/redaction/payload limits, built-in tools, bounded local-temp workspace limitations, REST catalog API, E2E verification, and explicit deferrals (SPI/Spring discovery, MCP, dynamic plugins, Web Console approval cards, production sandbox). Update `.planning/REQUIREMENTS.md` Phase 4 requirement statuses with concise validation evidence for WORK-03/07/08, TOOL-01..07, OPS-02/03/05, E2E-02/03/06.</action>
  <verify>
    <automated>test -f docs/phase-04-governed-tool-contracts.md && grep -q "TOOL-01" .planning/REQUIREMENTS.md && grep -q "ToolExecutionGateway" docs/phase-04-governed-tool-contracts.md</automated>
  </verify>
  <done>Phase 4 has a contract index for Phases 5-8 and requirements traceability reflects implemented verification evidence.</done>
</task>

</tasks>

<verification>
Run focused Phase 4 gates: `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-domain,pi-agent-app,pi-agent-infrastructure,pi-testkit,pi-agent-adapter-web -am test`. If full gate is Docker-blocked by pre-existing Testcontainers tests, record the Docker environment gate and ensure all Phase 4 no-Docker focused tests pass.
</verification>

<success_criteria>
Phase 4 requirements have automated verification evidence; successful, denied, approval-required, preview, workspace-bound, audit/event, and redaction paths are proven through product-like Cloud Server/runtime tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-08-SUMMARY.md`.
</output>
