---
phase: 04-governed-tool-registry-workspace-and-invocation-pipeline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolRegistry.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolExecutorBinding.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/ToolRegistryQueryService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultToolRegistryQueryService.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/tool/ToolCatalogResponse.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/tool/ToolDescriptorDto.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/port/ToolRegistryAppPortContractTest.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultToolRegistryQueryServiceTest.java
requirements: [TOOL-01, TOOL-02]
autonomous: true
must_haves:
  truths:
    - "App layer can list and resolve tools from normalized descriptors without source-specific registry methods."
    - "Executor bindings are registered behind descriptors and are not exposed through public client DTOs."
    - "Future Admin/Web Console can consume a read-only catalog DTO without Domain or implementation leakage."
  artifacts:
    - path: "pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolRegistry.java"
      provides: "Descriptor-first registry port per D-01/D-05"
    - path: "pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolExecutorBinding.java"
      provides: "Low-level executor binding contract behind the gateway per D-04"
    - path: "pi-agent-client/src/main/java/io/github/pi_java/agent/client/tool/ToolDescriptorDto.java"
      provides: "Public read-only tool catalog DTO per D-23"
  key_links:
    - from: "DefaultToolRegistryQueryService"
      to: "ToolRegistry"
      via: "list/resolve only"
      pattern: "registry\.listTools|registry\.resolve"
    - from: "ToolDescriptorDto"
      to: "ToolDescriptor"
      via: "adapter/service mapping without exposing executor"
      pattern: "ToolDescriptorDto"
---

<objective>
Add App/client contracts for descriptor-first tool registry and read-only tool catalog queries.

Purpose: Phase 4 needs a source-agnostic registry shape that mirrors Phase 3 provider registry while preventing Spring/MCP/plugin-specific registration APIs from becoming core contracts.
Output: App ports/use case and client DTO records for listing/resolving tool descriptors and executor bindings.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-CONTEXT.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-01-SUMMARY.md

<interfaces>
Mirror this Phase 3 registry pattern:
```java
public interface ModelProviderRegistry {
    List<ProviderDescriptor> listProviders();
    default Optional<ModelProviderResolution> resolve(ProviderModelRef modelRef) { ... }
}

public final class DefaultModelProviderQueryService implements ModelProviderQueryService {
    public ProviderCatalog listProviders(RequestContext context) { ... }
}
```
Do not add methods named `registerSpringBean`, `registerMcpTool`, or `registerPluginTool` per D-01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define descriptor-first ToolRegistry and executor binding ports</name>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolRegistry.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolExecutorBinding.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/port/ToolRegistryAppPortContractTest.java</files>
  <behavior>
    - Test 1: registry lists descriptors and resolves a namespaced tool id to descriptor plus executor binding.
    - Test 2: unknown tool resolution returns `Optional.empty()` rather than null.
    - Test 3: no registry API is source-specific; tests should fail if public methods contain Spring/MCP/plugin names.
  </behavior>
  <action>Create App-layer registry ports depending only on App/Domain types. `ToolExecutorBinding` should accept `ToolExecutionRequest` and `CancellationToken` or equivalent gateway-safe context; it must not perform policy/audit itself. Keep executor binding hidden behind registry resolution so `ToolExecutionGateway` is the only caller per D-03/D-04.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-app -am -Dtest=ToolRegistryAppPortContractTest test</automated>
  </verify>
  <done>App has source-agnostic `ToolRegistry` and `ToolExecutorBinding` contracts with tests proving descriptor-first shape and no source-specific APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only tool catalog query service and client DTOs</name>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/ToolRegistryQueryService.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultToolRegistryQueryService.java, pi-agent-client/src/main/java/io/github/pi_java/agent/client/tool/ToolCatalogResponse.java, pi-agent-client/src/main/java/io/github/pi_java/agent/client/tool/ToolDescriptorDto.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultToolRegistryQueryServiceTest.java</files>
  <behavior>
    - Test 1: catalog response includes id/name, description, schema metadata, provenance, version, scopes, risk, side effect, and timeout default.
    - Test 2: catalog response never exposes executor binding class names, raw secrets, or implementation objects.
  </behavior>
  <action>Implement read-only App use case for tool catalog visibility required by D-23. Client DTOs must be plain Java records in `pi-agent-client`, free of Domain imports and Spring/Jakarta annotations, matching Phase 2/3 DTO boundary decisions.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-app,pi-agent-client -am -Dtest=DefaultToolRegistryQueryServiceTest test</automated>
  </verify>
  <done>Tool catalog query contracts exist for future Adapter REST mapping and preserve client/domain separation.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-app,pi-agent-client -am test` and confirm existing App architecture tests remain green.
</verification>

<success_criteria>
Descriptor-first registry contracts exist; executor bindings are internal; read-only catalog DTOs expose required metadata without implementation details; no deferred SPI/Spring/MCP/plugin discovery is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-02-SUMMARY.md`.
</output>
