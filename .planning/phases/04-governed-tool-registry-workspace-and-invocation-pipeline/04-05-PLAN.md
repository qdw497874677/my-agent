---
phase: 04-governed-tool-registry-workspace-and-invocation-pipeline
plan: 05
type: execute
wave: 5
depends_on: [04-03, 04-04]
files_modified:
  - pi-testkit/pom.xml
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolExecutionGateway.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolExecutorBinding.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolRegistry.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolInvoker.java
  - pi-testkit/src/test/java/io/github/pi_java/agent/testkit/runtime/FakeGeneralAgentLoopTest.java
requirements: [TOOL-02, TOOL-05, TOOL-06, E2E-02, E2E-03]
autonomous: true
must_haves:
  truths:
    - "GeneralAgentLoop calls ToolExecutionGateway for model-requested tool calls, not ToolInvoker directly."
    - "Fake/testkit tool success, deny, and approval-required scenarios emit gateway lifecycle events."
    - "Low-level FakeToolInvoker remains only as executor binding compatibility detail."
  artifacts:
    - path: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java"
      provides: "Runtime/testkit tool path rerouted through gateway per D-04/D-24"
    - path: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolExecutionGateway.java"
      provides: "Deterministic gateway fake for no-key E2E"
    - path: "pi-testkit/src/test/java/io/github/pi_java/agent/testkit/runtime/FakeGeneralAgentLoopTest.java"
      provides: "Proof of model-to-tool-to-model through gateway"
  key_links:
    - from: "GeneralAgentLoop"
      to: "ToolExecutionGateway"
      via: "execute model tool-call intent"
      pattern: "toolExecutionGateway\.execute"
---

<objective>
Reroute the reusable fake General Agent loop and testkit assets through the governed ToolExecutionGateway.

Purpose: Existing no-key E2E and future runtime tests must prove the product path uses the gateway, not the legacy direct `ToolInvoker` path.
Output: Gateway-aware testkit loop and fakes for success/deny/approval contract testing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-CONTEXT.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-03-SUMMARY.md
@.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-04-SUMMARY.md

<interfaces>
Current direct path to replace:
```java
// pi-testkit/.../GeneralAgentLoop.java
PolicyDecision decision = policy.decide(toolCall);
ToolResult toolResult = toolInvoker.invoke(toolCall, context, context.cancellationToken());
publish(... RunEventType.TOOL_COMPLETED ...);
```
The new path must call `ToolExecutionGateway`; `ToolInvoker` may only be wrapped by a fake executor binding for compatibility per D-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gateway-aware testkit fakes</name>
  <files>pi-testkit/pom.xml, pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolExecutionGateway.java, pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolExecutorBinding.java, pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolRegistry.java, pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolInvoker.java</files>
  <behavior>
    - Test coverage in Task 2 should prove fake gateway success, deny, and approval branches.
  </behavior>
  <action>Add `pi-agent-app` dependency to `pi-testkit` if needed for App gateway ports. Create deterministic fake registry/gateway/bindings around existing FakeToolInvoker so older tests can be migrated without broad breakage. The fake gateway should publish the same Domain lifecycle events as DefaultToolExecutionGateway enough for E2E assertions.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit -am -DskipTests compile</automated>
  </verify>
  <done>Testkit compiles with gateway-aware fakes and keeps legacy fake invoker only behind executor binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reroute GeneralAgentLoop through ToolExecutionGateway</name>
  <files>pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java, pi-testkit/src/test/java/io/github/pi_java/agent/testkit/runtime/FakeGeneralAgentLoopTest.java</files>
  <behavior>
    - Test 1: successful model tool-call intent executes via fake gateway and returns tool result to the next model step.
    - Test 2: deny decision returns POLICY_BLOCKED/denied lifecycle without invoking the fake executor binding.
    - Test 3: approval-required returns suspended/policy-blocked-compatible test status with approval-required lifecycle and no side effect.
    - Test 4: source scan assertion in test or verification confirms `GeneralAgentLoop` no longer calls `toolInvoker.invoke` directly.
  </behavior>
  <action>Add constructor overloads that accept `ToolExecutionGateway`; migrate existing constructors to wrap `ToolInvoker` with fake gateway for source compatibility. Runtime tool-call handling must delegate to gateway and translate `ToolExecutionResult` statuses into existing `RunHandle` statuses without losing terminal-event invariants. Preserve streaming model behavior from Phase 3.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit -am -Dtest=FakeGeneralAgentLoopTest test</automated>
  </verify>
  <done>Fake General Agent loop tool calls are gateway-routed and tests prove success, deny, approval-required, and no direct invoker bypass.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit,pi-agent-adapter-web -am -Dtest=FakeGeneralAgentLoopTest,CloudServerOpenAiCompatibleE2ETest test` to ensure Phase 3 fake provider E2E still works after constructor migration.
</verification>

<success_criteria>
E2E-02 and E2E-03 testkit foundations exist; D-04 and D-24 are satisfied because the general loop uses the gateway path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governed-tool-registry-workspace-and-invocation-pipeline/04-05-SUMMARY.md`.
</output>
