---
phase: 03-model-provider-registry-and-openai-compatible-adapter
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeStreamingModelClient.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java
  - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeModelClient.java
  - pi-testkit/src/test/java/io/github/pi_java/agent/testkit/GeneralAgentLoopStreamingTest.java
  - pi-testkit/src/test/java/io/github/pi_java/agent/testkit/FakeStreamingModelClientTest.java
autonomous: true
requirements:
  - MODEL-02
  - MODEL-03
  - MODEL-05
must_haves:
  truths:
    - "General Agent loop can consume Pi-owned streaming model chunks and emit ordered model delta events."
    - "General Agent loop receives complete tool-call intents from the model stream and does not know OpenAI fragment semantics."
    - "Cancellation requested during streaming stops model consumption and produces a terminal cancellation/failure outcome without hanging."
  artifacts:
    - path: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeStreamingModelClient.java"
      provides: "Deterministic no-key fake streaming model provider"
    - path: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java"
      provides: "Streaming-aware fake General Agent runtime loop"
    - path: "pi-testkit/src/test/java/io/github/pi_java/agent/testkit/GeneralAgentLoopStreamingTest.java"
      provides: "Streaming runtime contract tests"
  key_links:
    - from: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java"
      to: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/model/StreamingModelClient.java"
      via: "constructor overload or adapter wrapper"
      pattern: "StreamingModelClient"
    - from: "pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java"
      to: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/RunEventPayload.java"
      via: "publish MODEL_DELTA for streaming text chunks"
      pattern: "MODEL_DELTA"
---

<objective>
Evolve the deterministic testkit runtime loop to validate provider-neutral streaming semantics.

Purpose: Before wiring a real OpenAI-compatible adapter, the platform needs no-key tests proving the General Agent loop can consume streaming text, complete tool-call intents, usage/finish/error metadata, timeout, and cancellation through Pi-owned contracts. This addresses D-04 through D-07, D-17, and D-18/D-19 at the runtime-contract level.
Output: FakeStreamingModelClient, streaming-aware GeneralAgentLoop path, and deterministic streaming tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-CONTEXT.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-02-SUMMARY.md
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeModelClient.java
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/EventCollector.java

<interfaces>
Created by Plan 03-02:
```java
StreamingModelClient
ModelStreamChunk
ModelUsage
ModelFinishReason
ProviderErrorSummary
```

Existing `GeneralAgentLoop` currently calls:
```java
ModelResponse response = modelClient.next(new ModelRequest(context, toolResults), context.cancellationToken());
```
Preserve backwards compatibility for existing tests while adding streaming-aware construction/path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic fake streaming model client</name>
  <files>pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeStreamingModelClient.java, pi-testkit/src/test/java/io/github/pi_java/agent/testkit/FakeStreamingModelClientTest.java</files>
  <behavior>
    - FakeStreamingModelClient scripts ordered ModelStreamChunk values and emits them deterministically.
    - It can script text deltas, complete tool-call intents, usage/finish chunks, normalized errors, delayed chunks, and cancellation triggers.
    - It never needs real provider API keys or network access.
  </behavior>
  <action>Implement a testkit fake against the Plan 03-02 StreamingModelClient contract. Keep it simple and deterministic like `FakeModelClient`; do not add Spring, Reactor, sleeps longer than tiny deterministic hooks, or real HTTP.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit -Dtest=FakeStreamingModelClientTest test</automated>
  </verify>
  <done>FakeStreamingModelClient supports scripted streaming chunks and cancellation behavior under unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make GeneralAgentLoop consume streaming chunks</name>
  <files>pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java, pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeModelClient.java, pi-testkit/src/test/java/io/github/pi_java/agent/testkit/GeneralAgentLoopStreamingTest.java</files>
  <behavior>
    - Streaming text chunks publish one or more MODEL_DELTA events in sequence before terminal RUN_COMPLETED.
    - A complete tool-call-intent chunk publishes TOOL_PROPOSED and flows through existing fake policy/tool invoker path.
    - Usage/finish chunks are captured in model event payloads where the Domain payload supports it.
    - Cancellation before/during streaming returns RunStatus.CANCELLED and emits exactly one terminal event last.
    - Provider error chunk maps to RunStatus.FAILED with PiError.Category.MODEL and no raw secret content.
  </behavior>
  <action>Add a constructor overload or adapter wrapper so existing `GeneralAgentLoop(ModelClient, ...)` tests still pass, while new tests can pass a `StreamingModelClient`. The runtime loop should handle provider-neutral chunks only; do not model OpenAI incremental fragments or execute tools from provider callbacks. Preserve existing max-step/deadline/cancellation behavior.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit test</automated>
  </verify>
  <done>Testkit GeneralAgentLoop validates streaming text, tool-call intents, usage/error metadata, cancellation, and existing synchronous tests remain green.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-testkit test`.
</verification>

<success_criteria>
- MODEL-02 is proven usable by the General Agent loop via fake streaming model.
- MODEL-03 normalized stream chunks become ordered RunEvents.
- MODEL-05 cancellation/timeout/error behavior is covered without real model keys.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-04-SUMMARY.md`.
</output>
