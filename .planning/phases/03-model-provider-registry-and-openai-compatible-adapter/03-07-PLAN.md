---
phase: 03-model-provider-registry-and-openai-compatible-adapter
plan: 07
type: execute
wave: 5
depends_on:
  - 03-03
  - 03-04
  - 03-05
  - 03-06
files_modified:
  - pi-agent-adapter-web/pom.xml
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ModelProviderBeanConfiguration.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java
  - pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcherModelRefTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ModelProviderWiringIntegrationTest.java
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-04
  - MODEL-05
must_haves:
  truths:
    - "Cloud runtime composition can wire the provider registry, secret resolver, and OpenAI-compatible streaming model client."
    - "Queued runs no longer hardcode `default-model`; they resolve/use explicit `provider:model` model refs."
    - "No-key tests still use fake streaming model client when provider config/API key is absent."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ModelProviderBeanConfiguration.java"
      provides: "Spring composition for provider registry, secret resolver, and OpenAI-compatible adapter"
    - path: "pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java"
      provides: "Dispatcher modelRef/provider selection integration"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ModelProviderWiringIntegrationTest.java"
      provides: "No-key wiring regression coverage"
  key_links:
    - from: "pi-agent-adapter-web/pom.xml"
      to: "pi-agent-infrastructure-model-openai"
      via: "adapter web depends on provider infrastructure module at composition root only"
      pattern: "pi-agent-infrastructure-model-openai"
    - from: "CloudRuntimeBeanConfiguration"
      to: "ModelProviderBeanConfiguration"
      via: "import/bean composition, not component scan of Domain/App"
      pattern: "ModelProviderBeanConfiguration"
    - from: "DefaultRunDispatcher"
      to: "AgentDefinition.modelRef"
      via: "configured provider:model default or queued run metadata"
      pattern: "openai-compatible:"
---

<objective>
Wire provider registry and OpenAI-compatible streaming model into the Cloud Server composition root.

Purpose: Phase 3 must make the real provider path usable by the General Agent loop while preserving no-key CI defaults. This plan integrates the provider module at the Adapter composition boundary per COLA, replacing hardcoded `default-model` behavior with `provider:model` resolution without exposing secrets.
Output: Web adapter dependency/wiring, model provider bean configuration, dispatcher modelRef integration, and no-key wiring tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-CONTEXT.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-03-SUMMARY.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-04-SUMMARY.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-05-SUMMARY.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-06-SUMMARY.md
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
@pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java
@pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/TestCloudRuntimeConfiguration.java

<interfaces>
Existing composition root:
```java
@Configuration(proxyBeanMethods = false)
@EnableScheduling
public class CloudRuntimeBeanConfiguration { ... @Bean RunDispatcher runDispatcher(... AgentRuntime agentRuntime, ...); }
```

Existing dispatcher hardcoded model ref:
```java
return new AgentDefinition(..., "default-model", ...);
```
Replace/reroute this toward configured `provider:model`, defaulting to `openai-compatible:<configured-default-model>` only when provider config exists; tests must preserve fake/no-key path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire provider beans in Adapter composition root</name>
  <files>pi-agent-adapter-web/pom.xml, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ModelProviderBeanConfiguration.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ModelProviderWiringIntegrationTest.java</files>
  <behavior>
    - Web adapter depends on `pi-agent-infrastructure-model-openai`; Domain/App still do not.
    - ModelProviderBeanConfiguration creates ModelProviderRegistry and SecretResolver beans from properties/env.
    - OpenAI-compatible StreamingModelClient/AgentRuntime bean is only used when provider config/credential refs are present or explicit property enables it; default tests can still override with fake AgentRuntime.
    - No bean string output or startup logs expose raw secrets.
  </behavior>
  <action>Add a dedicated `ModelProviderBeanConfiguration` instead of bloating `CloudRuntimeBeanConfiguration`. Use Spring configuration properties only in Adapter/Infrastructure boundary. Import or reference it from CloudRuntimeBeanConfiguration if needed. Preserve the existing `TestCloudRuntimeConfiguration` fake runtime path for no-key tests. Do not create Admin UI or REST provider management endpoints in this phase.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=ModelProviderWiringIntegrationTest test</automated>
  </verify>
  <done>Provider registry/secret resolver/OpenAI adapter can be wired by Spring while no-key fake runtime tests remain possible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace dispatcher hardcoded default-model with provider:model selection</name>
  <files>pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java, pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcherModelRefTest.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java</files>
  <behavior>
    - DefaultRunDispatcher can accept configured AgentDefinition/runtime limits or a modelRef supplier/config value using explicit provider:model format per D-02.
    - Existing constructor remains compatible for current tests where possible, but default modelRef becomes `openai-compatible:<default>` or test-provided fake value, not bare `default-model`.
    - Invalid modelRef is rejected before runtime dispatch rather than surfacing as provider SDK errors.
  </behavior>
  <action>Update dispatcher integration so queued run execution uses an explicit provider:model model ref. Prefer constructor injection from Adapter config over making Infrastructure read Spring properties directly. Add tests that invalid/bare model refs fail fast and valid `openai-compatible:gpt-test` is accepted. Preserve Phase 2 cancellation/timeout behavior.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure,pi-agent-adapter-web -Dtest=DefaultRunDispatcherModelRefTest,ModelProviderWiringIntegrationTest test</automated>
  </verify>
  <done>Dispatcher/modelRef wiring honors explicit provider:model format and no-key tests remain green.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am test`.
</verification>

<success_criteria>
- MODEL-01 registry resolution is wired at runtime composition.
- MODEL-02 adapter can be selected by Cloud Server config.
- MODEL-04 secret refs are resolved only in Infrastructure/Adapter boundary.
- MODEL-05 cancellation/timeout wiring remains compatible with worker dispatch.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-07-SUMMARY.md`.
</output>
