---
phase: 03-model-provider-registry-and-openai-compatible-adapter
plan: 08
type: execute
wave: 6
depends_on:
  - 03-06
  - 03-07
files_modified:
  - pi-agent-infrastructure-model-openai/src/test/java/io/github/pi_java/agent/infrastructure/model/openai/OpenAiCompatibleProviderContractTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerOpenAiCompatibleE2ETest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/OptionalRealProviderSmokeTest.java
  - docs/phase-03-model-provider-contracts.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
user_setup:
  - service: openai-compatible-provider
    why: "Optional real-provider smoke test only; normal CI/local verification must use fake provider and require no key."
    env_vars:
      - name: PI_OPENAI_COMPATIBLE_BASE_URL
        source: "OpenAI-compatible provider dashboard or local gateway URL"
      - name: PI_OPENAI_COMPATIBLE_API_KEY
        source: "Provider dashboard API key"
      - name: PI_OPENAI_COMPATIBLE_MODEL
        source: "Provider model ID"
must_haves:
  truths:
    - "Fake OpenAI-compatible contract tests cover text streaming, fragmented tool-call aggregation, usage presence/absence, finish, errors, timeout, cancellation, and secret redaction."
    - "Cloud Server headless E2E can run with fake OpenAI-compatible streaming provider and persist/replay normalized model events without real API keys."
    - "Optional real-provider smoke test is gated and skipped when env/profile is absent."
    - "Requirements MODEL-01..MODEL-05 are marked validated with a provider contract document."
  artifacts:
    - path: "pi-agent-infrastructure-model-openai/src/test/java/io/github/pi_java/agent/infrastructure/model/openai/OpenAiCompatibleProviderContractTest.java"
      provides: "Fake SSE/provider contract coverage"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerOpenAiCompatibleE2ETest.java"
      provides: "Cloud Server fake provider E2E"
    - path: "docs/phase-03-model-provider-contracts.md"
      provides: "Downstream provider registry/adapter contract index"
  key_links:
    - from: "OpenAiCompatibleProviderContractTest"
      to: "OpenAiCompatibleStreamingModelClient"
      via: "fake OpenAI-compatible streaming endpoint or fake stream source"
      pattern: "fragment|usage|429|timeout|cancel|secret"
    - from: "CloudServerOpenAiCompatibleE2ETest"
      to: "RunEventStore/SSE event history"
      via: "create run, stream/persist model.delta, query replay"
      pattern: "MODEL_DELTA|model.delta"
---

<objective>
Complete Phase 3 verification, optional smoke testing, and downstream contract documentation.

Purpose: The phase is only done when provider behavior is verified without real keys, raw secrets are proven absent from visible surfaces, and downstream phases know what provider contracts exist. This implements D-18, D-19, D-20 and closes MODEL-01..MODEL-05.
Output: Fake provider contract tests, Cloud Server fake provider E2E, optional real-provider smoke test, docs, and requirements status updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-CONTEXT.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-06-SUMMARY.md
@.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-07-SUMMARY.md
@pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerHeadlessE2ETest.java
@pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/TestCloudRuntimeConfiguration.java

<interfaces>
Phase 2 no-key E2E pattern:
- `CloudServerHeadlessE2ETest` validates REST/SSE/persistence without real model keys.
- `TestCloudRuntimeConfiguration` supplies a test-only fake AgentRuntime.

Phase 3 must preserve this pattern: normal CI/local verification cannot require real OpenAI-compatible API keys per D-18/D-20.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fake OpenAI-compatible provider contract tests</name>
  <files>pi-agent-infrastructure-model-openai/src/test/java/io/github/pi_java/agent/infrastructure/model/openai/OpenAiCompatibleProviderContractTest.java</files>
  <behavior>
    - Test text delta streaming and terminal finish reason.
    - Test fragmented tool-call argument aggregation into one complete ToolCallIntent.
    - Test optional usage present and missing usage cases.
    - Test latency metadata exists and is non-negative.
    - Test 429/5xx mapping and retry only before any emitted chunk.
    - Test mid-stream provider error is not retried after emitted output.
    - Test timeout and CancellationToken cancellation stop upstream stream.
    - Test raw secret values do not appear in chunks, errors, exception messages, or captured logs where available.
  </behavior>
  <action>Use a fake OpenAI-compatible SSE endpoint, WireMock/MockWebServer, or the module's fake stream source depending on dependencies available from prior plans. Do not use real API keys. Keep assertions provider-neutral: validate Pi ModelStreamChunk/ProviderErrorSummary outputs, not raw Spring AI/OpenAI classes.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-model-openai -Dtest=OpenAiCompatibleProviderContractTest test</automated>
  </verify>
  <done>Provider contract test suite covers D-19 cases and passes without network credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Cloud Server fake provider E2E and gated real-provider smoke</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerOpenAiCompatibleE2ETest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/OptionalRealProviderSmokeTest.java</files>
  <behavior>
    - Fake provider E2E creates a run through REST, activates worker, observes/persists normalized model.delta events, queries event history, and verifies terminal status.
    - Fake provider E2E asserts raw fake API key/secret does not appear in API responses or RunEvent payloads.
    - OptionalRealProviderSmokeTest is disabled unless an explicit profile/env gate is present and all required env vars are set.
    - Absence of real provider env vars never fails normal Maven test runs.
  </behavior>
  <action>Follow Phase 2's `CloudServerHeadlessE2ETest` style. Reuse fake provider/test configuration rather than Docker if Docker is unavailable. Add optional smoke test with JUnit assumptions or `@EnabledIfEnvironmentVariable` plus profile checks; it should be safe to skip by default. Do not add Admin UI provider screens.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=CloudServerOpenAiCompatibleE2ETest,OptionalRealProviderSmokeTest test</automated>
  </verify>
  <done>Cloud Server fake provider E2E passes without keys and real-provider smoke is safely gated/skipped by default.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 3 contracts and update requirement status</name>
  <files>docs/phase-03-model-provider-contracts.md, .planning/REQUIREMENTS.md</files>
  <action>Create `docs/phase-03-model-provider-contracts.md` summarizing provider/model registry contracts, `provider:model` ref format, StreamingModelClient/ModelStreamChunk semantics, OpenAI-compatible adapter boundaries, CredentialRef/SecretRef/SecretResolver rules, resilience behavior, no-key fake provider tests, optional real smoke env vars, and explicit deferrals (Anthropic/Gemini, direct HTTP adapter, Admin UI provider management, Vault/KMS). Update `.planning/REQUIREMENTS.md` to mark MODEL-01..MODEL-05 as validated by Phase 3 only after automated tests in Tasks 1-2 pass.</action>
  <verify>
    <automated>test -f docs/phase-03-model-provider-contracts.md && grep -q "MODEL-01" .planning/REQUIREMENTS.md && grep -q "OpenAI-compatible" docs/phase-03-model-provider-contracts.md</automated>
  </verify>
  <done>Downstream contract doc exists and MODEL-01..MODEL-05 are marked complete/validated in requirements tracking.</done>
</task>

</tasks>

<verification>
Run the full non-Docker phase gate:
`JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test`

If optional real-provider env vars are configured, also run the explicit smoke profile command documented in `docs/phase-03-model-provider-contracts.md`.
</verification>

<success_criteria>
- Fake OpenAI-compatible contract suite passes without real keys.
- Cloud Server fake provider E2E proves streaming model events pass through runtime, persistence, and query/replay.
- Raw secrets are absent from tested logs/errors/events/API responses.
- Optional real smoke test is gated and skipped by default.
- MODEL-01..MODEL-05 are documented and marked validated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-provider-registry-and-openai-compatible-adapter/03-08-SUMMARY.md`.
</output>
