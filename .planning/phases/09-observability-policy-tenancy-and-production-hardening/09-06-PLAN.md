---
phase: 09-observability-policy-tenancy-and-production-hardening
plan: 06
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - pi-agent-adapter-web/pom.xml
  - pi-agent-adapter-web/src/main/resources/application.yml
  - pi-agent-adapter-web/src/main/resources/logback-spring.xml
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ObservabilityBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/SecurityConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ObservabilityConfigurationTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ActuatorSecurityTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/StructuredLoggingRedactionTest.java
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Cloud Server exposes configuration-driven Prometheus/OTLP hooks without external backends in default tests."
    - "Structured JSON logs include safe correlation fields and exclude fake secrets."
    - "Actuator metrics/prometheus endpoints are covered by authenticated endpoint policy."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/resources/application.yml"
      provides: "Actuator, Prometheus, OTLP, and virtual-thread config"
    - path: "pi-agent-adapter-web/src/main/resources/logback-spring.xml"
      provides: "Structured JSON logging with MDC fields"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ObservabilityBeanConfiguration.java"
      provides: "PiTelemetry bean composition"
  key_links:
    - from: "application.yml"
      to: "/actuator/prometheus"
      via: "management endpoint exposure"
      pattern: "prometheus"
    - from: "CorrelationFilter MDC"
      to: "structured JSON logs"
      via: "logback MDC provider"
      pattern: "traceId|correlationId|tenantId|userId"
---

<objective>
Add production observability configuration, structured logging, and actuator security tests.

Purpose: Implements D-03, D-10, D-15, D-16, D-19, D-20, and D-21 at the Cloud Server configuration boundary.
Output: Adapter observability bean config, Prometheus/OTLP properties, JSON logging config, and security/redaction tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-CONTEXT.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-RESEARCH.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-02-SUMMARY.md

<interfaces>
Current config:
```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure PiTelemetry beans, Prometheus, OTLP, and actuator endpoint policy</name>
  <files>pi-agent-adapter-web/pom.xml, pi-agent-adapter-web/src/main/resources/application.yml, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ObservabilityBeanConfiguration.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/SecurityConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ObservabilityConfigurationTest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ActuatorSecurityTest.java</files>
  <read_first>
    - pi-agent-adapter-web/pom.xml
    - pi-agent-adapter-web/src/main/resources/application.yml
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/SecurityConfiguration.java
    - pi-agent-infrastructure-observability/pom.xml
  </read_first>
  <behavior>
    - `application.yml` exposes `health,info,metrics,prometheus` and configures OTLP metrics/logging endpoints via disabled-by-default env placeholders.
    - Adapter Web has a `PiTelemetry` bean using the Spring `MeterRegistry` and configured OTel tracer/noop fallback.
    - `/actuator/prometheus` is authenticated in application security tests while health remains accessible as currently intended.
  </behavior>
  <action>Add `io.micrometer:micrometer-registry-prometheus` to `pi-agent-adapter-web/pom.xml` if not already present. Add dependency on `pi-agent-infrastructure-observability`. Update `application.yml`: `management.endpoints.web.exposure.include: health,info,metrics,prometheus`; add `management.otlp.metrics.export.url: ${PI_OTLP_METRICS_URL:}` and `management.otlp.logging.endpoint: ${PI_OTLP_LOGS_URL:}` with comments if YAML style allows. Create `ObservabilityBeanConfiguration` producing `PiTelemetry`, `PiTelemetryRedactor`, and no-external-backend default tracer behavior. Update security only as needed so actuator health/info retain existing behavior and metrics/prometheus require authenticated access. Do not expose a public unauthenticated metrics endpoint.</action>
  <acceptance_criteria>
    - `pi-agent-adapter-web/pom.xml` contains `micrometer-registry-prometheus`.
    - `pi-agent-adapter-web/pom.xml` contains `pi-agent-infrastructure-observability`.
    - `application.yml` contains `prometheus` under management exposure.
    - `application.yml` contains `PI_OTLP_METRICS_URL` and `PI_OTLP_LOGS_URL`.
    - `ActuatorSecurityTest.java` asserts unauthenticated `/actuator/prometheus` is not 200 and authenticated access succeeds or is authorized according to current dev auth pattern.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web,pi-agent-infrastructure-observability -am -Dtest=ObservabilityConfigurationTest,ActuatorSecurityTest test</automated>
  </verify>
  <done>Cloud Server has production-ready observability configuration hooks without default external backend requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structured JSON logging with MDC correlation and redaction tests</name>
  <files>pi-agent-adapter-web/pom.xml, pi-agent-adapter-web/src/main/resources/logback-spring.xml, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/StructuredLoggingRedactionTest.java</files>
  <read_first>
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java
    - pi-agent-adapter-web/pom.xml
    - pi-agent-adapter-web/src/main/resources/application.yml
  </read_first>
  <behavior>
    - JSON logs include MDC fields `traceId`, `correlationId`, `tenantId`, `userId`, `sessionId`, and `runId` when present.
    - Fake sensitive value `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK` is redacted or absent from structured logs.
    - Default test captures logs in-memory; no external log collector is required.
  </behavior>
  <action>Add `net.logstash.logback:logstash-logback-encoder` version property/dependency if Boot does not provide structured logging sufficient for JSON MDC fields. Create `logback-spring.xml` with a console JSON encoder including timestamp, level, logger, message, thread, and MDC fields `traceId`, `correlationId`, `tenantId`, `userId`, `sessionId`, `runId`. Add a simple redaction turbo filter/provider or ensure logging helper sanitizes fake secrets before log emission. Add `StructuredLoggingRedactionTest` that sets MDC values, logs a message containing `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK`, captures the encoded event, asserts correlation fields exist, and asserts the fake secret does not appear.</action>
  <acceptance_criteria>
    - `logback-spring.xml` exists.
    - `logback-spring.xml` contains `traceId`, `correlationId`, `tenantId`, `userId`, `sessionId`, and `runId`.
    - `StructuredLoggingRedactionTest.java` contains `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK`.
    - `StructuredLoggingRedactionTest.java` asserts the fake secret is absent or `[REDACTED]` in captured output.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=StructuredLoggingRedactionTest test</automated>
  </verify>
  <done>Structured logs include safe correlation context and extend redaction coverage to the logging surface.</done>
</task>

</tasks>

<verification>
Run task commands and `grep -q "prometheus" pi-agent-adapter-web/src/main/resources/application.yml && grep -q "traceId" pi-agent-adapter-web/src/main/resources/logback-spring.xml`.
</verification>

<success_criteria>
Cloud Server can be configured for Prometheus/OTLP/JSON logs safely, while tests remain no-backend and authenticated endpoint policy is verified.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-06-SUMMARY.md`.
</output>
