---
phase: 09-observability-policy-tenancy-and-production-hardening
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolExecutionGateway.java
  - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolPolicyEvaluator.java
  - pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolExecutionGatewayTest.java
  - pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolPolicyEvaluatorTest.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ToolTelemetryWiringTest.java
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Every ToolExecutionGateway call records redacted tool execution metrics and spans."
    - "Every ToolPolicyEvaluator decision records redacted decision metrics and spans."
    - "Tool/policy telemetry includes decision/status but not raw args/results/secrets."
  artifacts:
    - path: "pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolExecutionGateway.java"
      provides: "Tool gateway decorator"
    - path: "pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolPolicyEvaluator.java"
      provides: "Policy evaluator decorator"
  key_links:
    - from: "ToolGovernanceBeanConfiguration"
      to: "DefaultToolExecutionGateway"
      via: "TelemetryToolExecutionGateway wrapper"
      pattern: "TelemetryToolExecutionGateway"
    - from: "ToolPolicyEvaluator"
      to: "policy metrics"
      via: "TelemetryToolPolicyEvaluator"
      pattern: "pi.policy.decisions.total"
---

<objective>
Instrument governed tool execution and policy decision chokepoints.

Purpose: Implements D-02, D-04, D-10, D-19, D-21, and D-22 for the most security-sensitive Phase 9 telemetry surfaces.
Output: Tool gateway and policy evaluator decorators, Adapter Web wiring, and redaction tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-CONTEXT.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-RESEARCH.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-02-SUMMARY.md
@docs/phase-04-governed-tool-contracts.md

<interfaces>
```java
public interface ToolExecutionGateway { ToolExecutionResult execute(ToolExecutionCommand command); }
public interface ToolPolicyEvaluator { PolicyEvaluation evaluate(PolicyEvaluationRequest request); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ToolExecutionGateway telemetry decorator</name>
  <files>pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolExecutionGateway.java, pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolExecutionGatewayTest.java</files>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolExecutionGateway.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultToolExecutionGateway.java
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/tool/ToolExecutionResult.java
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/tool/ToolExecutionRequest.java
  </read_first>
  <behavior>
    - Successful tool execution increments `pi.tool.executions.total` and records `pi.tool.execution.duration` with tags `tool_id`, `status`, `policy_decision` if available.
    - Failed delegate execution increments same counter/timer with `status=error` and rethrows.
    - Tags/spans never include raw `request.arguments()` or `result.rawOutput()` values.
  </behavior>
  <action>Create `TelemetryToolExecutionGateway implements ToolExecutionGateway`. It wraps a delegate and `PiTelemetry`. Open `PiTelemetryContext.from(command.context())`. Start span `pi.tool.execute` and timer `pi.tool.execution.duration`. Use sanitized tags: `tool_id` from `command.request().toolId()`, `status` from `ToolExecutionResult.status().name()` or `error`, and `policy_decision` from redacted result/event context if available else `unknown`. Increment `pi.tool.executions.total`. Never tag raw arguments, raw outputs, or summaries. Add tests using `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK` in arguments/raw output and assert meter tags/span attributes do not contain it.</action>
  <acceptance_criteria>
    - `TelemetryToolExecutionGateway.java` implements `ToolExecutionGateway`.
    - `TelemetryToolExecutionGateway.java` contains `pi.tool.executions.total` or `PiTelemetryNames.TOOL_EXECUTIONS_TOTAL`.
    - `TelemetryToolExecutionGatewayTest.java` contains `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK`.
    - `TelemetryToolExecutionGatewayTest.java` asserts raw arguments are not metric tags.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability -am -Dtest=TelemetryToolExecutionGatewayTest test</automated>
  </verify>
  <done>Tool gateway calls emit safe metrics/spans without exposing raw tool data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add policy decision telemetry decorator and Adapter wiring</name>
  <files>pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolPolicyEvaluator.java, pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryToolPolicyEvaluatorTest.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ToolTelemetryWiringTest.java</files>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/tool/ToolPolicyEvaluator.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/tool/DefaultToolPolicyEvaluator.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
  </read_first>
  <behavior>
    - Policy evaluation increments `pi.policy.decisions.total` with low-cardinality tags `decision`, `policy_ref`, and `tool_id`.
    - Policy evaluation span contains safe attributes and no raw tool arguments/results.
    - Adapter Web composes default policy and tool gateway through telemetry decorators.
  </behavior>
  <action>Create `TelemetryToolPolicyEvaluator implements ToolPolicyEvaluator`. Wrap delegate evaluation, open context from `PolicyEvaluationRequest.context()`, record span `pi.policy.evaluate`, increment `pi.policy.decisions.total` with sanitized `decision`, `policy_ref`, `tool_id`, and `status=success|error`. In `ToolGovernanceBeanConfiguration`, wrap the `ToolPolicyEvaluator` bean and the `ToolExecutionGateway` bean with telemetry decorators if `PiTelemetry` is available; keep existing default fallback for tests. Add `ToolTelemetryWiringTest` asserting configured beans include `TelemetryToolPolicyEvaluator` and `TelemetryToolExecutionGateway` in the chain.</action>
  <acceptance_criteria>
    - `TelemetryToolPolicyEvaluator.java` implements `ToolPolicyEvaluator`.
    - `TelemetryToolPolicyEvaluator.java` contains `pi.policy.decisions.total` or `PiTelemetryNames.POLICY_DECISIONS_TOTAL`.
    - `ToolGovernanceBeanConfiguration.java` contains `TelemetryToolPolicyEvaluator` and `TelemetryToolExecutionGateway`.
    - `ToolTelemetryWiringTest.java` asserts telemetry wrappers are used.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability,pi-agent-adapter-web -am -Dtest=TelemetryToolPolicyEvaluatorTest,ToolTelemetryWiringTest test</automated>
  </verify>
  <done>Policy and tool execution telemetry are wired through Cloud Server composition with safe default verification.</done>
</task>

</tasks>

<verification>
Run task commands plus `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability,pi-agent-adapter-web -am -Dtest=TelemetryToolExecutionGatewayTest,TelemetryToolPolicyEvaluatorTest,ToolTelemetryWiringTest test`.
</verification>

<success_criteria>
Governed tool and policy decisions emit safe metrics/spans while preserving the single ToolExecutionGateway safety boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-04-SUMMARY.md`.
</output>
