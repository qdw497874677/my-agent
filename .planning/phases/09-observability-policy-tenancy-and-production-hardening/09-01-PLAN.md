---
phase: 09-observability-policy-tenancy-and-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/common/PlatformIds.java
  - pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/common/PlatformIdsTest.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilterTest.java
  - pi-agent-infrastructure/src/main/resources/db/migration/V3__w3c_trace_ids.sql
  - pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/persistence/TraceIdMigrationTest.java
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "New API-created trace IDs are W3C-compatible 32-character lowercase hex values."
    - "Historical UUID-shaped trace IDs are converted deterministically while correlation IDs remain unchanged."
    - "API/SSE/audit/log correlation can continue to expose both traceId and correlationId."
  artifacts:
    - path: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/common/PlatformIds.java"
      provides: "TraceId validation and deterministic migration helper"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java"
      provides: "W3C-shaped request trace IDs and preserved correlation headers"
    - path: "pi-agent-infrastructure/src/main/resources/db/migration/V3__w3c_trace_ids.sql"
      provides: "Trace ID data compatibility migration"
  key_links:
    - from: "CorrelationFilter"
      to: "RequestContext/RunEvent trace IDs"
      via: "request attribute pi.traceId"
      pattern: "[0-9a-f]{32}"
    - from: "V3__w3c_trace_ids.sql"
      to: "run/session/event/audit trace columns"
      via: "replace UUID hyphens and lowercase"
      pattern: "replace\(.*trace_id.*'-'.*''\)"
---

<objective>
Normalize Pi trace IDs to W3C-compatible shape and migrate historical persisted data.

Purpose: Implements D-06, D-07, and D-08 so platform trace IDs can align with OpenTelemetry while keeping correlationId as a separate operator field.
Output: Domain trace validation, request generation update, Flyway migration, and focused tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-CONTEXT.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-RESEARCH.md
@docs/phase-02-cloud-server-api.md

<interfaces>
Existing contracts:
```java
public record TraceId(String value) { }
public record CorrelationId(String value) { }
public class CorrelationFilter extends OncePerRequestFilter {
  public static final String TRACE_ATTRIBUTE = "pi.traceId";
  public static final String CORRELATION_ATTRIBUTE = "pi.correlationId";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add W3C TraceId validation and request generation</name>
  <files>pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/common/PlatformIds.java, pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/common/PlatformIdsTest.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilterTest.java</files>
  <read_first>
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/common/PlatformIds.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java
    - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/ApprovalControllerTest.java
  </read_first>
  <behavior>
    - `new TraceId("0123456789abcdef0123456789abcdef")` succeeds.
    - `new TraceId("00000000000000000000000000000000")`, UUID strings with hyphens, uppercase values, and blank values throw `IllegalArgumentException`.
    - `TraceId.fromLegacyUuid("123e4567-e89b-12d3-a456-426614174000")` returns `123e4567e89b12d3a456426614174000`.
    - `CorrelationFilter` generates a trace ID matching `[0-9a-f]{32}` and still echoes `X-Correlation-ID` unchanged.
  </behavior>
  <action>Implement D-06 and D-08. In `PlatformIds.TraceId`, validate exactly 32 lowercase hex characters and reject the all-zero W3C-invalid trace ID. Add static helper `TraceId.newRandom()` using UUID random bits formatted as 32 lowercase hex, and `TraceId.fromLegacyUuid(String legacy)` that trims, removes hyphens from UUID-shaped values, lowercases, validates, and returns a `TraceId`. Keep `CorrelationId` unchanged and permissive. In `CorrelationFilter`, replace `UUID.randomUUID().toString()` for traceId with `PlatformIds.TraceId.newRandom().value()`. Do not change `correlationId` header generation or response behavior.</action>
  <acceptance_criteria>
    - `PlatformIds.java` contains `public static TraceId newRandom()` inside or associated with `TraceId`.
    - `PlatformIds.java` contains `fromLegacyUuid`.
    - `PlatformIds.java` contains regex or equivalent check for 32 lowercase hex characters.
    - `CorrelationFilter.java` does not assign `traceId = UUID.randomUUID().toString()`.
    - `CorrelationFilterTest.java` asserts generated trace id matches `[0-9a-f]{32}`.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-domain,pi-agent-adapter-web -am -Dtest=PlatformIdsTest,CorrelationFilterTest test</automated>
  </verify>
  <done>New request trace IDs and Domain TraceId values are W3C-compatible while correlationId behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic historical trace-id Flyway migration</name>
  <files>pi-agent-infrastructure/src/main/resources/db/migration/V3__w3c_trace_ids.sql, pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/persistence/TraceIdMigrationTest.java</files>
  <read_first>
    - pi-agent-infrastructure/src/main/resources/db/migration/V1__create_cloud_runtime_tables.sql
    - pi-agent-infrastructure/src/main/resources/db/migration/V2__tool_governance_indexes.sql
    - pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/persistence
  </read_first>
  <behavior>
    - UUID-shaped `trace_id` values in `sessions`, `runs`, `run_events`, `audit_records`, and `run_queue` become lowercase hyphenless 32-hex values.
    - Already W3C-shaped `trace_id` values remain unchanged.
    - `correlation_id` values remain byte-for-byte unchanged.
  </behavior>
  <action>Implement D-07. Create `V3__w3c_trace_ids.sql` with update statements for every existing trace_id column from V1 (`sessions`, `runs`, `run_events`, `audit_records`, `run_queue`). Use SQL equivalent to `lower(replace(trace_id, '-', ''))` only when `trace_id` matches UUID-with-hyphens shape. Do not update `correlation_id`. Add `TraceIdMigrationTest` using the existing Flyway/Testcontainers or JDBC migration test pattern to insert representative UUID-shaped and W3C-shaped rows, run migrations, and assert converted trace IDs plus unchanged correlation IDs.</action>
  <acceptance_criteria>
    - `V3__w3c_trace_ids.sql` exists.
    - `V3__w3c_trace_ids.sql` contains update statements for `sessions`, `runs`, `run_events`, `audit_records`, and `run_queue`.
    - `V3__w3c_trace_ids.sql` contains `replace(` and `trace_id`.
    - `TraceIdMigrationTest.java` asserts `correlation_id` remains unchanged.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure -am -Dtest=TraceIdMigrationTest test</automated>
  </verify>
  <done>Historical persisted trace IDs have an explicit tested migration path to W3C shape without losing correlationId continuity.</done>
</task>

</tasks>

<verification>
Run both task commands and `grep -R "traceId = UUID.randomUUID().toString" -n pi-agent-adapter-web/src/main/java; test $? -eq 1`.
</verification>

<success_criteria>
All new and migrated trace IDs are W3C-compatible, and both traceId and correlationId remain available across API/SSE/audit/log paths.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-01-SUMMARY.md`.
</output>
