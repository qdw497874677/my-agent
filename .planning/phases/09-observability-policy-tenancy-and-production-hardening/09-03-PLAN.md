---
phase: 09-observability-policy-tenancy-and-production-hardening
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySink.java
  - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryRunDispatcher.java
  - pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySinkTest.java
  - pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryRunDispatcherTest.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Persisted run/model/tool lifecycle events produce redacted Micrometer metrics and OTel-compatible spans."
    - "Run dispatch duration/status is measured without changing Domain/App contracts."
    - "Worker-thread MDC/span context is explicitly restored from queued run context."
  artifacts:
    - path: "pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySink.java"
      provides: "Event-derived lifecycle telemetry"
    - path: "pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryRunDispatcher.java"
      provides: "Run dispatch timing/context decorator"
  key_links:
    - from: "PersistingEventSink / RunEventFanout"
      to: "RunEventTelemetrySink"
      via: "composite event/fanout wrapper"
      pattern: "pi.run.events.total"
    - from: "DefaultRunDispatcher"
      to: "PiTelemetryContext"
      via: "TelemetryRunDispatcher decorator"
      pattern: "pi.run.dispatch.duration"
---

<objective>
Instrument the persisted run event and dispatcher lifecycle chokepoints.

Purpose: Implements D-02, D-04, D-09, D-10, D-19, D-20, and D-22 for run/model/tool event telemetry and run dispatch timing.
Output: Event-derived metrics/spans, dispatcher decorator, Adapter Web composition, and no-key tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-CONTEXT.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-RESEARCH.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-02-SUMMARY.md

<interfaces>
Existing chokepoints:
```java
public interface EventSink { void publish(RunEvent event); }
public class PersistingEventSink implements EventSink { public void publish(RunEvent event); }
public interface RunDispatcher { void dispatch(String workerId); void dispatchRun(String workerId, String runId); }
public class DefaultRunDispatcher implements RunDispatcher { }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RunEvent-derived telemetry sink</name>
  <files>pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySink.java, pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySinkTest.java</files>
  <read_first>
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/RunEvent.java
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/RunEventType.java
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/RunEventPayload.java
    - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/PiTelemetryNames.java
    - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/PiTelemetryContext.java
  </read_first>
  <behavior>
    - Publishing a `run.completed` or equivalent terminal event increments `pi.run.events.total` with tags `event_type` and `status`.
    - Publishing `model.delta` and `tool.*` events increments event metrics without raw payload values in tags.
    - Fake sensitive values in event payload maps do not appear in meter tag values or span attributes.
  </behavior>
  <action>Create `RunEventTelemetrySink` in the observability module. It should accept a delegate `EventSink` plus `PiTelemetry`; on `publish(RunEvent event)`, open `PiTelemetryContext.from(event)`, record counter `pi.run.events.total` with tags: `event_type=<event.type().wireName()>`, `terminal=<true|false>`, `source=run_event`, and a sanitized status tag derived only from event type/category (`completed`, `failed`, `cancelled`, `timed_out`, `policy`, `unknown`). Then start/end a span named `pi.run.event` with safe attributes `pi.event_type`, `pi.tenant_id`, `pi.session_id`, `pi.run_id`, `pi.trace_id`, `pi.correlation_id`. Call delegate exactly once and preserve delegate exceptions. Do not inspect or tag raw event payload maps except through redacted safe summaries.</action>
  <acceptance_criteria>
    - `RunEventTelemetrySink.java` contains `pi.run.events.total` or `PiTelemetryNames.RUN_EVENTS_TOTAL`.
    - `RunEventTelemetrySink.java` delegates to `delegate.publish(event)` exactly once.
    - `RunEventTelemetrySinkTest.java` uses `SimpleMeterRegistry`.
    - `RunEventTelemetrySinkTest.java` includes fake sensitive string `PI_PHASE9_FAKE_SECRET_DO_NOT_LEAK` and asserts it is absent from meter IDs/tags or spans.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability -am -Dtest=RunEventTelemetrySinkTest test</automated>
  </verify>
  <done>Persisted event lifecycle telemetry is derived from the same event truth consumed by SSE and audit paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run dispatcher telemetry decorator and wire it</name>
  <files>pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/TelemetryRunDispatcher.java, pi-agent-infrastructure-observability/src/test/java/io/github/pi_java/agent/infrastructure/observability/TelemetryRunDispatcherTest.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java</files>
  <read_first>
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/RunDispatcher.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
    - pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/PiTelemetryContext.java
  </read_first>
  <behavior>
    - `dispatch(workerId)` and `dispatchRun(workerId, runId)` record `pi.run.dispatch.duration` with status `success` or `error`.
    - Decorator opens explicit telemetry scope and restores MDC cleanup even when delegate throws.
    - Adapter Web composes `RunDispatcher` through telemetry when observability beans are present.
  </behavior>
  <action>Create `TelemetryRunDispatcher implements RunDispatcher` wrapping a delegate. For each call, start a timer sample and span named `pi.run.dispatch`; tag `operation=dispatch` or `dispatchRun`, `status=success|error`, and sanitized `worker_id`. For `dispatchRun`, include sanitized `run_id`. Do not tag tenant/user because dispatcher interface lacks full context; downstream event telemetry carries persisted context. Ensure exceptions are rethrown. In `CloudRuntimeBeanConfiguration`, wrap the existing `DefaultRunDispatcher` bean in `new TelemetryRunDispatcher(defaultDispatcher, piTelemetry)` if/when `PiTelemetry` bean is available; otherwise keep default behavior. Add wiring test asserting the bean class or delegate chain contains `TelemetryRunDispatcher` and default no-key tests still pass.</action>
  <acceptance_criteria>
    - `TelemetryRunDispatcher.java` implements `RunDispatcher`.
    - `TelemetryRunDispatcher.java` contains `pi.run.dispatch.duration` or `PiTelemetryNames.RUN_DISPATCH_DURATION`.
    - `TelemetryRunDispatcherTest.java` asserts error status when delegate throws.
    - `RunTelemetryWiringTest.java` asserts Adapter Web composes telemetry dispatcher.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability,pi-agent-adapter-web -am -Dtest=TelemetryRunDispatcherTest,RunTelemetryWiringTest test</automated>
  </verify>
  <done>Run dispatch timing and error telemetry are active in Cloud Server without Domain/App dependency leakage.</done>
</task>

</tasks>

<verification>
Run task commands plus `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-observability,pi-agent-adapter-web -am -Dtest=RunEventTelemetrySinkTest,TelemetryRunDispatcherTest,RunTelemetryWiringTest test`.
</verification>

<success_criteria>
Run lifecycle telemetry covers persisted event truth and worker dispatch timing with explicit safe context propagation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-03-SUMMARY.md`.
</output>
