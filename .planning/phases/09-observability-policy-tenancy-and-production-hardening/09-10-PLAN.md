---
phase: 09-observability-policy-tenancy-and-production-hardening
plan: 10
type: execute
wave: 6
depends_on: [09-09]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java
autonomous: true
requirements: [OPS-01]
gap_closure: true
must_haves:
  truths:
    - "Platform emits structured logs, metrics, and OpenTelemetry-compatible spans for real production RunEvent lifecycles, not only unit-test sink invocations."
    - "Admin Governance operations run metrics receive real production run-event meter data from the EventSink path."
    - "Default Cloud Server behavior is preserved when PiTelemetry is absent."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java"
      provides: "Production EventSink composition that wraps PersistingEventSink with RunEventTelemetrySink when telemetry exists"
      contains: "RunEventTelemetrySink"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java"
      provides: "Spring wiring proof for run dispatcher and EventSink telemetry decorators"
      contains: "eventSinkIsComposedThroughRunEventTelemetryDecorator"
  key_links:
    - from: "CloudRuntimeBeanConfiguration.eventSink(...)"
      to: "RunEventTelemetrySink"
      via: "ObjectProvider<PiTelemetry> optional wrapper around PersistingEventSink"
      pattern: "new RunEventTelemetrySink"
    - from: "RunEventTelemetrySink"
      to: "MicrometerOperationsMetricsReader runs section"
      via: "PiTelemetryNames.RUN_EVENTS_TOTAL meter produced from persisted RunEvent publication"
      pattern: "pi.run.events.total"
---

<objective>
Close the Phase 9 verification gap by wiring the already-implemented run-event telemetry sink into the production Cloud Server EventSink path.

Purpose: Verification found `RunEventTelemetrySink` is substantive and tested but orphaned from production wiring. This plan connects it so OPS-01 run lifecycle metrics/spans and Admin operations run metrics are real, while preserving no-telemetry fallback behavior.
Output: Adapter Web composition change, wiring regression coverage, and selected Phase 9 smoke evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-CONTEXT.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-VERIFICATION.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-02-SUMMARY.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-03-SUMMARY.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-07-SUMMARY.md
@.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-09-SUMMARY.md
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
@pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java
@pi-agent-infrastructure-observability/src/main/java/io/github/pi_java/agent/infrastructure/observability/RunEventTelemetrySink.java

<interfaces>
Key contracts the executor needs:

From `io.github.pi_java.agent.domain.event.EventSink`:
```java
public interface EventSink {
    void publish(RunEvent event);
}
```

From `RunEventTelemetrySink`:
```java
public final class RunEventTelemetrySink implements EventSink {
    public static final String RUN_EVENT_SPAN = "pi.run.event";
    public RunEventTelemetrySink(EventSink delegate, PiTelemetry telemetry);
    public void publish(RunEvent event);
}
```

From current `CloudRuntimeBeanConfiguration.eventSink(...)`:
```java
@Bean
EventSink eventSink(TransactionTemplate transactionTemplate,
        RunEventStore runEventStore,
        RunProjectionRepository runProjectionRepository,
        RunEventFanout runEventFanout) {
    return new PersistingEventSink(transactionTemplate, runEventStore, runProjectionRepository, runEventFanout);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire RunEventTelemetrySink into production EventSink composition</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java</files>
  <behavior>
    - When a `PiTelemetry` bean is available, the Spring `EventSink` bean is a `RunEventTelemetrySink` whose delegate is the existing `PersistingEventSink`.
    - When no `PiTelemetry` bean is available, the Spring `EventSink` bean remains the existing `PersistingEventSink` behavior with no telemetry dependency required.
    - The existing `RunDispatcher` telemetry wiring test remains green.
  </behavior>
  <action>Implement the verification missing items exactly. In `CloudRuntimeBeanConfiguration`, import `RunEventTelemetrySink` and add `ObjectProvider<PiTelemetry>` to the `eventSink(...)` bean method. Build the current `PersistingEventSink` first as the delegate. If `piTelemetry.getIfAvailable()` returns `null`, return the delegate unchanged; otherwise return `new RunEventTelemetrySink(delegate, telemetry)`. This implements D-02 and D-04 from CONTEXT by deriving lifecycle metrics from the persisted EventSink path, and implements D-09/D-10 because `RunEventTelemetrySink` already uses explicit `PiTelemetryContext.from(event)` and redacted tags. Do not move telemetry classes into Domain/App/client and do not inspect raw event payload maps.</action>
  <acceptance_criteria>
    - `CloudRuntimeBeanConfiguration.eventSink(...)` references `ObjectProvider<PiTelemetry>` and `RunEventTelemetrySink`.
    - The method still constructs `PersistingEventSink` exactly once as the durable persist-then-fanout delegate.
    - No Micrometer/OpenTelemetry imports are added outside Adapter Web composition or the observability infrastructure module.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=RunTelemetryWiringTest test</automated>
  </verify>
  <done>Production EventSink publication path emits run-event telemetry whenever PiTelemetry is configured and remains unchanged otherwise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove run metrics flow into Admin operations and rerun Phase 9 smoke gate</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunTelemetryWiringTest.java</files>
  <behavior>
    - Publishing a safe fake `RunEvent` through the Spring `EventSink` increments `PiTelemetryNames.RUN_EVENTS_TOTAL` in the in-memory test `MeterRegistry`.
    - The wiring test proves the Admin operations runs data source is no longer hollow by producing the same meter `MicrometerOperationsMetricsReader` reads.
    - Selected Phase 9 no-key smoke tests pass without Docker, Prometheus, OTLP Collector, model keys, or real MCP servers.
  </behavior>
  <action>Extend `RunTelemetryWiringTest` with an event-sink wiring assertion. Reuse the existing Spring Boot test context and available test `MeterRegistry`/`PiTelemetry` beans from `ObservabilityBeanConfiguration`. Fetch the `EventSink` bean, assert it is a `RunEventTelemetrySink`, publish a minimal valid `RunEvent` using W3C-shaped trace ID and safe tenant/user/session/run/workspace IDs, then assert `meterRegistry.find(PiTelemetryNames.RUN_EVENTS_TOTAL).counter()` is non-null and has count `1.0`. If a direct delegate assertion requires production accessors that do not exist, prefer the observable meter assertion over adding public production getters. Do not use reflection unless unavoidable. Then run the selected Phase 9 smoke gate that includes run-event sink, operations reader, Admin operations controller/UI, redaction, and critical-path tests.</action>
  <acceptance_criteria>
    - `RunTelemetryWiringTest` contains `eventSinkIsComposedThroughRunEventTelemetryDecorator` or equivalently named test.
    - Test publishes through the Spring `EventSink` bean rather than constructing `RunEventTelemetrySink` directly.
    - Test observes `PiTelemetryNames.RUN_EVENTS_TOTAL` incrementing in the Spring test meter registry.
    - The selected Phase 9 Maven smoke command exits 0.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-domain,pi-agent-infrastructure,pi-agent-infrastructure-observability,pi-agent-infrastructure-model-openai,pi-agent-infrastructure-mcp,pi-agent-infrastructure-plugin,pi-agent-adapter-web -am -Dtest=PlatformIdsTest,TraceIdMigrationTest,PiTelemetryArchitectureTest,RunEventTelemetrySinkTest,RunTelemetryWiringTest,MicrometerOperationsMetricsReaderTest,AdminOperationsControllerTest,AdminOperationsViewTest,Phase09TelemetryRedactionRegressionTest,Phase09CriticalPathRegressionTest test</automated>
  </verify>
  <done>Verification gaps for run-event telemetry production wiring and Admin operations run metrics are closed with automated evidence.</done>
</task>

</tasks>

<verification>
Run both task verification commands. Then confirm `grep -R "new RunEventTelemetrySink" -n pi-agent-adapter-web/src/main/java` finds production composition and `grep -R "io\.micrometer\|io\.opentelemetry\|ch\.qos\.logback" -n pi-agent-domain/src/main pi-agent-app/src/main pi-agent-client/src/main pi-agent-extension-api/src/main; test $? -eq 1` still passes.
</verification>

<success_criteria>
Phase 9 gap closure succeeds when `RunEventTelemetrySink` is wired into production EventSink composition, a Spring wiring test proves published events produce `pi.run.events.total`, Admin operations run metrics are no longer hollow, and the selected no-key Phase 9 smoke gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability-policy-tenancy-and-production-hardening/09-10-SUMMARY.md`.
</output>
