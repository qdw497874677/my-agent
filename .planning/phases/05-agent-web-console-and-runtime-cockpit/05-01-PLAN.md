---
phase: 05-agent-web-console-and-runtime-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - pi-agent-adapter-web/pom.xml
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/SecurityConfig.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/MainConsoleLayout.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/AdminGovernanceLayout.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/EventStreamClient.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebConsoleFoundationTest.java
autonomous: true
requirements: [GUI-08]
must_haves:
  truths:
    - "Web Console routes can be served by the Spring Boot web adapter without weakening /api authentication"
    - "Vaadin UI code lives only in Adapter Web and calls public HTTP/SSE endpoints"
  artifacts:
    - path: "pi-agent-adapter-web/pom.xml"
      provides: "Vaadin Flow/Spring production dependencies in Adapter Web only"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java"
      provides: "Public REST client boundary for Vaadin views"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/EventStreamClient.java"
      provides: "Browser-facing SSE connection helper using public stream endpoint"
  key_links:
    - from: "Vaadin views"
      to: "/api/** and /api/**/stream"
      via: "ConsoleHttpClient/EventStreamClient only"
      pattern: "ConsoleHttpClient|EventStreamClient"
---

<objective>
Create the Vaadin Web Console foundation, route separation, security allowances, and public API/SSE client boundary required by every Phase 5 UI plan.

Purpose: Satisfy D-03 and GUI-08 before feature views exist; Vaadin must remain Adapter-only and must not access private App/Domain/DB objects.
Output: Vaadin dependency setup, two route layouts, security config, and reusable REST/SSE client helpers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-agent-web-console-and-runtime-cockpit/05-CONTEXT.md
@.planning/phases/05-agent-web-console-and-runtime-cockpit/05-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-10-SUMMARY.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-11-SUMMARY.md

<interfaces>
Existing endpoints to preserve:
- `POST /api/sessions`, `GET /api/sessions/{sessionId}`, `GET /api/sessions/{sessionId}/history`
- `POST /api/sessions/{sessionId}/runs`, `GET /api/sessions/{sessionId}/runs/{runId}/events`, `POST /api/sessions/{sessionId}/runs/{runId}/cancel`
- `GET /api/sessions/{sessionId}/runs/{runId}/stream` produces `text/event-stream` with run-scoped sequence ids.
Existing security: `SecurityConfig` authenticates `/api/**`, permits `/actuator/health` and `/actuator/info`, and denies other routes. This plan must add Vaadin route/static-resource access without making `/api/**` public.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Vaadin Adapter-only dependency and route security foundation</name>
  <files>pom.xml, pi-agent-adapter-web/pom.xml, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/SecurityConfig.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebConsoleFoundationTest.java</files>
  <behavior>
    - Test 1: Maven dependency tree shows Vaadin only in `pi-agent-adapter-web`, not Domain/App.
    - Test 2: `/api/sessions` remains authenticated while `/console` and `/admin/governance` Vaadin routes are reachable under dev/test auth.
  </behavior>
  <action>Add Vaadin Flow/Spring dependency management compatible with Spring Boot 3.5.x and add Vaadin dependencies only to `pi-agent-adapter-web`. Update `SecurityConfig` so Vaadin routes/static resources are served while `/api/**` remains authenticated and health endpoints remain public. Follow Vaadin security ordering from research: default Vaadin resources/routes before final catch-all, no redirect-loop-prone early `anyRequest`. Implement per D-03 two UI route families: `/console` and `/admin/governance`; do not add any provider/tool/policy mutation routes.</action>
  <verify><automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=WebConsoleFoundationTest test</automated></verify>
  <done>Vaadin compiles in Adapter Web, `/api/**` auth behavior is preserved, and test routes for Console/Admin are reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create public REST/SSE client helpers and separate layouts</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/MainConsoleLayout.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/AdminGovernanceLayout.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/EventStreamClient.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebConsoleFoundationTest.java</files>
  <behavior>
    - Test 1: UI package has no direct imports from `io.github.pi_java.agent.app.usecase`, `io.github.pi_java.agent.domain`, or persistence repositories.
    - Test 2: `ConsoleHttpClient` builds authenticated relative URLs for `/api/...`; `EventStreamClient` builds the existing run stream URL with `afterSequence`.
  </behavior>
  <action>Create `MainConsoleLayout` for the user surface and `AdminGovernanceLayout` for the separated admin surface per D-03. Add `ConsoleHttpClient` as the only Vaadin-side REST boundary using public `pi-agent-client` DTO classes and relative `/api` endpoints. Add `EventStreamClient` that supplies browser-side connection metadata for `/api/sessions/{sessionId}/runs/{runId}/stream?afterSequence=N`. Do not inject App services, repositories, runtime objects, or tool/model gateways into Vaadin views; GUI-08 requires public APIs only.</action>
  <verify><automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=WebConsoleFoundationTest test</automated></verify>
  <done>Layouts and client helpers exist, tests enforce public API-only access, and downstream UI plans can consume these helpers.</done>
</task>
</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am test` after both tasks if focused tests pass.
</verification>

<success_criteria>
- Vaadin is Adapter-only.
- User Console and Admin Governance have distinct layout/route roots per D-03.
- `/api/**` remains authenticated and is the only data boundary for Vaadin per D-04/GUI-08.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-web-console-and-runtime-cockpit/05-01-SUMMARY.md`.
</output>
