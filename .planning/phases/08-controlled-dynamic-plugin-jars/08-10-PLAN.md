---
phase: 08-controlled-dynamic-plugin-jars
plan: 10
type: execute
wave: 8
depends_on: [08-09]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginToolRegistryWiringTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginGovernanceApiTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarE2ETest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarCompatibilityE2ETest.java
  - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginInfrastructureArchitectureTest.java
autonomous: true
gap_closure: true
requirements: [PLUG-01, PLUG-02, PLUG-05, E2E-08]
must_haves:
  truths:
    - "Cloud Server plugin ToolRegistry uses dynamic current plugin contributions for new run resolution."
    - "Admin REST refresh rediscoveries can make a newly copied sample plugin available."
    - "Adapter Web production code does not import PF4J directly."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java"
      provides: "Spring composition using plugin infrastructure discovery and dynamic registry only"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarCompatibilityE2ETest.java"
      provides: "Product-path disable/quarantine/refresh regression coverage"
    - path: "pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginInfrastructureArchitectureTest.java"
      provides: "PF4J leak gate including Adapter Web production packages"
  key_links:
    - from: "AdminGovernanceController POST /plugins/{id}/disable"
      to: "ToolExecutionGateway new run resolution"
      via: "PluginGovernanceCatalogAdapter state store + DynamicPluginToolRegistry"
      pattern: "DynamicPluginToolRegistry"
    - from: "POST /api/admin/governance/plugins/refresh"
      to: "controlled directory PF4J rediscovery"
      via: "Pf4jControlledPluginDiscoveryService"
      pattern: "refresh\(\)"
    - from: "PluginInfrastructureArchitectureTest"
      to: "Adapter Web production package"
      via: "ArchUnit importPackages/noClasses rule"
      pattern: "io.github.pi_java.agent.adapter.web.config"
---

<objective>
Wire the dynamic plugin infrastructure into the live Cloud Server product path and prove Admin mutations affect new runs.

Purpose: Close the verifier's product-path gaps for disable/quarantine, manual refresh, and PF4J isolation in Adapter Web.
Output: PF4J-free Adapter Web composition, dynamic plugin ToolRegistry wiring, Admin REST product-path tests, and expanded architecture gate.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-CONTEXT.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-VERIFICATION.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-09-SUMMARY.md

<interfaces>
<!-- Expected Plan 08-09 outputs consumed here. -->
```java
public final class Pf4jControlledPluginDiscoveryService {
    public Pf4jControlledPluginDiscoveryService(PluginRegistryProperties properties);
    public List<Pf4jPluginSourceDiscovery.PluginDiscoveredSource> discover();
}

public final class DynamicPluginToolRegistry implements ToolRegistry {
    public DynamicPluginToolRegistry(Supplier<DefaultExtensionContributionRegistry> contributionSupplier);
}

public final class PluginGovernanceCatalogAdapter {
    public DefaultExtensionContributionRegistry contributionRegistry();
    public PluginMutationStatus refresh();
    public PluginMutationStatus disable(String pluginId, String actor, String reason);
    public PluginMutationStatus quarantine(String pluginId, String actor, String reason);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire Adapter Web to use plugin infrastructure discovery and dynamic registry</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginToolRegistryWiringTest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginGovernanceApiTest.java</files>
  <read_first>
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
    - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginToolRegistryWiringTest.java
    - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginGovernanceApiTest.java
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/Pf4jControlledPluginDiscoveryService.java
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/DynamicPluginToolRegistry.java
  </read_first>
  <behavior>
    - Adapter Web production code must not import `org.pf4j..`.
    - `pluginGovernanceCatalogAdapter` must be constructed from `Pf4jControlledPluginDiscoveryService` and use refreshable discovery, not a one-time Adapter-local `discoverPlugins()` helper.
    - `pluginToolRegistry` bean must return `new DynamicPluginToolRegistry(adapter::contributionRegistry)` when plugins are enabled and configured.
  </behavior>
  <action>Remove `DefaultPluginManager`, `PluginManager`, and direct PF4J imports from `PluginGovernanceBeanConfiguration`. Replace the private `discoverPlugins(PluginRegistryProperties)` helper with a plugin-infrastructure bean `Pf4jControlledPluginDiscoveryService pluginDiscoveryService(PluginRegistryProperties properties)`. Construct `PluginGovernanceCatalogAdapter` with current discovery from that service plus a refresh callback/supplier so `refresh()` re-runs controlled-directory discovery. Change the `@Qualifier("pluginToolRegistry") ToolRegistry` bean to return `new DynamicPluginToolRegistry(adapter::contributionRegistry)` instead of `new ExtensionToolRegistry(adapter.contributionRegistry())`. Preserve disabled/no-directory `EmptyToolRegistry` fallback. Keep Admin REST endpoints unchanged.</action>
  <acceptance_criteria>
    - `PluginGovernanceBeanConfiguration.java` contains no `org.pf4j` import.
    - `PluginGovernanceBeanConfiguration.java` contains `Pf4jControlledPluginDiscoveryService`.
    - `PluginGovernanceBeanConfiguration.java` contains `new DynamicPluginToolRegistry(adapter::contributionRegistry)`.
    - `PluginGovernanceApiTest` still asserts no plugin upload/install/delete/upgrade routes exist.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=PluginToolRegistryWiringTest,PluginGovernanceApiTest test</automated>
  </verify>
  <done>Cloud Server composes plugin discovery through infrastructure and the active plugin registry is dynamic for new list/resolve calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add product-path refresh and disable/quarantine regression tests</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarE2ETest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarCompatibilityE2ETest.java</files>
  <read_first>
    - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarE2ETest.java
    - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SamplePluginJarCompatibilityE2ETest.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
  </read_first>
  <behavior>
    - Product-path test disables the real sample plugin through `POST /api/admin/governance/plugins/sample-readonly-plugin/disable`, then proves `/api/tools` no longer contains `plugin.sample.readonly.lookup` and a new REST-created run that requests the sample plugin tool cannot complete a plugin tool invocation.
    - Product-path test quarantines through `POST /api/admin/governance/plugins/sample-readonly-plugin/quarantine`, then proves new resolution remains unavailable and governance status is `QUARANTINED`.
    - Refresh test starts with an empty controlled directory, copies the sample plugin JAR into that directory, calls `POST /api/admin/governance/plugins/refresh`, and then observes the sample capability through `GET /api/admin/governance/plugins` and `GET /api/tools`.
  </behavior>
  <action>Extend sample plugin E2E coverage using the existing no-key SpringBootTest patterns. Add helper methods for Admin POST mutation calls and plugin-directory preparation. For disable/quarantine, avoid manually rebuilding `PluginGovernanceCatalogAdapter`; assert the live Spring context `ToolRegistry` path changes after REST mutation. For refresh, create a separate test class or nested configuration whose `pi.plugins.directory` points at a temp directory initially empty; copy `SamplePluginJarE2ETest.samplePluginJar()` into it after startup; call refresh; assert response contains `REFRESHED`, then assert plugin/tool visibility. Use deterministic fake runtime/model/tool setup already present; do not add Docker, network, real model keys, plugin upload routes, or hot-watch behavior.</action>
  <acceptance_criteria>
    - `SamplePluginJarCompatibilityE2ETest` no longer proves disable/quarantine only by constructing a new adapter after state-store mutation; it includes REST product-path disable/quarantine assertions or is replaced by a product-path equivalent.
    - Test source contains `POST` path `/api/admin/governance/plugins/sample-readonly-plugin/disable` or formatted equivalent.
    - Test source contains `POST` path `/api/admin/governance/plugins/sample-readonly-plugin/quarantine` or formatted equivalent.
    - Test source contains `/api/admin/governance/plugins/refresh` and asserts `REFRESHED`.
    - After disable/quarantine, test source asserts absence of `plugin.sample.readonly.lookup` from `/api/tools` or failed/empty tool resolution for a new run.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-sample-plugin-readonly,pi-agent-adapter-web -am -Dtest=SamplePluginJarE2ETest,SamplePluginJarCompatibilityE2ETest test</automated>
  </verify>
  <done>Admin refresh/disable/quarantine are verified through live Cloud Server REST and affect new registry resolution/new runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand PF4J isolation architecture gate to Adapter Web production code</name>
  <files>pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginInfrastructureArchitectureTest.java</files>
  <read_first>
    - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginInfrastructureArchitectureTest.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
  </read_first>
  <behavior>
    - ArchUnit must fail if Adapter Web production packages import `org.pf4j..`.
    - The gate should still allow the sample plugin module/tests to depend on PF4J where intentionally needed for packaging and product-path tests.
    - The gate should continue protecting Domain, App, client, extension API, Spring starter, MCP, and model modules from plugin infrastructure/PF4J imports.
  </behavior>
  <action>Update `PluginInfrastructureArchitectureTest` to import `io.github.pi_java.agent.adapter.web` production packages in the forbidden PF4J rule and include `io.github.pi_java.agent.adapter.web..` in the `noClasses().that().resideInAnyPackage(...)` package list for `org.pf4j..` dependencies. Keep `io.github.pi_java.agent.infrastructure.plugin..` allowed to use PF4J. If forbidding all plugin infrastructure imports from Adapter Web would block intended composition, split the rule: forbid `org.pf4j..` for Adapter Web, while retaining the existing stricter plugin-infrastructure package leak rule for core/App/client/API/starter/MCP/model modules. Do not include test packages or `io.github.pi_java.agent.sample.plugin` in the forbidden set.</action>
  <acceptance_criteria>
    - `PluginInfrastructureArchitectureTest.java` contains `io.github.pi_java.agent.adapter.web` in `importPackages`.
    - `PluginInfrastructureArchitectureTest.java` has an ArchUnit rule where Adapter Web production code is checked against `org.pf4j..`.
    - `PluginInfrastructureArchitectureTest.java` does not forbid `io.github.pi_java.agent.sample.plugin` from importing PF4J.
    - `PluginGovernanceBeanConfiguration.java` contains no `org.pf4j` import, so the architecture gate passes.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-plugin,pi-agent-adapter-web -am -Dtest=PluginInfrastructureArchitectureTest test</automated>
  </verify>
  <done>PF4J isolation gate catches Adapter Web production leakage and passes with PF4J isolated in plugin infrastructure/sample plugin scopes.</done>
</task>

</tasks>

<verification>
Run all task commands plus `grep -R "org\.pf4j" -n pi-agent-adapter-web/src/main/java` and confirm it prints nothing.
</verification>

<success_criteria>
The live Cloud Server product path uses refreshable/state-aware plugin governance and Adapter Web no longer owns PF4J implementation details.
</success_criteria>

<output>
After completion, create `.planning/phases/08-controlled-dynamic-plugin-jars/08-10-SUMMARY.md`.
</output>
