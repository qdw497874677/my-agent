---
phase: 08-controlled-dynamic-plugin-jars
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - pi-agent-adapter-web/pom.xml
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginToolRegistryWiringTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/PluginGovernanceApiTest.java
autonomous: true
requirements: [PLUG-01, PLUG-02, PLUG-03, PLUG-04, PLUG-05]
must_haves:
  truths:
    - "Cloud Server can bind controlled plugin directory configuration through Spring properties."
    - "Plugin-provided tool capabilities join the primary ToolRegistry after built-ins, extensions, and MCP without bypassing ToolExecutionGateway."
    - "Admin REST can view, refresh, disable, and quarantine plugins through public DTOs."
  artifacts:
    - path: "PluginGovernanceBeanConfiguration.java"
      provides: "Adapter composition root for plugin properties, bridge, catalog, and fallback"
    - path: "ToolGovernanceBeanConfiguration.java"
      provides: "Primary ToolRegistry composition including plugins"
    - path: "AdminGovernanceController.java"
      provides: "Authenticated plugin governance REST endpoints"
  key_links:
    - from: "ToolGovernanceBeanConfiguration.java"
      to: "PluginGovernanceBeanConfiguration.java"
      via: "plugin ToolRegistry bean added to composite registry"
      pattern: "plugin.*ToolRegistry|Plugin.*ToolRegistry"
    - from: "AdminGovernanceController.java"
      to: "GovernanceQueryService"
      via: "thin controller delegation"
      pattern: "plugins\(|disablePlugin|quarantinePlugin|refreshPlugins"
---

<objective>
Wire plugin infrastructure into Cloud Server composition, primary ToolRegistry, and Admin Governance REST.

Purpose: Make controlled plugins a real runtime source while preserving public DTO boundaries and gateway-only execution.
Output: Adapter Web plugin bean configuration, tool registry composition, REST endpoints, and wiring/API tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-CONTEXT.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-02-SUMMARY.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-03-SUMMARY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-06-SUMMARY.md
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Adapter Web plugin composition and ToolRegistry wiring</name>
  <files>pi-agent-adapter-web/pom.xml, PluginGovernanceBeanConfiguration.java, ToolGovernanceBeanConfiguration.java, PluginToolRegistryWiringTest.java</files>
  <behavior>
    - Binding prefix `pi.plugins` includes enable flag, controlled directory, allowlist, platform API version, startup discovery, and duplicate override controls per D-03.
    - Safe fallback catalog/registry is used when plugins are disabled or directory is absent.
    - Primary ToolRegistry order is built-ins, Phase 6 extensions, Phase 7 MCP, then plugins unless existing code explicitly requires a safer earlier ordering; plugin tools cannot override built-ins by default per D-18 through D-20.
  </behavior>
  <action>Add `pi-agent-infrastructure-plugin` dependency to Adapter Web. Create Spring `@ConfigurationProperties` binding and bean configuration for plugin discovery/catalog/registry. Compose plugin tool registry into the primary governed `ToolRegistry`; do not add plugin-specific invocation APIs.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=PluginToolRegistryWiringTest test</automated>
  </verify>
  <done>Adapter Web can start with plugins disabled, and fake plugin tools appear in the composed ToolRegistry only when enabled/compatible/usable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Admin plugin governance REST endpoints</name>
  <files>AdminGovernanceController.java, PluginGovernanceApiTest.java</files>
  <behavior>
    - `GET /api/admin/governance/plugins` returns plugin status/details/capabilities through `PluginGovernanceResponse` per D-12 and D-16.
    - `POST /api/admin/governance/plugins/refresh` triggers manual rediscovery only; no hot watching or configuration CRUD per D-04 and D-11.
    - `POST /api/admin/governance/plugins/{pluginId}/disable` and `/quarantine` accept optional reason, delegate to App use case, and return audited mutation status; no upload/install/delete/upgrade endpoints per deferred scope.
    - Tests assert fake secrets/raw paths do not appear in JSON per D-13, D-15, and D-21.
  </behavior>
  <action>Extend the thin Admin Governance controller to delegate plugin actions to `GovernanceQueryService`, building `RequestContext` exactly like existing MCP endpoints. Keep endpoint methods authenticated and DTO-only.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=PluginGovernanceApiTest test</automated>
  </verify>
  <done>Plugin REST status/refresh/disable/quarantine endpoints pass and expose only redacted public DTOs.</done>
</task>

</tasks>

<verification>
Run both Adapter Web focused tests; inspect endpoint list to ensure no upload/install/delete/upgrade plugin routes were added.
</verification>

<success_criteria>
Cloud Server composition can load plugin capabilities into the governed registry and expose narrow Admin REST lifecycle operations.
</success_criteria>

<output>
After completion, create `.planning/phases/08-controlled-dynamic-plugin-jars/08-04-SUMMARY.md`.
</output>
