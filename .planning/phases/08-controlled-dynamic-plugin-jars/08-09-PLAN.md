---
phase: 08-controlled-dynamic-plugin-jars
plan: 09
type: execute
wave: 7
depends_on: [08-08]
files_modified:
  - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java
  - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryProperties.java
  - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/Pf4jControlledPluginDiscoveryService.java
  - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/DynamicPluginToolRegistry.java
  - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapterTest.java
  - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginCapabilityDisablementTest.java
  - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryPropertiesTest.java
autonomous: true
gap_closure: true
requirements: [PLUG-01, PLUG-02, PLUG-05, E2E-08]
must_haves:
  truths:
    - "Disabled or quarantined plugin tools are unavailable for new registry resolution without reconstructing the adapter."
    - "Admin refresh can rediscover controlled-directory JARs through an infrastructure-owned PF4J service."
    - "Controlled plugin directory allowlist/selected controls are enforced before capabilities become usable."
  artifacts:
    - path: "pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/Pf4jControlledPluginDiscoveryService.java"
      provides: "PF4J manager construction/loading/rediscovery isolated inside plugin infrastructure"
    - path: "pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java"
      provides: "Refreshable/state-aware plugin governance and contribution registry"
    - path: "pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/DynamicPluginToolRegistry.java"
      provides: "ToolRegistry wrapper that reads current contributions on every list/resolve"
  key_links:
    - from: "PluginGovernanceCatalogAdapter.disable/quarantine"
      to: "DynamicPluginToolRegistry.resolve"
      via: "current contribution registry supplier after state mutation"
      pattern: "contributionRegistry\(\)"
    - from: "PluginGovernanceCatalogAdapter.refresh"
      to: "Pf4jControlledPluginDiscoveryService.discover"
      via: "rediscovery callback/service"
      pattern: "discover\(\)"
    - from: "PluginRegistryProperties.allowedPluginIds/selectedPluginIds"
      to: "PluginGovernanceCatalogAdapter effective contributions"
      via: "selection filter/rejected governance status"
      pattern: "allowedPluginIds|selectedPluginIds"
---

<objective>
Close Phase 8 live-registry semantic gaps inside the isolated plugin infrastructure module.

Purpose: Ensure plugin disable/quarantine, refresh, and allowlist/selected controls affect new plugin capability resolution without leaking PF4J into Adapter Web.
Output: Infrastructure-owned PF4J discovery service, refreshable plugin governance catalog, state-aware plugin ToolRegistry, and focused regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-CONTEXT.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-VERIFICATION.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-02-SUMMARY.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-04-SUMMARY.md
@.planning/phases/08-controlled-dynamic-plugin-jars/08-07-SUMMARY.md

<interfaces>
<!-- Key current contracts the executor must preserve. -->

From `PluginGovernanceCatalogAdapter`:
```java
public final class PluginGovernanceCatalogAdapter {
    public DefaultExtensionContributionRegistry contributionRegistry();
    public List<PluginSourceStatus> plugins();
    public PluginMutationStatus refresh();
    public PluginMutationStatus disable(String pluginId, String actor, String reason);
    public PluginMutationStatus quarantine(String pluginId, String actor, String reason);
}
```

From `PluginRegistryProperties`:
```java
public record PluginRegistryProperties(
        boolean enabled,
        Optional<Path> pluginDirectory,
        boolean loadOnStartup,
        boolean manualRefreshEnabled,
        List<String> allowedPluginIds,
        List<String> selectedPluginIds,
        String platformApiVersion,
        boolean allowDuplicateOverrides,
        boolean nonSandboxWarningAcknowledged) { }
```

From `ToolRegistry` and `ExtensionToolRegistry`:
```java
public interface ToolRegistry {
    List<ToolDescriptor> listTools();
    Optional<ToolResolution> resolve(String toolId);
}
public final class ExtensionToolRegistry implements ToolRegistry {
    public ExtensionToolRegistry(DefaultExtensionContributionRegistry contributions);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move PF4J controlled-directory discovery behind an infrastructure service</name>
  <files>pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/Pf4jControlledPluginDiscoveryService.java, pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryProperties.java, pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryPropertiesTest.java</files>
  <read_first>
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/Pf4jPluginSourceDiscovery.java
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryProperties.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/PluginGovernanceBeanConfiguration.java
    - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryPropertiesTest.java
  </read_first>
  <behavior>
    - `Pf4jControlledPluginDiscoveryService` owns `org.pf4j.DefaultPluginManager` construction, `loadPlugins()`, `startPlugins()`, and `Pf4jPluginSourceDiscovery.discover()` per D-05/D-24 and verification gap 4.
    - If `PluginRegistryProperties.enabled()` is false, `pluginDirectory()` is empty, or `loadOnStartup()` is false, discovery returns `List.of()` without touching PF4J.
    - The service exposes a concrete method `public List&lt;Pf4jPluginSourceDiscovery.PluginDiscoveredSource&gt; discover()` and accepts `PluginRegistryProperties` through its constructor.
  </behavior>
  <action>Create `Pf4jControlledPluginDiscoveryService` in `pi-agent-infrastructure-plugin` with constructor `Pf4jControlledPluginDiscoveryService(PluginRegistryProperties properties)` and method `discover()`. Move the exact PF4J behavior currently in Adapter Web (`new DefaultPluginManager(List.of(directory))`, `loadPlugins()`, `startPlugins()`, `new Pf4jPluginSourceDiscovery(pluginManager, directory, properties.platformApiVersion()).discover()`) into this class. Keep all `org.pf4j..` imports in this infrastructure module only. Do not change public App/client DTOs. Preserve `PluginRegistryProperties.requireValid()` semantics and add tests for disabled/no-directory/no-startup cases if not already present.</action>
  <acceptance_criteria>
    - `Pf4jControlledPluginDiscoveryService.java` contains `new DefaultPluginManager(List.of(directory))`.
    - `Pf4jControlledPluginDiscoveryService.java` contains `pluginManager.loadPlugins()` and `pluginManager.startPlugins()`.
    - `Pf4jControlledPluginDiscoveryService.java` contains `public List&lt;Pf4jPluginSourceDiscovery.PluginDiscoveredSource&gt; discover()`.
    - `PluginGovernanceBeanConfiguration.java` no longer needs any `org.pf4j` import after Plan 08-10 consumes this service.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-plugin -am -Dtest=PluginRegistryPropertiesTest test</automated>
  </verify>
  <done>PF4J manager construction is available behind a plugin-infrastructure service and Adapter Web no longer needs to create PF4J managers directly in later wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make plugin governance contributions refreshable and state-aware</name>
  <files>pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java, pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/DynamicPluginToolRegistry.java, pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapterTest.java, pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginCapabilityDisablementTest.java</files>
  <read_first>
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginStateStore.java
    - pi-agent-infrastructure-extension/src/main/java/io/github/pi_java/agent/infrastructure/extension/DefaultExtensionContributionRegistry.java
    - pi-agent-infrastructure-extension/src/main/java/io/github/pi_java/agent/infrastructure/extension/ExtensionToolRegistry.java
    - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginCapabilityDisablementTest.java
  </read_first>
  <behavior>
    - `PluginGovernanceCatalogAdapter.contributionRegistry()` reflects the current `PluginStateStore` on every call; it must not return a final registry computed only in the constructor.
    - `disable()` and `quarantine()` must update current governance status and subsequent `DynamicPluginToolRegistry.resolve/listTools` without requiring a new adapter object.
    - `refresh()` must invoke a discovery supplier/service and replace the adapter's discovered plugin snapshot for later governance and registry resolution.
  </behavior>
  <action>Change `PluginGovernanceCatalogAdapter` so `discoveredPlugins` is replaceable (for example `AtomicReference&lt;List&lt;PluginDiscoveredSource&gt;&gt;`) and `contributionRegistry()` rebuilds `DefaultExtensionContributionRegistry.build(toDiscoveredSources(), effectiveProperties())` from the latest snapshot and current `PluginStateStore`. Add an overload or constructor accepting a discovery supplier/service for refresh while preserving the existing list-based constructor for tests. Implement `refresh()` to: (1) return failure with action `refresh` and status `REFRESH_DISABLED` when manual refresh is disabled if properties are available; (2) call the supplier/service when enabled; (3) replace the snapshot; (4) return success with status `REFRESHED` and metadata containing `pluginCount`. Create `DynamicPluginToolRegistry implements ToolRegistry` that wraps `Supplier&lt;DefaultExtensionContributionRegistry&gt;` and delegates each `listTools()`/`resolve()` call to `new ExtensionToolRegistry(supplier.get())`. Do not interrupt active runs or promise hot unload per D-07/D-09.</action>
  <acceptance_criteria>
    - `PluginGovernanceCatalogAdapter.java` does not contain `private final DefaultExtensionContributionRegistry contributionRegistry;`.
    - `PluginGovernanceCatalogAdapter.java` contains `DefaultExtensionContributionRegistry.build(toDiscoveredSources(), effectiveProperties())` inside or beneath `contributionRegistry()` rather than only the constructor.
    - `PluginGovernanceCatalogAdapter.java` contains status string `REFRESHED` in `refresh()` success path.
    - `DynamicPluginToolRegistry.java` contains `implements ToolRegistry` and `new ExtensionToolRegistry(contributionSupplier.get())`.
    - `PluginCapabilityDisablementTest` proves disable/quarantine on the same adapter instance makes `new DynamicPluginToolRegistry(adapter::contributionRegistry).resolve(SAMPLE_TOOL_ID)` empty.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-plugin -am -Dtest=PluginGovernanceCatalogAdapterTest,PluginCapabilityDisablementTest test</automated>
  </verify>
  <done>Disable/quarantine and refresh affect current contribution resolution for new list/resolve calls without reconstructing the adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce allowlist and selected plugin controls before contribution</name>
  <files>pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java, pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapterTest.java, pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryPropertiesTest.java</files>
  <read_first>
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapter.java
    - pi-agent-infrastructure-plugin/src/main/java/io/github/pi_java/agent/infrastructure/plugin/PluginRegistryProperties.java
    - pi-agent-infrastructure-plugin/src/test/java/io/github/pi_java/agent/infrastructure/plugin/PluginGovernanceCatalogAdapterTest.java
  </read_first>
  <behavior>
    - If `allowedPluginIds` is non-empty, plugins whose `pluginId` is not in it must not contribute usable capabilities.
    - If `selectedPluginIds` is non-empty, plugins whose `pluginId` is not selected must not contribute usable capabilities even if allowed.
    - Rejected/unselected plugins remain visible in governance with sanitized metadata and a lifecycle/status that clearly indicates unavailable new resolution.
  </behavior>
  <action>Implement selection filtering in `PluginGovernanceCatalogAdapter.effectiveProperties()` or source-to-contribution mapping. Convert not-allowlisted or unselected plugin source IDs into disabled contribution sources so `DefaultExtensionContributionRegistry.usableCapabilities()` excludes them. For governance `PluginSourceStatus`, show `enabled=false`; include metadata keys `selectionStatus` with exact values `NOT_ALLOWLISTED` or `NOT_SELECTED`, and preserve safe relative path/error metadata. Do not silently hide rejected plugins; do not start marketplace/upload/delete workflows.</action>
  <acceptance_criteria>
    - `PluginGovernanceCatalogAdapter.java` references `allowedPluginIds()` and `selectedPluginIds()`.
    - `PluginGovernanceCatalogAdapter.java` contains literal strings `NOT_ALLOWLISTED` and `NOT_SELECTED`.
    - `PluginGovernanceCatalogAdapterTest` contains assertions for `NOT_ALLOWLISTED` and `NOT_SELECTED`.
    - Tests prove not-allowlisted/unselected sample plugin statuses remain visible via `plugins()` while `DynamicPluginToolRegistry.resolve(SAMPLE_TOOL_ID)` is empty.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-plugin -am -Dtest=PluginGovernanceCatalogAdapterTest,PluginRegistryPropertiesTest test</automated>
  </verify>
  <done>Controlled-directory allowlist/selected controls determine plugin contribution availability and are visible in governance.</done>
</task>

</tasks>

<verification>
Run the task commands plus `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-plugin -am test` if time permits.
</verification>

<success_criteria>
Infrastructure plugin state is dynamic for new resolution: refresh rediscovery, disable/quarantine state, and allowlist/selected config all change the current plugin contribution registry used by new ToolRegistry list/resolve calls.
</success_criteria>

<output>
After completion, create `.planning/phases/08-controlled-dynamic-plugin-jars/08-09-SUMMARY.md`.
</output>
