---
phase: 10-responsive-baseline-and-mobile-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/PiWebAppShell.java
  - pi-agent-adapter-web/src/main/frontend/themes/pi-mobile/styles.css
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/console/ConsoleView.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/AdminGovernanceLayout.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebMobileBaselineContractTest.java
autonomous: true
requirements: [MH5-01, MH5-03]
must_haves:
  truths:
    - "Mobile users can open the existing Console/Admin Vaadin shell with a project-owned responsive theme loaded."
    - "Baseline routed pages have mobile-safe global overflow, box sizing, wrapping, and viewport assumptions before route smoke tests run."
    - "Stable route/layout/action markers exist for Console and Admin root surfaces without relying on brittle body text."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/PiWebAppShell.java"
      provides: "Vaadin AppShell/@Theme wiring for project-owned mobile baseline"
      contains: "pi-mobile"
    - path: "pi-agent-adapter-web/src/main/frontend/themes/pi-mobile/styles.css"
      provides: "Global responsive CSS baseline"
      contains: "overflow-x"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebMobileBaselineContractTest.java"
      provides: "Fast contract tests for theme/hooks"
      contains: "data-mobile-critical"
  key_links:
    - from: "PiWebAppShell.java"
      to: "themes/pi-mobile/styles.css"
      via: "@Theme/AppShell theme name"
      pattern: "@Theme\\(\\\"pi-mobile\\\"\\)"
    - from: "ConsoleView.java"
      to: "e2e mobile smoke selectors"
      via: "data-route/data-layout/data-mobile-critical"
      pattern: "data-mobile-critical"
---

<objective>
Establish the Phase 10 Vaadin responsive baseline and stable root UI selector contract.

Purpose: Phase 10 must create mobile-safe defaults before broad UI conversion. This plan implements D-07, D-08, D-10, D-11, and D-12 without starting deferred Phase 11-14 redesign work.
Output: A project-owned Vaadin theme, AppShell/theme wiring, stable Console/Admin root route markers, and fast Java contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsive-baseline-and-mobile-test-harness/10-CONTEXT.md
@docs/phase-05-web-console.md
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/PiCloudServerApplication.java
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/console/ConsoleView.java
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/AdminGovernanceLayout.java

<interfaces>
Existing contracts to preserve:
```java
// ConsoleView.java
@Route("console")
public class ConsoleView extends Div {
  // existing hooks: data-route="console", data-layout="three-column-workbench"
}

// AdminGovernanceLayout.java
@Route("admin/governance")
public class AdminGovernanceLayout extends Main { }
```

Vaadin documentation notes from planning research:
```css
html, body, #outlet { height: 100%; width: 100%; margin: 0; }
```
Use a Vaadin Flow 24 project-owned theme folder under `src/main/frontend/themes/{theme-name}/styles.css` and an AppShell/@Theme entry point. Do not add React/Next/Hilla mobile code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Java contract tests for mobile theme and root route hooks</name>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebMobileBaselineContractTest.java</files>
  <behavior>
    - Test 1: AppShell class exists and is annotated/configured with theme name `pi-mobile`.
    - Test 2: Constructing `ConsoleView` exposes `data-route="console"`, `data-layout="three-column-workbench"`, and a mobile-critical marker.
    - Test 3: Constructing `AdminGovernanceLayout` exposes `data-route="admin-governance"`, `data-layout` or `data-surface`, and a mobile-critical marker.
    - Test 4: `themes/pi-mobile/styles.css` exists and contains baseline tokens/selectors for `box-sizing`, `overflow-x`, `max-width`, and wrapping.
  </behavior>
  <action>Create `WebMobileBaselineContractTest` first and run it to see RED failures. Keep tests fast and no-key; do not start the Spring Boot server. Use direct component construction/reflection/file-content assertions. This implements MH5-01/MH5-03 and decisions D-07, D-10, D-11, D-12.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=WebMobileBaselineContractTest test</automated>
  </verify>
  <done>Focused test exists, initially fails before implementation, and describes theme wiring plus stable root mobile selector contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Vaadin mobile baseline theme</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/PiWebAppShell.java, pi-agent-adapter-web/src/main/frontend/themes/pi-mobile/styles.css</files>
  <behavior>
    - AppShell uses project-owned theme `pi-mobile`.
    - Global CSS sets `box-sizing: border-box`, removes body margin, preserves full-width/full-height app shell, and prevents page-level horizontal scrolling.
    - Global CSS makes text/code/pre content wrap or constrain width instead of forcing page-level overflow.
    - Baseline CSS includes safe-area variables but does not implement a Phase 11 navigation drawer or Phase 14 table-to-card migration.
  </behavior>
  <action>Create `PiWebAppShell` in Adapter Web UI package as the Vaadin theme/AppShell owner. Create `themes/pi-mobile/styles.css` with conservative global rules: `html, body, #outlet` full width/height, universal `box-sizing`, `body { overflow-x: hidden; }`, flexible `img/svg/vaadin-*` max-width defaults where safe, `pre/code` wrapping, and mobile CSS custom properties for spacing/safe-area. Per D-07/D-09, avoid full responsive shell redesign, route navigation changes, final Console flow conversion, and Admin table-to-card migration.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=WebMobileBaselineContractTest test</automated>
  </verify>
  <done>Vaadin app loads a project-owned `pi-mobile` theme with baseline responsive defaults and the focused Java contract test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize root mobile-critical selectors</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/console/ConsoleView.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/AdminGovernanceLayout.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/WebMobileBaselineContractTest.java</files>
  <behavior>
    - Console root keeps `data-route="console"` and `data-layout="three-column-workbench"` and adds `data-mobile-critical="true"` or equivalent stable marker.
    - Admin root route `admin/governance` exposes `data-route="admin-governance"`, an Admin surface/layout marker, and mobile-critical marker.
    - Existing route names and public REST/SSE DTO boundaries remain unchanged.
  </behavior>
  <action>Add only stable `data-*` attributes/classes needed for Phase 10 smoke and future phases, per D-10/D-12. Reuse existing `data-route`/`data-layout` conventions. Do not replace existing selectors wholesale and do not implement deferred Phase 11 navigation or Phase 14 card conversion.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=WebMobileBaselineContractTest test</automated>
  </verify>
  <done>Console/Admin roots have stable mobile-critical route hooks and the focused Java contract test passes.</done>
</task>

</tasks>

<verification>
Run the focused contract gate: `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=WebMobileBaselineContractTest test`.
</verification>

<success_criteria>
- `pi-mobile` theme exists and is wired through Vaadin AppShell.
- Baseline CSS prevents common page-level overflow and text/code overflow without final mobile redesign.
- Console and Admin root routes expose stable data hooks for downstream Playwright smoke tests.
- No production code outside `pi-agent-adapter-web` is touched.
</success_criteria>

<output>
After completion, create `.planning/phases/10-responsive-baseline-and-mobile-test-harness/10-01-SUMMARY.md`.
</output>
