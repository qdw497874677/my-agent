---
phase: 10-responsive-baseline-and-mobile-test-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - scripts/e2e-install.sh
  - e2e/fixtures/mobile-smoke.ts
  - docs/phase-10-mobile-baseline.md
autonomous: true
requirements: [MVER-01]
must_haves:
  truths:
    - "Maintainer can list and run a representative mobile/tablet Playwright project matrix while keeping Desktop Chrome coverage."
    - "CI-supported browser install covers Chromium, Firefox, and WebKit for the Phase 10 matrix where supported."
    - "Known emulation gaps are documented instead of requiring real mobile devices in Phase 10."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Desktop Chrome plus representative mobile/tablet Playwright projects"
      contains: "Mobile Chrome"
    - path: "scripts/e2e-install.sh"
      provides: "Browser install script for chromium/firefox/webkit"
      contains: "chromium firefox webkit"
    - path: "e2e/fixtures/mobile-smoke.ts"
      provides: "Reusable no-overflow and selector helpers for mobile route smoke"
      exports: ["expectNoPageHorizontalOverflow"]
    - path: "docs/phase-10-mobile-baseline.md"
      provides: "Phase 10 matrix and emulation-gap documentation"
      contains: "WebKit-as-Mobile-Safari"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/phase-10-mobile-route-smoke.spec.ts"
      via: "project names and device contexts"
      pattern: "Mobile Chrome|Mobile Safari|Tablet"
    - from: "scripts/e2e-install.sh"
      to: "playwright.config.ts"
      via: "installed browser engines"
      pattern: "chromium firefox webkit"
---

<objective>
Create the representative mobile browser test harness foundation for Phase 10.

Purpose: MVER-01 requires automated representative Mobile Chrome, Mobile Safari/WebKit, Mobile Firefox or Firefox-sized mobile, and tablet contexts while preserving existing Desktop Chrome regression. This plan implements D-01, D-02, D-03, D-13, and D-14.
Output: Playwright projects, browser install support, shared mobile smoke helpers, and documented CI/emulation boundaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsive-baseline-and-mobile-test-harness/10-CONTEXT.md
@playwright.config.ts
@scripts/e2e-install.sh
@scripts/e2e-web-server.sh
@package.json
@e2e/fixtures/fake-runtime.ts

<interfaces>
Existing Playwright config shape:
```typescript
import { defineConfig, devices } from '@playwright/test';
export default defineConfig({
  testDir: './e2e',
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: { command: `bash scripts/e2e-web-server.sh ${port}` }
});
```

Playwright documentation notes from planning research:
```typescript
projects: [
  { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  { name: 'Mobile Chrome', use: { ...devices['Pixel 5'] } },
  { name: 'Mobile Safari', use: { ...devices['iPhone 12'] } },
]
```
Browser install command supports `playwright install chromium firefox webkit`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative Playwright projects</name>
  <files>playwright.config.ts</files>
  <action>Extend `projects` per D-01/D-03: keep the existing Desktop Chrome regression project (do not remove or rename without a compatibility alias), add Mobile Chrome using a Pixel device descriptor, add Mobile Safari using a WebKit/iPhone descriptor, add Mobile Firefox using either Desktop Firefox with a custom phone viewport/hasTouch/isMobile where Playwright device support is limited, and add a tablet/iPad-style context. Keep `fullyParallel: false`, existing dev headers, artifacts, and webServer behavior unchanged. Do not add exhaustive orientation/device matrices in Phase 10.</action>
  <verify>
    <automated>npm run e2e -- --list</automated>
  </verify>
  <done>`npm run e2e -- --list` shows Desktop Chrome plus representative mobile/tablet projects without starting product-flow expansion.</done>
</task>

<task type="auto">
  <name>Task 2: Install browser engines needed by matrix</name>
  <files>scripts/e2e-install.sh</files>
  <action>Update `scripts/e2e-install.sh` to install `chromium firefox webkit` through the existing local Playwright CLI while preserving the current `--with-deps=false` normalization. This implements D-01/D-02. Do not add real-device provisioning or CI-specific secrets.</action>
  <verify>
    <automated>npm run e2e:install -- --with-deps=false</automated>
  </verify>
  <done>Install script can install Chromium, Firefox, and WebKit browser engines for the representative matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared mobile smoke helpers and matrix documentation</name>
  <files>e2e/fixtures/mobile-smoke.ts, docs/phase-10-mobile-baseline.md</files>
  <action>Create a reusable Playwright helper module exporting route metadata types and functions for page-level horizontal overflow checks (`document.documentElement.scrollWidth <= clientWidth + tolerance` and body equivalent), stable selector visibility checks, and primary-action/content assertions. Create `docs/phase-10-mobile-baseline.md` documenting the Phase 10 matrix, exact project names, what each project approximates, WebKit-as-Mobile-Safari limitation, Firefox mobile emulation/proxy limitation if applicable, and the Phase 15 handoff for real-device/UAT per D-13/D-14.</action>
  <verify>
    <automated>npm run e2e -- --list</automated>
  </verify>
  <done>Shared helpers are importable by future smoke specs and docs clearly record CI/emulation gaps without blocking on real devices.</done>
</task>

</tasks>

<verification>
Run `npm run e2e:install -- --with-deps=false` and `npm run e2e -- --list`.
</verification>

<success_criteria>
- Desktop Chrome project remains available.
- Mobile Chrome, Mobile Safari/WebKit, Mobile Firefox/proxy, and tablet projects are configured.
- Browser install covers required engines.
- Phase 10 docs record representative coverage and emulation gaps, not Phase 15 real-device/UAT gates.
</success_criteria>

<output>
After completion, create `.planning/phases/10-responsive-baseline-and-mobile-test-harness/10-02-SUMMARY.md`.
</output>
