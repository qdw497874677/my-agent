---
phase: 01-runtime-spine-workspace-and-domain-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - pi-agent-client/pom.xml
  - pi-agent-domain/pom.xml
  - pi-agent-app/pom.xml
  - pi-agent-infrastructure/pom.xml
  - pi-agent-adapter-web/pom.xml
  - pi-testkit/pom.xml
  - pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/architecture/DomainDependencyArchTest.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/architecture/AppDependencyArchTest.java
autonomous: true
requirements: [CORE-06, CORE-09]
must_haves:
  truths:
    - "Developer can run Maven tests against a Java 21 multi-module COLA skeleton."
    - "Domain module has no Spring, Vaadin, PF4J, MCP, DB, provider SDK, Jackson annotation, or Jakarta annotation dependencies."
    - "COLA dependency direction is executable as an architecture test."
  artifacts:
    - path: "pom.xml"
      provides: "Java 21 Maven parent and dependency/plugin management"
      contains: "<maven.compiler.release>21</maven.compiler.release>"
    - path: "pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/architecture/DomainDependencyArchTest.java"
      provides: "Domain forbidden dependency guard"
      contains: "noClasses().that().resideInAPackage"
    - path: "pi-agent-app/src/test/java/io/github/pi_java/agent/app/architecture/AppDependencyArchTest.java"
      provides: "COLA direction guard for App layer"
      contains: "should().onlyDependOnClassesThat"
  key_links:
    - from: "pom.xml"
      to: "pi-agent-domain/pom.xml"
      via: "parent/module declaration"
      pattern: "<module>pi-agent-domain</module>"
    - from: "pi-agent-app/pom.xml"
      to: "pi-agent-domain"
      via: "Maven dependency"
      pattern: "<artifactId>pi-agent-domain</artifactId>"
---

<objective>
Create the Phase 1 Maven/COLA execution foundation.

Purpose: All later Phase 1 domain, workspace, runtime, and testkit work needs a Java 21 multi-module skeleton plus executable boundary tests before production code is added.
Output: Maven parent, module POMs, empty source trees, and first architecture tests for CORE-06 and CORE-09.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-RESEARCH.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-VALIDATION.md
@README.md

<interfaces>
This plan creates the project structure consumed by all later plans.

Required package namespace per D-23: `io.github.pi_java.agent`.

Required modules per D-20/D-21:
- `pi-agent-client`: client DTO/command/query/response contracts.
- `pi-agent-domain`: Spring-free runtime contracts, ports, entities, state/event/workspace models.
- `pi-agent-app`: use-case orchestration over domain ports.
- `pi-agent-infrastructure`: placeholder Infrastructure module for later port implementations.
- `pi-agent-adapter-web`: placeholder Adapter module for later REST/SSE/Web surface.
- `pi-testkit`: reusable fakes and assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Java 21 Maven multi-module skeleton</name>
  <files>pom.xml, pi-agent-client/pom.xml, pi-agent-domain/pom.xml, pi-agent-app/pom.xml, pi-agent-infrastructure/pom.xml, pi-agent-adapter-web/pom.xml, pi-testkit/pom.xml</files>
  <read_first>
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md`
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-RESEARCH.md`
    - `.planning/ROADMAP.md`
  </read_first>
  <action>Create root `pom.xml` with `groupId` `io.github.pi_java`, `artifactId` `pi-java-agent-platform`, `version` `0.1.0-SNAPSHOT`, `packaging` `pom`, Java release `21`, UTF-8 source/reporting encodings, modules exactly `pi-agent-client`, `pi-agent-domain`, `pi-agent-app`, `pi-agent-infrastructure`, `pi-agent-adapter-web`, and `pi-testkit`. Manage versions for `org.junit.jupiter:junit-jupiter:5.10.3`, `org.assertj:assertj-core:3.26.3`, and `com.tngtech.archunit:archunit-junit5:1.3.0`. Configure `maven-compiler-plugin` `3.13.0` with `<release>21</release>` and `maven-surefire-plugin` `3.2.5` with `<useModulePath>false</useModulePath>`. Create every module POM with the root parent. Per D-20/D-21, make `pi-agent-domain` depend only on test-scoped JUnit/AssertJ/ArchUnit; make `pi-agent-app` depend on `pi-agent-domain` and `pi-agent-client`; make `pi-agent-infrastructure` depend on `pi-agent-domain`; make `pi-agent-adapter-web` depend on `pi-agent-app` and `pi-agent-client`; make `pi-testkit` depend on `pi-agent-domain` and test dependencies. Do not add Spring, Jakarta, Jackson, Vaadin, PF4J, MCP, JDBC, or provider SDK dependencies per D-22.</action>
  <verify>
    <automated>mvn -q -DskipTests=false test</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` contains `<maven.compiler.release>21</maven.compiler.release>`.
    - `pom.xml` contains `<module>pi-agent-domain</module>` and `<module>pi-testkit</module>`.
    - `pi-agent-domain/pom.xml` does not contain `spring`, `jakarta`, `jackson`, `vaadin`, `pf4j`, `mcp`, `jdbc`, or `openai`.
    - `mvn -q -DskipTests=false test` exits 0.
  </acceptance_criteria>
  <done>Maven can build all modules with Java 21 and no production framework dependency has entered Domain.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable COLA architecture gates</name>
  <files>pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/architecture/DomainDependencyArchTest.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/architecture/AppDependencyArchTest.java</files>
  <read_first>
    - `pom.xml`
    - `pi-agent-domain/pom.xml`
    - `pi-agent-app/pom.xml`
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md`
  </read_first>
  <action>Create ArchUnit tests. In `DomainDependencyArchTest`, import `io.github.pi_java.agent.domain..` and assert no class in Domain depends on packages matching `org.springframework..`, `jakarta..`, `javax.persistence..`, `com.vaadin..`, `org.pf4j..`, `io.modelcontextprotocol..`, `org.springframework.ai..`, `com.fasterxml.jackson.annotation..`, `java.sql..`, `javax.sql..`, `dev.langchain4j..`, `com.openai..`, or `software.amazon.awssdk..`. In `AppDependencyArchTest`, import `io.github.pi_java.agent..` and assert classes under `..app..` only depend on Java/JUnit/AssertJ/ArchUnit, `io.github.pi_java.agent.app..`, `io.github.pi_java.agent.domain..`, and `io.github.pi_java.agent.client..`. Use descriptive test method names `domain_must_not_depend_on_outer_frameworks_or_sdks` and `app_must_only_depend_on_client_domain_and_app_packages`. This enforces CORE-06/CORE-09 and D-22/D-26.</action>
  <verify>
    <automated>mvn -q test</automated>
  </verify>
  <acceptance_criteria>
    - `DomainDependencyArchTest.java` contains `org.springframework..` and `com.fasterxml.jackson.annotation..`.
    - `AppDependencyArchTest.java` contains `should().onlyDependOnClassesThat()`.
    - `mvn -q test` exits 0.
  </acceptance_criteria>
  <done>Architecture tests are present and green before domain implementation starts.</done>
</task>

</tasks>

<verification>
Run `mvn test`. Confirm all module POMs exist and `pi-agent-domain/pom.xml` has no production dependencies beyond Java itself.
</verification>

<success_criteria>
- Java 21 Maven multi-module skeleton exists.
- Boundary tests exist and run in CI-style Maven command.
- Domain dependency guard blocks outer framework/SDK pollution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-01-SUMMARY.md`.
</output>
