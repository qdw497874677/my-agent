---
phase: 01-runtime-spine-workspace-and-domain-contracts
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Workspace.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceSession.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceScope.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceSnapshot.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Resource.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Mount.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceGateway.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/CommandExecutionGateway.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/Artifact.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/Attachment.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/ExternalReference.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/Session.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionEntry.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionEntryPayload.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionContext.java
  - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionContextResolver.java
  - pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/workspace/WorkspaceContractsTest.java
  - pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/session/SessionTreeContextResolverTest.java
autonomous: true
requirements: [CORE-02, CORE-08, WORK-01, WORK-02, WORK-04, WORK-05]
must_haves:
  truths:
    - "Workspace is modeled as a first-class runtime boundary, not a host filesystem alias."
    - "Artifacts, attachments, and external references are independent from messages."
    - "Session context can be rebuilt from an append-only entry tree and current leaf pointer."
    - "Domain defines gateway ports for workspace/resource and command execution without real shell/filesystem implementations."
  artifacts:
    - path: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceGateway.java"
      provides: "Workspace operations Domain port"
      contains: "interface WorkspaceGateway"
    - path: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/CommandExecutionGateway.java"
      provides: "Command execution Domain port"
      contains: "interface CommandExecutionGateway"
    - path: "pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionContextResolver.java"
      provides: "Active leaf context reconstruction contract"
      contains: "resolve"
  key_links:
    - from: "SessionContextResolver.java"
      to: "SessionEntry.java"
      via: "leaf-to-root traversal"
      pattern: "parentEntryId"
    - from: "WorkspaceSession.java"
      to: "WorkspaceScope.java"
      via: "run-scoped workspace constraints"
      pattern: "WorkspaceScope scope"
---

<objective>
Define Workspace, Artifact/Attachment, and append-only Session tree contracts.

Purpose: Workspace and Session shape are foundational for future Cloud persistence, governed tools, Admin history, compaction, branching, resource boundaries, and safe command/file execution.
Output: Spring-free Domain contracts plus tests for WORK-01/02/04/05 and CORE-08.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-RESEARCH.md
@.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-01-SUMMARY.md

<interfaces>
Created by Plan 01:
- Package namespace: `io.github.pi_java.agent`.
- Domain must remain JDK-first and framework-independent.

This plan must not depend on Plan 02 classes except common IDs if they exist. If Plan 02 is not present, create any missing ID records needed locally in a compatible package and avoid file conflicts by not editing files owned by Plan 02.

Required semantics from D-12/D-13/D-16/D-18:
- Session entries have stable IDs, parent entry IDs, timestamps, types, and typed payloads.
- Session has current leaf pointer.
- Context resolver rebuilds active context from root-to-leaf path.
- Workspace snapshot has ID, fingerprint, drift metadata, and replay-safety marker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Workspace resource and snapshot contracts</name>
  <files>pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Workspace.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceSession.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceScope.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceSnapshot.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Resource.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Mount.java, pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/workspace/WorkspaceContractsTest.java</files>
  <read_first>
    - `pi-agent-domain/pom.xml`
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md`
    - `README.md`
  </read_first>
  <behavior>
    - Test 1: `WorkspaceScope` contains tenant/session/run/workspace context and allowed resource/mount IDs.
    - Test 2: `WorkspaceSnapshot` exposes snapshot ID, fingerprint, drift metadata, and replay-safe marker.
  </behavior>
  <action>Write `WorkspaceContractsTest` first. Implement `Workspace(String workspaceId, String tenantId, String displayName, Set<Mount> mounts)`, `WorkspaceSession(String workspaceSessionId, String workspaceId, String runId, WorkspaceScope scope, Instant openedAt)`, `WorkspaceScope(String tenantId, String userId, String sessionId, String runId, String workspaceId, Set<String> allowedResourceIds, Set<String> allowedMountIds)`, `WorkspaceSnapshot(String snapshotId, String workspaceId, String fingerprint, Map<String, String> driftMetadata, boolean replaySafe, Instant createdAt)`, `Resource(String resourceId, String kind, String uri, Map<String, String> metadata)`, and `Mount(String mountId, String resourceId, String logicalPath, boolean readOnly)`. Use only JDK types. Do not use `java.io.File`, `java.nio.file.Path`, shell command types, or host filesystem assumptions per D-16/D-18.</action>
  <verify>
    <automated>mvn -q -pl pi-agent-domain test</automated>
  </verify>
  <acceptance_criteria>
    - `WorkspaceSnapshot.java` contains `fingerprint`, `driftMetadata`, and `replaySafe`.
    - No file in `pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace` contains `java.io.File` or `java.nio.file.Path`.
    - `WorkspaceScope.java` contains `workspaceId` and `runId`.
    - `mvn -q -pl pi-agent-domain test` exits 0.
  </acceptance_criteria>
  <done>Workspace boundary contracts satisfy D-16 and D-18 without host filesystem coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define WorkspaceGateway and CommandExecutionGateway ports</name>
  <files>pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceGateway.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/CommandExecutionGateway.java, pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/workspace/WorkspaceContractsTest.java</files>
  <read_first>
    - `pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/Workspace.java`
    - `pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/workspace/WorkspaceSnapshot.java`
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md`
  </read_first>
  <behavior>
    - Test 1: gateway contracts expose snapshot and restore operations by IDs/scopes.
    - Test 2: command execution request contains workspace session/scope, command arguments, deadline, cancellation token ID, and environment map without executing anything.
  </behavior>
  <action>Extend tests, then implement `WorkspaceGateway` as a Domain port with methods `WorkspaceSession openSession(WorkspaceScope scope)`, `WorkspaceSnapshot createSnapshot(String workspaceSessionId, String reason)`, `void restoreSnapshot(String workspaceSessionId, String snapshotId)`, `Optional<Resource> findResource(String workspaceId, String resourceId)`, and `void closeSession(String workspaceSessionId)`. Implement `CommandExecutionGateway` as a Domain port with nested records `CommandRequest(String workspaceSessionId, List<String> command, Map<String, String> environment, Duration timeout, String cancellationTokenId)` and `CommandResult(int exitCode, String outputSummary, String errorSummary, boolean timedOut, boolean cancelled)`, plus method `CommandResult execute(CommandRequest request)`. Per D-17, this is only a port; do not implement real shell execution or use `ProcessBuilder`.</action>
  <verify>
    <automated>mvn -q -pl pi-agent-domain test</automated>
  </verify>
  <acceptance_criteria>
    - `WorkspaceGateway.java` contains `createSnapshot` and `restoreSnapshot`.
    - `CommandExecutionGateway.java` contains `CommandRequest` and does not contain `ProcessBuilder`.
    - `CommandExecutionGateway.java` contains `cancellationTokenId` and `Duration timeout`.
    - `mvn -q -pl pi-agent-domain test` exits 0.
  </acceptance_criteria>
  <done>Workspace and command execution are domain ports only, satisfying D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define Artifact/Attachment and append-only session tree</name>
  <files>pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/Artifact.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/Attachment.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/artifact/ExternalReference.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/Session.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionEntry.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionEntryPayload.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionContext.java, pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/session/SessionContextResolver.java, pi-agent-domain/src/test/java/io/github/pi_java/agent/domain/session/SessionTreeContextResolverTest.java</files>
  <read_first>
    - `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-CONTEXT.md`
    - `README.md`
  </read_first>
  <behavior>
    - Test 1: a session with root → message → artifact → branch-summary entries resolves the current leaf path in order.
    - Test 2: `SessionContext` separates messages, artifacts, attachments, external references, memory refs, and workspace scope.
  </behavior>
  <action>Write `SessionTreeContextResolverTest` first. Implement `Artifact(String artifactId, String kind, String title, String summary, Map<String, String> metadata)`, `Attachment(String attachmentId, String kind, String name, String summary, Map<String, String> metadata)`, and `ExternalReference(String referenceId, String kind, String uri, String title, Map<String, String> metadata)`. Implement `Session(String sessionId, String tenantId, String userId, String currentLeafEntryId, List<SessionEntry> entries)`. Implement `SessionEntry(String entryId, String parentEntryId, Instant timestamp, SessionEntryPayload payload)`. Implement sealed `SessionEntryPayload` with `MessageEntry`, `ArtifactEntry`, `AttachmentEntry`, `ExternalReferenceEntry`, `MemoryReferenceEntry`, `WorkspaceScopeEntry`, `CompactionSummaryEntry`, and `BranchSummaryEntry`. Implement `SessionContext` with separate lists for messages, artifacts, attachments, externalReferences, memoryRefs, optional workspaceScope, and activeEntryPath. Implement `SessionContextResolver.resolve(Session session)` that follows `currentLeafEntryId` parent links to root, reverses to root-to-leaf order, and accumulates context. Do not copy pi JSONL persistence or cwd/session path concepts per D-15/D-28/D-30.</action>
  <verify>
    <automated>mvn -q -pl pi-agent-domain test</automated>
  </verify>
  <acceptance_criteria>
    - `SessionEntry.java` contains `parentEntryId`.
    - `Session.java` contains `currentLeafEntryId`.
    - `SessionEntryPayload.java` contains `CompactionSummaryEntry` and `BranchSummaryEntry`.
    - `SessionContext.java` contains separate fields for `artifacts`, `attachments`, `externalReferences`, and `memoryRefs`.
    - `mvn -q -pl pi-agent-domain test` exits 0.
  </acceptance_criteria>
  <done>Session tree and context resolver satisfy D-09 and D-12 through D-15.</done>
</task>

</tasks>

<verification>
Run `mvn test`. Confirm no Domain workspace contract imports host filesystem or process execution classes.
</verification>

<success_criteria>
- Workspace contracts exist and are host-filesystem-free.
- Gateway ports exist without real shell/file implementations.
- Session context reconstruction works from append-only tree entries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-spine-workspace-and-domain-contracts/01-03-SUMMARY.md`.
</output>
