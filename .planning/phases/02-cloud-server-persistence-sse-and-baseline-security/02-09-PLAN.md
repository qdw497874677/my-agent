---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 09
type: execute
wave: 6
depends_on: [02-04]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/PiCloudServerApplication.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/PiPrincipal.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/SecurityConfig.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/DevAuthenticationFilter.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java
  - pi-agent-adapter-web/src/main/resources/application.yml
  - pi-agent-adapter-web/src/main/resources/application-test.yml
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SecurityAndCorrelationIntegrationTest.java
autonomous: true
requirements: [CLOUD-01, CLOUD-05]
must_haves:
  truths:
    - "Spring Boot web adapter starts with authenticated `/api/**`, public health/info, dev/test principal support, JWT-ready production shape, and request correlation."
    - "No Spring composition-root beans for runtime/persistence/queue/SSE are created in this plan."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/SecurityConfig.java"
      provides: "Dev auth + JWT-ready security"
      contains: "oauth2ResourceServer"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java"
      provides: "Trace/correlation/MDC baseline"
      contains: "X-Correlation-ID"
  key_links:
    - from: "PiPrincipal"
      to: "App RequestContext"
      via: "toRequestContext(traceId, correlationId, causationId)"
      pattern: "toRequestContext"
---

<objective>
Build the Spring Boot shell, security baseline, and correlation filter only.

Purpose: split the previously oversized web-adapter plan and keep runtime Spring bean composition in one later plan. This plan satisfies D-09/D-10/D-11 for the web boundary but deliberately does not wire persistence, queue, dispatcher, EventSink, or SSE fanout beans.
Output: runnable Spring Boot application class, security/correlation infrastructure, application configuration, and integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
@docs/phase-01-domain-contracts.md
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/context/RequestContext.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Spring Boot shell, security baseline, correlation filter, and health config</name>
  <read_first>
    - pi-agent-adapter-web/pom.xml
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/context/RequestContext.java
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
  </read_first>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/PiCloudServerApplication.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/PiPrincipal.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/SecurityConfig.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/DevAuthenticationFilter.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java, pi-agent-adapter-web/src/main/resources/application.yml, pi-agent-adapter-web/src/main/resources/application-test.yml, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/SecurityAndCorrelationIntegrationTest.java</files>
  <action>
    Create the runnable web adapter and security/correlation baseline only.
    - `PiCloudServerApplication` annotated with `@SpringBootApplication(scanBasePackages = "io.github.pi_java.agent")`. Add `@EnableScheduling` here or in the later composition plan, but do not create runtime bean definitions in this plan.
    - `PiPrincipal(String tenantId, String userId, Collection<String> authorities)` with `RequestContext toRequestContext(String traceId, String correlationId, String causationId)`.
    - `SecurityConfig.apiSecurity(HttpSecurity http)` with `csrf.disable`, permit `/actuator/health` and `/actuator/info`, authenticate `/api/**`, deny all other paths, and configure `.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()))` for non-dev profiles. In `dev`/`test`, register `DevAuthenticationFilter` before `UsernamePasswordAuthenticationFilter` and still require auth for `/api/**`.
    - `DevAuthenticationFilter` reads `X-Pi-Dev-Tenant` and `X-Pi-Dev-User`; defaults to `dev-tenant`/`dev-user` only under `test`; creates `PiPrincipal` with `ROLE_DEV_USER`; never echoes raw auth headers or JWT claims.
    - `CorrelationFilter` reads `X-Correlation-ID` or `X-Request-ID`, generates UUIDs when absent, sets request attributes `pi.traceId`, `pi.correlationId`, and `pi.causationId`, writes response header `X-Correlation-ID`, and populates MDC keys `traceId`, `correlationId`, `tenantId`, and `userId` where available.
    - `application.yml` sets `spring.threads.virtual.enabled: true` and `management.endpoints.web.exposure.include: health,info,metrics` with no real secrets.
    - `application-test.yml` enables test/dev auth behavior and does not disable `/api/**` authentication.
    - Do not create `CloudRuntimeBeanConfiguration`, `InfrastructureConfig`, or any `@Bean` methods for `RunQueue`, `CancellationRegistry`, `RunDispatcher`, `RunWorkerScheduler`, `EventSink`, or `RunEventFanout` in this plan. That single composition-root strategy is owned by Plan 02-12.
    Add `SecurityAndCorrelationIntegrationTest` methods `actuatorHealthIsPublic`, `apiRequiresAuthentication`, `devHeadersCreateTenantUserContext`, and `responseContainsCorrelationHeader`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=SecurityAndCorrelationIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `SecurityConfig.java` contains `/actuator/health`, `/actuator/info`, `/api/**`, and `oauth2ResourceServer`.
    - `DevAuthenticationFilter.java` contains `X-Pi-Dev-Tenant`, `X-Pi-Dev-User`, `dev-tenant`, and `dev-user`.
    - `CorrelationFilter.java` contains `X-Correlation-ID`, `X-Request-ID`, `pi.traceId`, `pi.correlationId`, and `MDC.put`.
    - No file created by this plan contains `@Bean` methods for `RunQueue`, `CancellationRegistry`, `RunDispatcher`, `RunWorkerScheduler`, `EventSink`, or `RunEventFanout`.
    - Tests contain all four required method names.
  </acceptance_criteria>
  <done>Web adapter has a runnable Spring Boot shell with secure API and correlation context baseline, without overlapping runtime bean wiring.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=SecurityAndCorrelationIntegrationTest`.
</verification>

<success_criteria>
- `/api/**` is authenticated while health/info remain public.
- Request correlation IDs flow into headers/MDC/request attributes.
- Runtime dependency-chain composition remains unimplemented here and is owned by Plan 02-12 only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-09-SUMMARY.md`.
</output>
