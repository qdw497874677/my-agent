---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultSessionCommandService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultSessionQueryService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunQueryService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunTerminalEventPublisher.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultSessionUseCaseTest.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandServiceTest.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultRunQueryServiceTest.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/AppUseCaseImplementationArchTest.java
autonomous: true
requirements: [CLOUD-01, CLOUD-03, CLOUD-04, CLOUD-05, CLOUD-06, E2E-04]
must_haves:
  truths:
    - "Concrete App use-case implementations orchestrate repositories, audit, queue, cancellation registry, and query delegation."
    - "App implementations remain Spring/JDBC/Servlet-free."
    - "Cancel is idempotent for queued, running, and terminal runs and avoids duplicate terminal events."
    - "Queued cancellation publishes exactly one durable `run.cancelled` event when no terminal event exists."
  artifacts:
    - path: "pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandService.java"
      provides: "Create/cancel run orchestration"
      contains: "cancelRun"
    - path: "pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandServiceTest.java"
      provides: "Run command orchestration tests"
      contains: "cancelQueuedRunUpdatesDurableStateAuditAndQueue"
    - path: "pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunTerminalEventPublisher.java"
      provides: "Idempotent terminal RunEvent publisher"
      contains: "hasTerminalEvent"
  key_links:
    - from: "DefaultRunCommandService.createRun"
      to: "RunProjectionRepository + RunQueue + AuditRepository"
      via: "durable create, audit, enqueue"
      pattern: "createRun.*record.*enqueue"
    - from: "DefaultRunCommandService.cancelRun"
      to: "RunProjectionRepository + RunQueue + CancellationRegistry + AuditRepository + RunTerminalEventPublisher"
      via: "durable cancel, queued cancel returning QueuedRun, active runtime signal, audit, terminal event if absent"
      pattern: "requestCancellation.*cancelQueuedAndReturn.*publishCancelledIfAbsent.*record"
---

<objective>
Implement concrete framework-free App use cases for sessions, runs, queries, and cancellation.

Purpose: address checker blocker 1 by making App more than interfaces while keeping COLA boundaries intact.
Output: `DefaultSessionCommandService`, `DefaultSessionQueryService`, `DefaultRunCommandService`, `DefaultRunQueryService`, and unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@docs/phase-01-domain-contracts.md
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/RunCommandService.java
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/RunQueue.java
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/CancellationRegistry.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement session and query use-case services</name>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/SessionCommandService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/SessionQueryService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/RunQueryService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/SessionRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunProjectionRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunEventStore.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/AuditRepository.java
  </read_first>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultSessionCommandService.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultSessionQueryService.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunQueryService.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultSessionUseCaseTest.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultRunQueryServiceTest.java</files>
  <action>
    Implement plain Java classes with constructor injection; do not use Spring annotations.
    - `DefaultSessionCommandService(SessionRepository sessionRepository, AuditRepository auditRepository, Supplier<String> sessionIdSupplier, Clock clock)` implements `SessionCommandService.createSession`: create a session ID, call `sessionRepository.create(context, request, sessionId, clock.instant())`, then `auditRepository.record(context, "session.create", "session", sessionId, sessionId, null, Map.of("workspaceId", request.workspaceId()))`.
    - `DefaultSessionQueryService(SessionRepository sessionRepository)` implements `getSession` by `findById` and throws `NoSuchElementException("session not found: " + sessionId)` if missing; `getSessionHistory` delegates to `sessionRepository.history`.
    - `DefaultRunQueryService(RunProjectionRepository runProjectionRepository, RunEventStore runEventStore)` implements all `RunQueryService` methods by delegation. `listEvents` must call `runEventStore.listByRun(runId, afterSequence, limit)`, map Domain `RunEvent` to `RunEventDto` through an App-local private mapper or injected `Function<RunEvent, RunEventDto>`, compute `nextAfterSequence`, and set `terminal` from `runEventStore.hasTerminalEvent(runId)`.
    Add `DefaultSessionUseCaseTest` methods `createSessionPersistsAndAudits` and `getSessionDelegatesToRepository`.
    Add `DefaultRunQueryServiceTest` methods `listEventsDelegatesToEventStoreAndComputesCursor` and `queryMethodsDelegateToProjectionRepository`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-app -am -Dtest='DefaultSessionUseCaseTest,DefaultRunQueryServiceTest'</automated>
  </verify>
  <acceptance_criteria>
    - `DefaultSessionCommandService.java` contains `sessionRepository.create` and `auditRepository.record` with action `session.create`.
    - `DefaultRunQueryService.java` contains `runEventStore.listByRun` and `runEventStore.hasTerminalEvent`.
    - Tests contain `createSessionPersistsAndAudits`, `getSessionDelegatesToRepository`, `listEventsDelegatesToEventStoreAndComputesCursor`, and `queryMethodsDelegateToProjectionRepository`.
  </acceptance_criteria>
  <done>Session commands/queries and run queries have concrete App implementations and unit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run create and idempotent cancellation orchestration</name>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/RunCommandService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunProjectionRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunEventStore.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/AuditRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/RunQueue.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/CancellationRegistry.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/execution/QueuedRun.java
  </read_first>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandService.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunTerminalEventPublisher.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandServiceTest.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/AppUseCaseImplementationArchTest.java</files>
  <action>
    Implement `DefaultRunTerminalEventPublisher` and `DefaultRunCommandService` as plain Java classes with constructor injection; do not use Spring annotations.
    - `DefaultRunTerminalEventPublisher(RunEventStore runEventStore, EventSink eventSink, Clock clock, Supplier<String> eventIdSupplier)` implements `RunTerminalEventPublisher`. Each `publish*IfAbsent` method first calls `runEventStore.hasTerminalEvent(run.runId())`; if true, return false without publishing. If false, build a Domain `RunEvent` using the queued run's tenant/user/session/run/workspace/trace/correlation IDs, a sequence greater than the current last event (`findLastByRun(...).sequence()+1`, or `1` if empty), the existing Phase 1 terminal wire type (`run.completed`, `run.cancelled`, `run.failed`; for timeout use allowed terminal wire name `run.failed` with payload/status reason `TIMED_OUT` unless Phase 1 defines a dedicated timeout terminal), redaction/visibility defaults, and publish through `EventSink.publish(event)`. Return true after successful publish.
    - `DefaultRunCommandService` constructor dependencies: `RunProjectionRepository runProjectionRepository`, `RunQueue runQueue`, `CancellationRegistry cancellationRegistry`, `RunTerminalEventPublisher runTerminalEventPublisher`, `AuditRepository auditRepository`, `Supplier<String> runIdSupplier`, and `Clock clock`.
    - `createRun(RequestContext context, String sessionId, CreateRunRequest request)`: generate `runId`, call `runProjectionRepository.createRun(context, sessionId, runId, request)`, enqueue `new QueuedRun(runId, sessionId, context.tenantId(), context.userId(), request.workspaceId(), context.traceId(), context.correlationId(), request.inputType(), request.input(), clock.instant(), 0)`, record audit action `run.create`, and return the created/found `RunResponse` from `runProjectionRepository.findRun(...).orElseThrow()`.
    - `cancelRun(RequestContext context, String sessionId, String runId, CancelRunRequest request)`: read current status via `runProjectionRepository.getStatus`; if `terminal == true`, record audit action `run.cancel.noop_terminal` and return current status without calling `markTerminalIfNotTerminal` or appending terminal events.
    - For non-terminal runs, call `runProjectionRepository.requestCancellation(runId, reason, now)` and record audit action `run.cancel.requested` exactly once per API call.
    - Then call exact App port method `Optional<QueuedRun> cancelledQueuedRun = runQueue.cancelQueuedAndReturn(runId, reason, now)`. If present, call `runProjectionRepository.markTerminalIfNotTerminal(runId, "CANCELLED", Map.of(), Map.of("reason", reason), now)`, call `runQueue.markTerminal(runId, "CANCELLED", now)` only if needed by the queue implementation, then call `runTerminalEventPublisher.publishCancelledIfAbsent(cancelledQueuedRun.get(), reason, now)` so queued cancellation publishes exactly one durable `run.cancelled` event using real queued context. Return latest status. This is the queued cancellation path.
    - If `cancelQueuedAndReturn` returns empty, call `cancellationRegistry.requestCancellation(runId, reason)` to signal an active runtime token and return latest status. This is the running cancellation path.
    - Do not append duplicate terminal events; all App-created terminal events must go through `RunTerminalEventPublisher` and its `RunEventStore.hasTerminalEvent(runId)` guard.
    Add `DefaultRunCommandServiceTest` methods `createRunPersistsAuditsAndEnqueues`, `cancelQueuedRunUpdatesDurableStateAuditQueueAndPublishesCancelledEvent`, `cancelRunningRunSignalsCancellationRegistry`, `cancelTerminalRunIsIdempotentAndDoesNotDuplicateTerminalState`, and `terminalEventPublisherSkipsWhenTerminalEventAlreadyExists`.
    Add `AppUseCaseImplementationArchTest` that fails if `Default*Service` imports `org.springframework`, `jakarta.servlet`, `javax.servlet`, `JdbcTemplate`, `SseEmitter`, or `DataSource`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-app -am -Dtest='DefaultRunCommandServiceTest,AppUseCaseImplementationArchTest,AppDependencyArchTest'</automated>
  </verify>
  <acceptance_criteria>
    - `DefaultRunCommandService.java` contains `runProjectionRepository.createRun`, `runQueue.enqueue`, `auditRepository.record`, `runQueue.cancelQueuedAndReturn`, `cancellationRegistry.requestCancellation`, `runTerminalEventPublisher.publishCancelledIfAbsent`, and `markTerminalIfNotTerminal`.
    - `DefaultRunCommandService.java` does not contain `runQueue.cancelQueued(`.
    - `DefaultRunTerminalEventPublisher.java` contains `runEventStore.hasTerminalEvent`, `eventSink.publish`, `run.cancelled`, `run.failed`, and `run.completed`.
    - `DefaultRunCommandService.java` contains audit actions `run.create`, `run.cancel.requested`, and `run.cancel.noop_terminal`.
    - `DefaultRunCommandServiceTest.java` contains all five required test method names including `cancelQueuedRunUpdatesDurableStateAuditQueueAndPublishesCancelledEvent`.
    - No `Default*Service.java` under `pi-agent-app/src/main/java` contains Spring/JDBC/Servlet/SSE imports.
  </acceptance_criteria>
  <done>Concrete App run command service orchestrates durable create/cancel, queue, active cancellation, audit, and idempotent terminal handling.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-app -am -Dtest='DefaultSessionUseCaseTest,DefaultRunCommandServiceTest,DefaultRunQueryServiceTest,AppUseCaseImplementationArchTest,AppDependencyArchTest'`.
</verification>

<success_criteria>
- Default App services exist and are tested.
- App remains Spring/JDBC/Servlet-free.
- Create run wires persistence, audit, and queue; cancellation wires durable state, queue, active runtime token, audit, terminal event publishing for queued cancellation, and duplicate-terminal guards.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-06-SUMMARY.md`.
</output>
