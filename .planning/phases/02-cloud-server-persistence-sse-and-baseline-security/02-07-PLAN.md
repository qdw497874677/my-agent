---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 07
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - pi-agent-infrastructure/src/main/resources/db/migration/V1__create_cloud_runtime_tables.sql
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunEventStore.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunProjectionRepository.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcSessionRepository.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcAuditRepository.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/PersistingEventSink.java
  - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/RunEventFanout.java
  - pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcPersistenceIntegrationTest.java
autonomous: true
requirements: [CLOUD-02, CLOUD-03, CLOUD-04, CLOUD-06, E2E-05]
user_setup:
  - service: docker
    why: "Testcontainers PostgreSQL integration tests for Flyway/JDBC persistence semantics"
    dashboard_config:
      - task: "Run integration verification on a Docker-enabled host or CI runner"
        location: "local Docker daemon or CI container runtime"
must_haves:
  truths:
    - "PostgreSQL schema stores sessions, runs, events, projections, audit, and queue rows durably."
    - "Persist-then-emit writes run events/projections before live fanout."
    - "Terminal persistence is idempotent and can detect an existing terminal event."
  artifacts:
    - path: "pi-agent-infrastructure/src/main/resources/db/migration/V1__create_cloud_runtime_tables.sql"
      provides: "Flyway PostgreSQL runtime schema"
      contains: "CREATE TABLE run_events"
    - path: "pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/PersistingEventSink.java"
      provides: "Persist-then-emit EventSink"
      contains: "fanout.publish"
  key_links:
    - from: "PersistingEventSink"
      to: "RunEventFanout"
      via: "fanout after transaction persistence"
      pattern: "eventStore.append.*fanout.publish"
---

<objective>
Implement Flyway/JDBC persistence, projections, audit, event replay, and persist-then-emit event sink.

Purpose: satisfy CLOUD-06 and the durable source-of-truth needed by REST, SSE, worker, and cancellation.
Output: PostgreSQL migration, JDBC port implementations, event fanout seam, and Testcontainers integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
@docs/phase-01-domain-contracts.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Flyway schema and JDBC repositories</name>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunEventStore.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunProjectionRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/SessionRepository.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/AuditRepository.java
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/RunEvent.java
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
  </read_first>
  <files>pi-agent-infrastructure/src/main/resources/db/migration/V1__create_cloud_runtime_tables.sql, pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunEventStore.java, pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunProjectionRepository.java, pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcSessionRepository.java, pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcAuditRepository.java, pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcPersistenceIntegrationTest.java</files>
  <action>
    Create `V1__create_cloud_runtime_tables.sql` and JDBC implementations using Spring `JdbcTemplate` in Infrastructure only.
    - Migration tables and exact columns: `sessions(tenant_id,user_id,session_id PK,workspace_id,current_entry_id,status,created_at,updated_at,metadata jsonb)`, `session_entries(entry_id PK,session_id,parent_entry_id,sequence,payload_type,payload jsonb,created_at)`, `runs(run_id PK,session_id,tenant_id,user_id,workspace_id,status,input_type,input jsonb,terminal_result jsonb,failure jsonb,trace_id,correlation_id,created_at,updated_at,started_at,finished_at,cancel_requested_at,cancel_reason,checkpoint_ref,resume_token,last_event_sequence bigint default 0,runtime_snapshot_ref)`, `run_events(event_id PK,run_id,session_id,tenant_id,user_id,workspace_id,step_id,sequence,event_type,timestamp,trace_id,correlation_id,causation_id,visibility,redaction jsonb,payload_schema,payload_version int,payload jsonb)`, `steps`, `messages`, `tool_calls`, `audit_records`, and `run_queue` with columns from `02-RESEARCH.md` lines 462-477.
    - Add indexes/constraints `unique_run_event_sequence` on `(run_id, sequence)`, `idx_run_events_run_sequence`, `idx_runs_session_created`, `idx_runs_tenant_user_status`, `idx_audit_run`, `idx_audit_correlation`, and `idx_run_queue_status_available`.
    - `JdbcRunEventStore` implements `append`, `listByRun`, `findLastByRun`, and `hasTerminalEvent`. `listByRun` must query `WHERE run_id = ? AND sequence > ? ORDER BY sequence ASC LIMIT ?`; `hasTerminalEvent` must check `event_type in ('run.completed','run.failed','run.cancelled','run.policy_blocked')`.
    - `JdbcRunProjectionRepository` implements `markRunning`, `requestCancellation`, and `markTerminalIfNotTerminal` with `WHERE status NOT IN ('COMPLETED','FAILED','CANCELLED','TIMED_OUT','POLICY_BLOCKED')` so terminal state is idempotent.
    - `JdbcSessionRepository` and `JdbcAuditRepository` implement their App ports; audit inserts `tenant_id`, `user_id`, `action`, `resource_type`, `resource_id`, `run_id`, `session_id`, `trace_id`, `correlation_id`, `timestamp`, and `details` JSONB.
    Add `JdbcPersistenceIntegrationTest` using `PostgreSQLContainer<>("postgres:17-alpine")` with methods `flywayCreatesCloudRuntimeTables`, `runEventsEnforcePerRunSequenceUniqueness`, `appendAndReplayRunEventsOrderedBySequence`, `markTerminalIfNotTerminalIsIdempotent`, and `auditRecordsPersistCorrelationContext`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-infrastructure -am -Dtest=JdbcPersistenceIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - Migration contains `CREATE TABLE sessions`, `CREATE TABLE runs`, `CREATE TABLE run_events`, `CREATE TABLE steps`, `CREATE TABLE messages`, `CREATE TABLE tool_calls`, `CREATE TABLE audit_records`, and `CREATE TABLE run_queue`.
    - Migration contains `UNIQUE (run_id, sequence)` or `unique_run_event_sequence`, `checkpoint_ref`, `resume_token`, `last_event_sequence`, and `runtime_snapshot_ref`.
    - `JdbcRunEventStore.java` contains `ORDER BY sequence ASC`, `payload_schema`, `payload_version`, and does not contain `@class`.
    - `JdbcRunProjectionRepository.java` contains `markTerminalIfNotTerminal` and `status NOT IN`.
    - `JdbcPersistenceIntegrationTest.java` contains all five required test method names and `PostgreSQLContainer<>("postgres:17-alpine")`.
  </acceptance_criteria>
  <done>PostgreSQL schema and JDBC repositories persist/replay/query/audit run state with idempotent terminal guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement persist-then-emit EventSink</name>
  <read_first>
    - pi-agent-domain/src/main/java/io/github/pi_java/agent/domain/event/EventSink.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunEventStore.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/persistence/RunProjectionRepository.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunEventStore.java
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
  </read_first>
  <files>pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/RunEventFanout.java, pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/PersistingEventSink.java, pi-agent-infrastructure/src/test/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcPersistenceIntegrationTest.java</files>
  <action>
    Create the event pipeline seam.
    - `RunEventFanout` interface method: `void publish(RunEvent event)`.
    - `PersistingEventSink` implements Domain `EventSink`; constructor dependencies are `TransactionTemplate tx`, `RunEventStore eventStore`, `RunProjectionRepository runProjectionRepository`, and `RunEventFanout fanout`.
    - `publish(RunEvent event)` must call `tx.executeWithoutResult(...)`, inside it call `eventStore.append(event)` and projection methods that update `last_event_sequence`/terminal status as applicable, then call `fanout.publish(event)` only after `tx.executeWithoutResult` returns successfully. This is the required DB-before-SSE chain: `AgentRuntime/RunTerminalEventPublisher -> EventSink -> RunEventStore DB transaction -> SseRunEventFanout`.
    - If append/projection throws, do not call `fanout.publish`.
    Extend `JdbcPersistenceIntegrationTest` with methods `persistingEventSinkDoesNotFanoutWhenAppendFails` and `persistingEventSinkPersistsBeforeFanout`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-infrastructure -am -Dtest=JdbcPersistenceIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `PersistingEventSink.java` contains `tx.executeWithoutResult`, `eventStore.append(event)`, and `fanout.publish(event)`.
    - In `PersistingEventSink.java`, `fanout.publish(event)` appears after the `tx.executeWithoutResult` block.
    - `JdbcPersistenceIntegrationTest.java` contains `persistingEventSinkDoesNotFanoutWhenAppendFails`.
    - `JdbcPersistenceIntegrationTest.java` contains `persistingEventSinkPersistsBeforeFanout` and asserts a fanout subscriber can read the same event row from `run_events` during/after fanout observation.
  </acceptance_criteria>
  <done>Runtime events persist and project before live fanout, preserving replay correctness.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-infrastructure -am -Dtest=JdbcPersistenceIntegrationTest` on a Docker-enabled host. If Docker is unavailable locally, record the container gate caveat and keep non-container module tests green.
</verification>

<success_criteria>
- Flyway creates all durable Phase 2 tables and indexes.
- JDBC event store supports ordered replay and terminal detection.
- PersistingEventSink enforces persist-then-emit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-07-SUMMARY.md`.
</output>
