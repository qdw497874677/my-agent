---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - pi-agent-client/pom.xml
  - pi-agent-app/pom.xml
  - pi-agent-infrastructure/pom.xml
  - pi-agent-adapter-web/pom.xml
  - pi-testkit/pom.xml
autonomous: true
requirements: [CLOUD-01, CLOUD-02, CLOUD-03, CLOUD-04, CLOUD-05, CLOUD-06, E2E-01, E2E-04, E2E-05]
must_haves:
  truths:
    - "Maven modules have Spring Boot, JDBC, Flyway, Security, and Testcontainers foundations without contaminating Domain."
    - "Local non-container checks remain runnable with Java 21 while Docker/Testcontainers gates are explicit."
  artifacts:
    - path: "pom.xml"
      provides: "Boot/Testcontainers dependency management"
      contains: "spring-boot-dependencies"
    - path: "pi-agent-infrastructure/pom.xml"
      provides: "JDBC/Flyway/PostgreSQL infrastructure dependencies"
      contains: "spring-boot-starter-jdbc"
  key_links:
    - from: "root pom.xml"
      to: "Phase 2 module tests"
      via: "managed dependency versions"
      pattern: "spring-boot.version|testcontainers.version"
---

<objective>
Create the Phase 2 dependency and verification foundation.

Purpose: enable Spring MVC/SSE, Spring Security, JDBC/Flyway/PostgreSQL, and Testcontainers in outer modules while preserving Java-first COLA boundaries.
Output: Maven dependency-management and module dependencies for later plans.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
@docs/phase-01-domain-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 Maven dependency foundation</name>
  <read_first>
    - pom.xml
    - pi-agent-client/pom.xml
    - pi-agent-app/pom.xml
    - pi-agent-infrastructure/pom.xml
    - pi-agent-adapter-web/pom.xml
    - pi-testkit/pom.xml
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
  </read_first>
  <files>pom.xml, pi-agent-client/pom.xml, pi-agent-app/pom.xml, pi-agent-infrastructure/pom.xml, pi-agent-adapter-web/pom.xml, pi-testkit/pom.xml</files>
  <action>
    Update Maven only; do not add production Java code in this task.
    - In root `pom.xml`, add property `spring-boot.version` = `3.5.9` and `testcontainers.version` = `2.0.3`, then import `org.springframework.boot:spring-boot-dependencies:${spring-boot.version}` in `dependencyManagement`.
    - In `pi-agent-client/pom.xml`, add test-scope/use-scope test dependencies needed for `CloudApiDtoContractTest`: `org.junit.jupiter:junit-jupiter` and `org.assertj:assertj-core` only.
    - In `pi-agent-infrastructure/pom.xml`, add dependencies on `pi-agent-app`, `spring-boot-starter-jdbc`, `org.postgresql:postgresql`, `org.flywaydb:flyway-core`, and `org.flywaydb:flyway-database-postgresql`; add `org.testcontainers:postgresql` and `org.testcontainers:junit-jupiter` in test scope.
    - In `pi-agent-adapter-web/pom.xml`, add dependencies on `pi-agent-infrastructure`, `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-boot-starter-actuator`, `spring-security-oauth2-resource-server`, `spring-security-oauth2-jose`, `spring-boot-starter-test` test scope, `spring-security-test` test scope, `org.testcontainers:postgresql` test scope, and `org.testcontainers:junit-jupiter` test scope.
    - In `pi-agent-app/pom.xml`, ensure tests have JUnit/AssertJ/ArchUnit but do not add Spring/JDBC/Servlet dependencies.
    - In `pi-testkit/pom.xml`, do not add Spring production dependencies; keep fake runtime testkit plain Java unless a downstream test directly needs test-scope dependencies.
    - Per D-04/D-12, do not add Spring, Jackson, Jakarta, JDBC, PostgreSQL, Flyway, Vaadin, PF4J, or MCP dependencies to `pi-agent-domain/pom.xml`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-client,pi-agent-app -am</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` contains `spring-boot.version`, `3.5.9`, `testcontainers.version`, `2.0.3`, and `spring-boot-dependencies`.
    - `pi-agent-infrastructure/pom.xml` contains `spring-boot-starter-jdbc`, `postgresql`, `flyway-core`, and `flyway-database-postgresql`.
    - `pi-agent-adapter-web/pom.xml` contains `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-boot-starter-actuator`, `spring-security-oauth2-resource-server`, and `spring-security-oauth2-jose`.
    - `pi-agent-domain/pom.xml` still contains none of `spring-boot`, `jackson`, `jakarta`, `jdbc`, `postgresql`, `flyway`, `vaadin`, `pf4j`, or `mcp`.
  </acceptance_criteria>
  <done>Maven foundations compile under Java 21 and preserve Domain/App framework isolation.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-client,pi-agent-app -am`. Docker/Testcontainers checks are intentionally deferred to plans that create PostgreSQL integration tests; if Docker is unavailable, record that those gates must run on Docker-enabled CI.
</verification>

<success_criteria>
- Spring Boot, Security, JDBC/Flyway/PostgreSQL, and Testcontainers dependencies are available only in the modules that need them.
- Domain and App remain free of outer framework dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-01-SUMMARY.md`.
</output>
