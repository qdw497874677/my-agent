---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 11
type: execute
wave: 8
depends_on: [02-03, 02-07, 02-10]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunEventStreamController.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseSubscription.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/RunEventStreamService.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunSseIntegrationTest.java
autonomous: true
requirements: [CLOUD-02, CLOUD-03, CLOUD-05, E2E-05]
must_haves:
  truths:
    - "SSE stream replays persisted events before subscribing to live fanout."
    - "Fanout has concrete subscribe/unsubscribe contract and cleans subscribers on timeout, error, completion, or send failure."
    - "SSE reconnect uses per-run sequence from afterSequence or Last-Event-ID."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java"
      provides: "Live SSE subscription fanout"
      contains: "subscribe(String runId, Consumer<RunEventDto> consumer)"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunEventStreamController.java"
      provides: "SSE endpoint"
      contains: "MediaType.TEXT_EVENT_STREAM_VALUE"
  key_links:
    - from: "RunEventStreamService"
      to: "RunQueryService and SseRunEventFanout"
      via: "replay-before-subscribe"
      pattern: "listEvents.*subscribe"
---

<objective>
Implement SSE replay-before-subscribe streaming with explicit subscription lifecycle.

Purpose: satisfy D-06/D-07/D-08/E2E-05 with durable replay and live cleanup semantics. This plan creates `SseRunEventFanout` before Plan 02-12 wires it into the Spring `EventSink -> DB -> SSE` chain.
Output: SSE controller, subscription contract, fanout implementation, stream service, and integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@docs/phase-01-domain-contracts.md
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/RunQueryService.java
@pi-agent-client/src/main/java/io/github/pi_java/agent/client/event/RunEventDto.java
@pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/RunEventFanout.java
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/mapper/RunEventDtoMapper.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit SSE subscription fanout contract</name>
  <read_first>
    - pi-agent-client/src/main/java/io/github/pi_java/agent/client/event/RunEventDto.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/RunEventFanout.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/mapper/RunEventDtoMapper.java
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
  </read_first>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseSubscription.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunSseIntegrationTest.java</files>
  <action>
    Implement exact subscription API before any Spring configuration wires it.
    - `SseSubscription(String runId, String subscriberId, Runnable unsubscribe)` record/class with method `void close()` that invokes `unsubscribe.run()` exactly once.
    - `SseRunEventFanout` implements Infrastructure `RunEventFanout` and exposes exact methods `SseSubscription subscribe(String runId, Consumer<RunEventDto> consumer)` and `void unsubscribe(String runId, String subscriberId)`.
    - Internally use `ConcurrentHashMap<String, ConcurrentHashMap<String, Consumer<RunEventDto>>> subscribersByRunId` and UUID subscriber IDs.
    - `publish(RunEvent event)` maps Domain `RunEvent` to `RunEventDto` with `RunEventDtoMapper` and sends only to subscribers under `event.runId()`. This method is called only by `PersistingEventSink` after the DB transaction commits, preserving `EventSink -> DB -> SseRunEventFanout` ordering. If a consumer throws, call `unsubscribe(runId, subscriberId)` and continue other subscribers.
    - Do not store replay buffers here; persisted `run_events` remains the source of truth.
    Add `RunSseIntegrationTest` methods `fanoutSubscribeReturnsSubscriberId`, `fanoutUnsubscribeRemovesSubscriber`, and `fanoutRemovesSubscriberOnSendFailure`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=RunSseIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `SseRunEventFanout.java` contains `SseSubscription subscribe(String runId, Consumer<RunEventDto> consumer)` and `void unsubscribe(String runId, String subscriberId)`.
    - `SseRunEventFanout.java` implements `RunEventFanout` and has no direct `RunEventStore` or JDBC dependency; persistence ordering is owned by `PersistingEventSink`.
    - `SseRunEventFanout.java` contains `ConcurrentHashMap` and removes subscribers on thrown consumer errors.
    - `RunSseIntegrationTest.java` contains `fanoutSubscribeReturnsSubscriberId`, `fanoutUnsubscribeRemovesSubscriber`, and `fanoutRemovesSubscriberOnSendFailure`.
  </acceptance_criteria>
  <done>SSE fanout has an explicit live subscription contract and robust unsubscribe cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replay-before-subscribe SSE endpoint</name>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/RunQueryService.java
    - pi-agent-client/src/main/java/io/github/pi_java/agent/client/event/EventHistoryResponse.java
    - pi-agent-client/src/main/java/io/github/pi_java/agent/client/event/RunEventDto.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/security/PiPrincipal.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/correlation/CorrelationFilter.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java
  </read_first>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunEventStreamController.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/RunEventStreamService.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunSseIntegrationTest.java</files>
  <action>
    Implement Spring MVC `SseEmitter` stream service and controller.
    - `RunEventStreamService.replayThenSubscribe(RequestContext context, String sessionId, String runId, long afterSequence, String lastEventId)` returns `SseEmitter`.
    - Resolve cursor: if `lastEventId` parses as a positive long, use it; otherwise use `afterSequence`.
    - Create `SseEmitter` with configured timeout (default `300000L` ms). Before subscribing, call `RunQueryService.listEvents(context, sessionId, runId, cursor, 500)` and send replay events in ascending sequence with `SseEmitter.event().id(Long.toString(event.sequence())).name(event.type()).data(event)`.
    - After replay completes, call `SseRunEventFanout.subscribe(runId, consumer)` and wire `emitter.onCompletion`, `emitter.onTimeout`, and `emitter.onError` to `subscription.close()`.
    - Consumer sends live events with the same SSE id/name/data shape; on `IOException` or `IllegalStateException`, close the subscription and complete the emitter with error.
    - If a replayed or live event has terminal type `run.completed`, `run.failed`, `run.cancelled`, or `run.policy_blocked`, complete emitter after send and close subscription.
    - `RunEventStreamController` endpoint: `@GetMapping(path = "/api/sessions/{sessionId}/runs/{runId}/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)` with query param `afterSequence` default `0` and header `Last-Event-ID` optional.
    Add/extend `RunSseIntegrationTest` with methods `streamReplaysPersistedEventsBeforeLiveEvents`, `lastEventIdOverridesAfterSequence`, `sseEventIdIsRunSequence`, and `streamCleansSubscriberOnTimeoutErrorAndCompletion`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=RunSseIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `RunEventStreamController.java` contains `/api/sessions/{sessionId}/runs/{runId}/stream`, `MediaType.TEXT_EVENT_STREAM_VALUE`, `Last-Event-ID`, and `afterSequence`.
    - `RunEventStreamService.java` contains `replayThenSubscribe`, `listEvents(context, sessionId, runId`, `subscribe(runId`, `.id(Long.toString(event.sequence()))`, `.name(event.type())`, `onCompletion`, `onTimeout`, and `onError`.
    - `RunSseIntegrationTest.java` contains all four stream test method names.
  </acceptance_criteria>
  <done>SSE endpoint performs durable replay before live subscription and cleans resources on timeout/error/completion.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=RunSseIntegrationTest`.
</verification>

<success_criteria>
- SSE live subscription contract is explicit and exists before Spring composition wires it.
- Replay-before-subscribe prevents missing persisted events.
- Subscriber cleanup is tested for timeout, error, completion, and send failure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-11-SUMMARY.md`.
</output>
