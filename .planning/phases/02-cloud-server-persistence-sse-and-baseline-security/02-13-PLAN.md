---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 13
type: execute
wave: 10
depends_on: [02-11, 02-12]
files_modified:
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerHeadlessE2ETest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunCancellationIntegrationTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/TestCloudRuntimeConfiguration.java
  - pi-agent-adapter-web/src/test/resources/application-e2e.yml
  - docs/phase-02-cloud-server-api.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLOUD-01, CLOUD-02, CLOUD-03, CLOUD-04, CLOUD-05, CLOUD-06, E2E-01, E2E-04, E2E-05]
user_setup:
  - service: docker
    why: "Final headless E2E requires Testcontainers PostgreSQL"
    dashboard_config:
      - task: "Run final Phase 2 integration gate on Docker-enabled CI/local runner"
        location: "local Docker daemon or CI container runtime"
must_haves:
  truths:
    - "Headless E2E creates a run over REST, streams SSE, queries persisted history/status/result, and reaches terminal state without model keys."
    - "Headless E2E verifies automatic worker activation, queued/running/terminal cancellation, timeout/max-step terminal behavior, no hanging tasks, persisted terminal event rows, and exactly one terminal event."
    - "Phase 2 API contract and requirement status are documented for downstream clients."
  artifacts:
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerHeadlessE2ETest.java"
      provides: "No-key Cloud Server E2E"
      contains: "successfulRunCreateStreamPersistQuery"
    - path: "docs/phase-02-cloud-server-api.md"
      provides: "Downstream API contract index"
      contains: "/api/sessions/{sessionId}/runs/{runId}/stream"
  key_links:
    - from: "CloudServerHeadlessE2ETest"
      to: "TestCloudRuntimeConfiguration fake AgentRuntime"
      via: "GeneralAgentLoop test bean"
      pattern: "GeneralAgentLoop"
---

<objective>
Close Phase 2 with no-key E2E, cancellation/replay hardening, API docs, and requirement status.

Purpose: prove the Cloud Server product slice end-to-end using fake runtime/testkit and durable PostgreSQL state, without adding deferred providers/tools/UI/MCP/plugins/brokers/crash replay.
Output: E2E tests, cancellation tests, e2e wiring, API docs, and requirement checklist updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
@docs/phase-01-domain-contracts.md
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/EventCollector.java
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeModelClient.java
@pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolInvoker.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add no-key Cloud Server E2E and cancellation integration tests</name>
  <read_first>
    - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/GeneralAgentLoop.java
    - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/EventCollector.java
    - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeModelClient.java
    - pi-testkit/src/main/java/io/github/pi_java/agent/testkit/FakeToolInvoker.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunController.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunEventStreamController.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/RunEventStreamService.java
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
  </read_first>
  <files>pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudServerHeadlessE2ETest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/RunCancellationIntegrationTest.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/TestCloudRuntimeConfiguration.java, pi-agent-adapter-web/src/test/resources/application-e2e.yml</files>
  <action>
    Build Docker/Testcontainers-backed E2E tests in `pi-agent-adapter-web`.
    - `TestCloudRuntimeConfiguration` provides test beans using `pi-testkit` `GeneralAgentLoop`, `FakeModelClient`, `FakeToolInvoker`, `FakePolicy`, `DeterministicIds`, and `DeterministicClock`; test sources only.
    - `application-e2e.yml` activates test/dev auth, Flyway, virtual threads, and PostgreSQL datasource from Testcontainers dynamic properties; no real provider keys or secrets.
    - `CloudServerHeadlessE2ETest` uses `@SpringBootTest(webEnvironment = RANDOM_PORT)`, `@Testcontainers`, and `PostgreSQLContainer<>("postgres:17-alpine")` with tests `successfulRunCreateStreamPersistQueryUsesAutomaticWorkerActivation`, `cancelAndTimeoutPathsTerminateWithPersistedTerminalEvents`, `maxStepRunEmitsSingleTerminalEventAndDoesNotHang`, and `sseReconnectReplaysWithoutGaps`.
    - `successfulRunCreateStreamPersistQueryUsesAutomaticWorkerActivation` must POST `/api/sessions`, POST `/api/sessions/{sessionId}/runs`, then rely on `RunWorkerScheduler.triggerAsync()`/scheduled polling from the product path; it MUST NOT call `DefaultRunDispatcher.dispatch(...)` or any internal dispatcher directly. It then connects to `/api/sessions/{sessionId}/runs/{runId}/stream`, GETs `/events`, `/status`, `/result`, and asserts terminal state without real model keys.
    - `sseReconnectReplaysWithoutGaps` must connect once, capture an SSE `id` sequence, reconnect with header `Last-Event-ID: <sequence>`, and assert every replayed event has `sequence > <sequence>`.
    - `maxStepRunEmitsSingleTerminalEventAndDoesNotHang` must configure the fake/testkit runtime or create-run metadata/default `RuntimeLimits` so the run deterministically exceeds a max-step limit through the public REST/product path. It must POST `/api/sessions`, POST `/api/sessions/{sessionId}/runs`, rely on `RunWorkerScheduler.triggerAsync()`/scheduled polling, assert terminal status through `GET /api/sessions/{sessionId}/runs/{runId}/status`, assert exactly one terminal event row exists and is last in both `run_events` and `GET /api/sessions/{sessionId}/runs/{runId}/events`, and assert no hanging worker/model/tool task remains. It MUST NOT call `DefaultRunDispatcher.dispatch(...)` or any internal dispatcher directly.
    - `RunCancellationIntegrationTest` adds `cancelRunningRunEmitsCancelledTerminalEventRow`, `cancelQueuedRunPublishesExactlyOneCancelledEventRow`, `terminalRunCancelReturnsCurrentStateWithoutDuplicateTerminalEvent`, and `timeoutRunEmitsSingleAllowedTerminalEventRow`.
    - Assert exact terminal event wire names `run.completed`, `run.failed`, `run.cancelled`, or `run.policy_blocked`; for timeout assert run status `TIMED_OUT`, an allowed terminal event row exists (`run.failed` is acceptable when paired with timeout failure payload/status), and no hanging model/tool tasks. Exactly one terminal event row must appear last in `run_events` and via `/events`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest='CloudServerHeadlessE2ETest,RunCancellationIntegrationTest'</automated>
  </verify>
  <acceptance_criteria>
    - `CloudServerHeadlessE2ETest.java` contains `PostgreSQLContainer<>("postgres:17-alpine")`, `successfulRunCreateStreamPersistQueryUsesAutomaticWorkerActivation`, `cancelAndTimeoutPathsTerminateWithPersistedTerminalEvents`, `maxStepRunEmitsSingleTerminalEventAndDoesNotHang`, and `sseReconnectReplaysWithoutGaps`.
    - `CloudServerHeadlessE2ETest.java` contains `RuntimeLimits`, `max-step`, `exactly one terminal event`, and `no hanging` or equivalent assertions inside `maxStepRunEmitsSingleTerminalEventAndDoesNotHang`.
    - `CloudServerHeadlessE2ETest.java` contains `/api/sessions`, `/runs`, `/stream`, `/events`, `/status`, `/result`, and `Last-Event-ID`.
    - `CloudServerHeadlessE2ETest.java` does not contain `DefaultRunDispatcher.dispatch(` or `.dispatch("e2e-worker")`; product path activation must come from `RunWorkerScheduler` polling/triggering.
    - `TestCloudRuntimeConfiguration.java` contains `GeneralAgentLoop`, `FakeModelClient`, `FakeToolInvoker`, and `FakePolicy`.
    - `RunCancellationIntegrationTest.java` contains all four required test method names.
    - Test files contain no `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `apiKey`, or `secretKey` strings except negative assertions if needed.
  </acceptance_criteria>
  <done>Headless E2E proves create/stream/persist/query/cancel/timeout/replay paths with fake runtime and PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 2 API contract and mark requirement status</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
    - docs/phase-01-domain-contracts.md
    - pi-agent-client/src/main/java/io/github/pi_java/agent/client/event/RunEventDto.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunController.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunEventStreamController.java
  </read_first>
  <files>docs/phase-02-cloud-server-api.md, .planning/REQUIREMENTS.md</files>
  <action>
    Create `docs/phase-02-cloud-server-api.md` as the downstream contract index for Web Console/Admin/TUI/CLI clients.
    Include exact endpoint list: `POST /api/sessions`, `GET /api/sessions/{sessionId}`, `GET /api/sessions/{sessionId}/history`, `POST /api/sessions/{sessionId}/runs`, `GET /api/sessions/{sessionId}/runs/{runId}`, `GET /api/sessions/{sessionId}/runs/{runId}/status`, `POST /api/sessions/{sessionId}/runs/{runId}/cancel`, `GET /api/sessions/{sessionId}/runs/{runId}/events?afterSequence=0&limit=500`, `GET /api/sessions/{sessionId}/runs/{runId}/steps`, `GET /api/sessions/{sessionId}/runs/{runId}/messages`, `GET /api/sessions/{sessionId}/runs/{runId}/tool-calls`, `GET /api/sessions/{sessionId}/runs/{runId}/result`, and `GET /api/sessions/{sessionId}/runs/{runId}/stream`.
    Document event DTO fields exactly: `eventId`, `tenantId`, `userId`, `sessionId`, `runId`, `stepId`, `workspaceId`, `sequence`, `timestamp`, `type`, `traceId`, `correlationId`, `causationId`, `visibility`, `redaction`, `payloadSchema`, `payloadVersion`, `payload`.
    Document explicit Phase 2 non-goals: no real model provider, no ToolExecutionGateway, no UI, no MCP/PF4J/plugins, no Redis/Kafka/Rabbit, no crash-resumable replay, no unrestricted shell/file tools.
    In `.planning/REQUIREMENTS.md`, mark only Phase 2 requirement checkboxes implemented after tests pass as `[x]`: CLOUD-01 through CLOUD-06, E2E-01, E2E-04, and E2E-05. Do not mark E2E-02, E2E-03, E2E-06, E2E-07, or E2E-08.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn test</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-02-cloud-server-api.md` contains `/api/sessions/{sessionId}/runs/{runId}/stream` and every event DTO field listed above.
    - `docs/phase-02-cloud-server-api.md` contains exact phrases `no real model provider`, `no ToolExecutionGateway`, `no MCP/PF4J/plugins`, `no Redis/Kafka/Rabbit`, and `no crash-resumable replay`.
    - `.planning/REQUIREMENTS.md` contains `[x] **CLOUD-01**`, `[x] **CLOUD-06**`, `[x] **E2E-01**`, `[x] **E2E-04**`, and `[x] **E2E-05**` after implementation passes.
    - `.planning/REQUIREMENTS.md` still contains `[ ] **E2E-02**`, `[ ] **E2E-03**`, `[ ] **E2E-06**`, `[ ] **E2E-07**`, and `[ ] **E2E-08**`.
  </acceptance_criteria>
  <done>Phase 2 public API contract is documented and requirement status reflects implemented automated gates.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn test`. Docker may be unavailable locally; if Testcontainers cannot start, record that PostgreSQL/E2E gates must run in Docker-enabled CI and keep all non-container tests green.
</verification>

<success_criteria>
- E2E verifies authenticated REST create with automatic worker activation, SSE stream/reconnect, persisted history, persisted terminal event rows, terminal status, cancellation, timeout/max-step, and no real model keys.
- API docs name exact endpoints, event DTO fields, and Phase 2 non-goals.
- Requirement status is updated only after passing implementation gates.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-13-SUMMARY.md`.
</output>
