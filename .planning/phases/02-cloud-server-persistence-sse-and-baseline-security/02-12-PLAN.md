---
phase: 02-cloud-server-persistence-sse-and-baseline-security
plan: 12
type: execute
wave: 9
depends_on: [02-06, 02-07, 02-08, 02-10, 02-11]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudRuntimeWiringIntegrationTest.java
autonomous: true
requirements: [CLOUD-01, CLOUD-02, CLOUD-03, CLOUD-04, CLOUD-06, E2E-01, E2E-04, E2E-05]
must_haves:
  truths:
    - "There is exactly one Spring composition root for runtime/persistence/queue/worker/EventSink/SSE fanout beans."
    - "Spring context wires Controller -> App service -> JDBC repositories/queue/audit -> dispatcher -> AgentRuntime -> EventSink -> DB -> SseRunEventFanout."
    - "REST-created runs activate automatically through RunWorkerScheduler.triggerAsync()/scheduled polling, never by controller calling DefaultRunDispatcher."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java"
      provides: "Single Spring composition root"
      contains: "RunWorkerScheduler"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudRuntimeWiringIntegrationTest.java"
      provides: "Bean uniqueness and runtime-chain integration tests"
      contains: "exactlyOneRuntimeBeanOfEachCriticalType"
  key_links:
    - from: "CloudRuntimeBeanConfiguration"
      to: "PersistingEventSink and SseRunEventFanout"
      via: "EventSink bean persists through JDBC then emits to SSE fanout"
      pattern: "PersistingEventSink.*SseRunEventFanout"
    - from: "RunController"
      to: "RunWorkerScheduler"
      via: "RunActivationTrigger bean delegates to scheduler.triggerAsync"
      pattern: "RunActivationTrigger.*triggerAsync"
---

<objective>
Create the single Spring composition root for Cloud Runtime wiring.

Purpose: resolve duplicate/overlapping bean wiring by making this plan the only owner of active Spring `@Bean` definitions for App services, JDBC repositories, DB queue, cancellation registry, dispatcher, scheduler, EventSink, and SSE fanout. Plan 02-08 creates plain implementation classes only; Plan 02-11 creates SSE classes; this plan wires them after they exist.
Output: `CloudRuntimeBeanConfiguration` and wiring integration tests that assert bean uniqueness and product-path worker activation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-CONTEXT.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-RESEARCH.md
@.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-VALIDATION.md
@docs/phase-01-domain-contracts.md
@pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandService.java
@pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/RunWorkerScheduler.java
@pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire all runtime beans in one composition root</name>
  <read_first>
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultSessionCommandService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunCommandService.java
    - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultRunQueryService.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunEventStore.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/jdbc/JdbcRunProjectionRepository.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/queue/PostgresRunQueue.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/DefaultRunDispatcher.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/execution/RunWorkerScheduler.java
    - pi-agent-infrastructure/src/main/java/io/github/pi_java/agent/infrastructure/event/PersistingEventSink.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/sse/SseRunEventFanout.java
    - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/RunController.java
  </read_first>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/CloudRuntimeBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/CloudRuntimeWiringIntegrationTest.java</files>
  <action>
    Create `CloudRuntimeBeanConfiguration` as the single Spring composition root for Phase 2 runtime wiring.
    - Add `@Configuration` and, if not already present in `PiCloudServerApplication`, `@EnableScheduling` here.
    - Define `@Bean` methods for exactly these critical types: `SessionCommandService` (`DefaultSessionCommandService`), `SessionQueryService` (`DefaultSessionQueryService`), `RunCommandService` (`DefaultRunCommandService`), `RunQueryService` (`DefaultRunQueryService`), `RunEventStore` (`JdbcRunEventStore`), `RunProjectionRepository` (`JdbcRunProjectionRepository`), `SessionRepository` (`JdbcSessionRepository`), `AuditRepository` (`JdbcAuditRepository`), `RunQueue` (`PostgresRunQueue`), `CancellationRegistry` (`InMemoryCancellationRegistry`), `RunTerminalEventPublisher` (`DefaultRunTerminalEventPublisher`), `RunDispatcher` (`DefaultRunDispatcher`), `RunWorkerScheduler`, `RunEventFanout` (`SseRunEventFanout`), Domain `EventSink` (`PersistingEventSink`), and controller `RunActivationTrigger` delegating to `RunWorkerScheduler.triggerAsync()`.
    - If scheduling annotation is not in the plain `RunWorkerScheduler`, expose a scheduled adapter bean/method in this configuration that calls `runWorkerScheduler.pollOnce()` with fixed delay property `${pi.runtime.worker.poll-delay-ms:250}`. There must still be exactly one `RunWorkerScheduler` bean.
    - Wire `PersistingEventSink(TransactionTemplate, RunEventStore, RunProjectionRepository, RunEventFanout)` so the DB-before-SSE chain is `AgentRuntime/RunTerminalEventPublisher -> EventSink -> RunEventStore DB transaction -> SseRunEventFanout`.
    - Wire `DefaultRunDispatcher` with the `AgentRuntime` bean supplied by production/test configuration, the `RunQueue`, repositories, terminal publisher, cancellation registry, audit repository, clock, timeout property, and mapper/factory dependencies.
    - Do not create any second `InfrastructureConfig`; if an implementation from Plan 02-08 accidentally created one, remove or leave it inactive so this plan owns all active runtime `@Bean`s.
    Add `CloudRuntimeWiringIntegrationTest` methods `exactlyOneRuntimeBeanOfEachCriticalType`, `springContextWiresControllerToAppJdbcDispatcherRuntimeEventSinkAndSseFanout`, `eventSinkBeanPersistsBeforeSseFanout`, and `createRunTriggersWorkerActivationWithoutManualDispatcherCall`.
  </action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=CloudRuntimeWiringIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `CloudRuntimeBeanConfiguration.java` contains `@Bean` methods for `RunQueue`, `CancellationRegistry`, `RunDispatcher`, `RunWorkerScheduler`, `EventSink`, and `RunEventFanout`.
    - `CloudRuntimeBeanConfiguration.java` contains `DefaultRunCommandService`, `DefaultRunQueryService`, `JdbcRunEventStore`, `JdbcRunProjectionRepository`, `PostgresRunQueue`, `DefaultRunDispatcher`, `RunWorkerScheduler`, `AgentRuntime`, `PersistingEventSink`, and `SseRunEventFanout`.
    - `CloudRuntimeBeanConfiguration.java` wires `RunActivationTrigger` to `RunWorkerScheduler.triggerAsync()` and does not wire controllers directly to `DefaultRunDispatcher`.
    - `CloudRuntimeWiringIntegrationTest.java` asserts exactly one bean each for `RunQueue`, `CancellationRegistry`, `RunDispatcher`, `RunWorkerScheduler`, `EventSink`, and `RunEventFanout`.
    - `CloudRuntimeWiringIntegrationTest.java` contains all four required method names and asserts the listed beans are present in the Spring context.
  </acceptance_criteria>
  <done>Single composition root wires the complete product path with no duplicate runtime beans and with automatic worker activation.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q test -pl pi-agent-adapter-web -am -Dtest=CloudRuntimeWiringIntegrationTest`.
</verification>

<success_criteria>
- Exactly one Spring bean exists for RunQueue, CancellationRegistry, RunDispatcher, RunWorkerScheduler, EventSink, and RunEventFanout.
- `SseRunEventFanout` exists before it is wired.
- REST-created runs use the worker scheduler activation path and E2E/tests must not manually call `DefaultRunDispatcher.dispatch`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cloud-server-persistence-sse-and-baseline-security/02-12-SUMMARY.md`.
</output>
