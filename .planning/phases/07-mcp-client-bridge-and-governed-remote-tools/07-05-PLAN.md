---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBinding.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolResultMapper.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpInvocationErrorMapper.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistry.java
  - pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBindingTest.java
autonomous: true
requirements: [MCP-03, MCP-04, MCP-05]
must_haves:
  truths:
    - "When ToolExecutionGateway invokes a resolved MCP tool binding, the remote call uses MCP SDK only inside infrastructure."
    - "Approval, sandbox, deny, timeout, and cancellation gates still happen before remote MCP invocation through the existing gateway."
    - "Remote invocation failures return normalized redacted ToolExecutionResult failures."
  artifacts:
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBinding.java"
      provides: "ToolExecutorBinding for remote MCP tool calls"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolResultMapper.java"
      provides: "MCP CallToolResult to Pi ToolExecutionResult mapping"
    - path: "pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBindingTest.java"
      provides: "Invocation success/failure/redaction/cancellation coverage"
  key_links:
    - from: "McpToolRegistry.resolve"
      to: "McpToolExecutorBinding"
      via: "ToolRegistry.ToolResolution executor"
      pattern: "new ToolResolution"
---

<objective>
Complete MCP remote tool invocation as an ordinary `ToolExecutorBinding` behind the governed gateway.

Purpose: Ensure MCP tool execution cannot bypass validation, policy, approval/sandbox, timeout, audit, redaction, and tool lifecycle events already owned by `ToolExecutionGateway`.
Output: MCP executor binding, result/error mappers, registry wiring, and invocation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-04-SUMMARY.md
@docs/phase-04-governed-tool-contracts.md

<interfaces>
Target low-level executor seam:
```java
@FunctionalInterface
public interface ToolExecutorBinding {
    ToolExecutionResult execute(ToolExecutionRequest request, CancellationToken cancellationToken);
}
```
MCP Java SDK invocation shape:
```java
CallToolResult result = client.callTool(new CallToolRequest("toolName", Map.of("arg", "value")));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MCP ToolExecutorBinding</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBinding.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistry.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBindingTest.java</files>
  <behavior>
    - Binding receives `ToolExecutionRequest` already allowed by the gateway; it does not perform policy or audit itself per D-12 and D-18.
    - Binding checks `CancellationToken` before remote invocation and returns a cancelled/failed normalized result if cancelled per MCP-03.
    - Binding invokes only the configured server/tool from the registry snapshot; it does not accept arbitrary MCP endpoint/tool names from request arguments per D-15.
  </behavior>
  <action>Implement `McpToolExecutorBinding` with server id and MCP tool name captured from discovery, using `McpClientFactory`/handle seam to call `callTool`. Update `McpToolRegistry.resolve()` to return this binding for available tools. Do not emit events or audit here; those remain `DefaultToolExecutionGateway` responsibilities. Tests should use a fake client handle to assert the request args are passed to the expected configured MCP tool name.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolExecutorBindingTest test</automated>
  </verify>
  <done>Resolved MCP tool executions call the configured MCP tool only through the ToolRegistry/ToolExecutorBinding seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map MCP call results to Pi ToolExecutionResult</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolResultMapper.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBindingTest.java</files>
  <behavior>
    - Successful MCP call content maps to `ToolExecutionResult` success with summary-safe structured output per existing Phase 4 payload limiting/redaction flow.
    - MCP `isError`/error content maps to failure status and stable error category, not raw remote error body per D-23 and D-24.
    - Output preserves enough metadata for Admin/tool cards: server id, MCP tool name, content type summary, redaction hint.
  </behavior>
  <action>Create `McpToolResultMapper` to convert MCP `CallToolResult` into Pi `ToolExecutionResult`. Favor maps/lists/strings already accepted by Phase 4 result summarization. If MCP content objects are complex, reduce them to safe maps with type/text/resource-ref summaries; do not expose resources/prompts as separate Phase 7 features. Tests should include normal text, structured JSON-like content, and MCP error content.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolExecutorBindingTest test</automated>
  </verify>
  <done>MCP call responses become source-neutral Pi tool results suitable for existing redaction/audit/event handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize invocation/auth/timeout failures safely</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpInvocationErrorMapper.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/invocation/McpToolExecutorBindingTest.java</files>
  <behavior>
    - Server unavailable/auth failure/timeout/cancellation map to stable failure categories per D-22 and D-23.
    - Raw headers, request bodies, remote error bodies, credential values, and fake secret markers never appear in returned errors per D-19 and D-24.
    - Timeout/cancellation paths do not retry non-idempotent unknown tools automatically per D-17.
  </behavior>
  <action>Implement `McpInvocationErrorMapper` and use it in binding exception handling. Return normalized `ToolExecutionResult` failure/cancelled results with redacted `errorCategory`, `serverId`, `toolName`, and `actionHint` metadata. Tests must include a fake exception containing `PI_PHASE7_FAKE_SECRET_DO_NOT_LEAK` and assert the result and exception string outputs are clean.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolExecutorBindingTest test</automated>
  </verify>
  <done>MCP invocation failures are safe, categorized, and compatible with existing gateway lifecycle/audit output.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -am -Dtest=McpToolExecutorBindingTest test`.
</verification>

<success_criteria>
MCP tool calls execute only as low-level bindings resolved by `ToolRegistry`; existing `ToolExecutionGateway` remains the only runtime governance path, and all MCP invocation results/errors are normalized and redacted.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-05-SUMMARY.md`.
</output>
