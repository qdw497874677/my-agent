---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 07
type: execute
wave: 6
depends_on: [07-06]
files_modified:
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/admin/AdminGovernanceView.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/admin/AdminRegistryView.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpAdminGovernanceViewTest.java
  - e2e/phase-07-mcp-governance.spec.ts
autonomous: true
requirements: [MCP-04]
must_haves:
  truths:
    - "Admin can see MCP server connection/discovery/auth/health status in the Web Console/Admin Governance UI."
    - "Admin can trigger refresh discovery from UI without add/edit/delete controls."
    - "Unhealthy MCP servers and unavailable tools are visible with redacted, actionable status."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/admin/AdminRegistryView.java"
      provides: "MCP status rendering in Admin Governance registry"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java"
      provides: "UI public API client paths for MCP status/refresh"
    - path: "e2e/phase-07-mcp-governance.spec.ts"
      provides: "Browser-level MCP governance smoke assertions"
  key_links:
    - from: "AdminRegistryView"
      to: "/api/admin/governance/mcp"
      via: "ConsoleHttpClient public REST helper"
      pattern: "governance/mcp"
---

<objective>
Replace the Phase 5 MCP placeholder in Admin Governance UI with real read-only MCP status and refresh affordance.

Purpose: Complete the user-visible Phase 7 governance surface while preserving Java-first Vaadin UI and public REST boundary.
Output: Vaadin status rendering, UI client helpers, focused view test, and browser smoke spec.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-06-SUMMARY.md
@docs/phase-05-web-console.md

<interfaces>
Existing UI rule: Vaadin views use `ConsoleHttpClient`/public DTOs only, not App/Domain/repositories.

New DTOs from Plan 02 to consume:
```java
public record McpGovernanceResponse(List&lt;McpServerDto&gt; servers, Instant generatedAt) {}
public record McpServerDto(String serverId, String name, String transport, String status,
    String discoveryStatus, int toolCount, Map&lt;String,String&gt; metadata, List&lt;McpToolDto&gt; tools) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConsoleHttpClient MCP API helpers</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/ui/ConsoleHttpClient.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpAdminGovernanceViewTest.java</files>
  <behavior>
    - Client exposes GET MCP status path and refresh action path backed by public Admin REST per D-03.
    - Client method return/plan types use `pi-agent-client` DTOs only per Phase 5 GUI-08.
    - Helpers do not expose config mutation endpoints or forms per deferred Admin CRUD boundary.
  </behavior>
  <action>Extend `ConsoleHttpClient` with MCP governance URL/method helpers consistent with existing governance overview/audit helpers. If current client is URL-plan-only, add action plan records for `GET /api/admin/governance/mcp` and `POST /api/admin/governance/mcp/refresh`. Do not add add/edit/delete/disable methods.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=McpAdminGovernanceViewTest test</automated>
  </verify>
  <done>Vaadin code has public API helpers for MCP status and refresh only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render MCP server and tool status in Admin Governance</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/admin/AdminGovernanceView.java, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/admin/AdminRegistryView.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpAdminGovernanceViewTest.java</files>
  <behavior>
    - UI shows server id/name, transport, status, discovery status, auth summary, tool count, last refresh, and redacted error per D-03 and MCP-04.
    - UI shows discovered tools with `mcp.&lt;server&gt;.&lt;tool&gt;` id, availability, risk/read-only/destructive/open-world hints, and schema dialect per D-11, D-13, D-14, D-17.
    - UI renders refresh affordance/action plan but no configuration mutation controls per D-02 and deferred ideas.
  </behavior>
  <action>Replace the MCP placeholder section with real DTO-driven components. Keep it inspect-only: cards/grid/list labels and one refresh button/action plan are allowed; no text fields/forms for server config. Preserve existing extension/plugin sections. Tests should instantiate view/component fixtures with healthy and failed MCP DTOs, assert visible labels/status text, assert refresh action exists, and assert add/edit/delete labels are absent.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=McpAdminGovernanceViewTest test</automated>
  </verify>
  <done>Admin Governance UI displays real MCP health/discovery/tool status and read-only refresh behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 7 MCP browser governance smoke spec</name>
  <files>e2e/phase-07-mcp-governance.spec.ts</files>
  <behavior>
    - Browser spec uses the existing no-key Playwright harness and fake runtime fixtures per D-26.
    - Spec verifies Admin Governance shows MCP server status, a discovered tool id, an unhealthy/redacted error case, and refresh affordance per D-27.
    - Spec asserts no add/edit/delete MCP server controls are visible per deferred Admin CRUD boundary.
  </behavior>
  <action>Add a focused Playwright spec alongside Phase 5 E2E that targets the existing Spring Boot e2e profile. If the fake fixture does not yet provide MCP data, extend only test fixture data in the later E2E closeout plan; here create the spec with selectors/text that current UI tests establish. Keep assertions behavioral and public-UI based, not pixel snapshots.</action>
  <verify>
    <automated>npm run e2e -- --list</automated>
  </verify>
  <done>Playwright discovers the Phase 7 MCP governance smoke spec and it is ready for full E2E fixture completion in Plan 08.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=McpAdminGovernanceViewTest test` and `npm run e2e -- --list`.
</verification>

<success_criteria>
Admin users can inspect MCP status and trigger refresh through the Web Console/Admin Governance UI using public APIs only, with no config CRUD controls or secret leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-07-SUMMARY.md`.
</output>
