---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpGovernanceCatalog.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpServerStatus.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpToolStatus.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/GovernanceQueryService.java
  - pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultGovernanceQueryService.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpGovernanceResponse.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpServerDto.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpToolDto.java
  - pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpRefreshResponse.java
  - pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/GovernanceQueryServiceMcpTest.java
autonomous: true
requirements: [MCP-01, MCP-04]
must_haves:
  truths:
    - "Admin can inspect configured MCP servers, health, discovery status, tool count, and redacted errors through public read models."
    - "Admin can request a refresh through an App seam without gaining create/edit/delete MCP server mutation."
    - "App and client contracts contain no MCP SDK, Spring, Vaadin, or provider-specific types."
  artifacts:
    - path: "pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpGovernanceCatalog.java"
      provides: "App port for MCP status and refresh"
    - path: "pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpGovernanceResponse.java"
      provides: "Public Admin MCP DTO boundary"
    - path: "pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/GovernanceQueryServiceMcpTest.java"
      provides: "Framework-free App mapping coverage"
  key_links:
    - from: "DefaultGovernanceQueryService"
      to: "McpGovernanceCatalog"
      via: "constructor-injected App port"
      pattern: "McpGovernanceCatalog"
---

<objective>
Define the framework-free MCP governance App port and public Admin DTOs.

Purpose: Replace Phase 5 MCP placeholder status with real read-only-plus-refresh contracts while preserving public DTO and COLA boundaries.
Output: App `mcp` port records, client DTO records, `GovernanceQueryService` MCP methods, and no-SDK mapping tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@docs/phase-05-web-console.md
@docs/phase-06-extension-surface.md

<interfaces>
Existing governance service shape:
```java
public interface GovernanceQueryService {
    GovernanceOverviewResponse overview(RequestContext context);
    ExtensionGovernanceResponse extensions(RequestContext context);
    List<PolicyDecisionSummaryDto> policyDecisions(RequestContext context);
    List<AuditSummaryDto> audits(RequestContext context);
}
```

Existing overview DTO includes an MCP status slot:
```java
public record GovernanceOverviewResponse(..., GovernanceStatusDto mcp, GovernanceStatusDto plugins, ...) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add App MCP governance catalog contracts</name>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpGovernanceCatalog.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpServerStatus.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/port/mcp/McpToolStatus.java</files>
  <behavior>
    - App port exposes `servers()` and `refresh()` or equivalent per D-03 and D-20.
    - Server status includes server id/name/enabled/transport/auth summary/connection status/discovery status/tool count/last refreshed/redacted error per D-03, D-21, D-24.
    - Tool status includes server-qualified tool id, MCP tool name, availability, read-only/destructive/open-world metadata, schema summary, and redacted error per D-11, D-13, D-14, D-22.
    - Contracts use only JDK/App/client-safe types; no `io.modelcontextprotocol.*`, Spring, Vaadin, or HTTP client types per D-28.
  </behavior>
  <action>Create App port records under `io.github.pi_java.agent.app.port.mcp`. Use string statuses (`HEALTHY`, `UNAVAILABLE`, `DISCOVERY_FAILED`, `AUTH_FAILED`, `UNCONFIGURED`) and `Map&lt;String,String&gt; metadata` to mirror Phase 6 governance language without depending on extension SDK. Include an `EmptyMcpGovernanceCatalog` nested class or separate class only if needed by constructor defaults; it should return no servers and refresh status `UNCONFIGURED`.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-app -Dtest=GovernanceQueryServiceMcpTest test</automated>
  </verify>
  <done>App has a read-only-plus-refresh MCP governance seam with no MCP SDK leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public MCP governance DTO records</name>
  <files>pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpGovernanceResponse.java, pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpServerDto.java, pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpToolDto.java, pi-agent-client/src/main/java/io/github/pi_java/agent/client/admin/McpRefreshResponse.java</files>
  <behavior>
    - DTOs are plain Java records with strings, booleans, counts, `Instant`, lists, and `Map&lt;String,String&gt;` only.
    - DTOs expose configured server status, transport/auth metadata, discovery/health, redacted errors, tool status, and refresh result per D-03, D-07, D-19, D-21.
    - DTO names do not imply Admin create/edit/delete support per D-02 and deferred Admin CRUD boundary.
  </behavior>
  <action>Create public DTO records in `pi-agent-client`. Validate required IDs/statuses are nonblank and copy lists/maps defensively like existing `GovernanceStatusDto`. Do not import Domain/App/Spring/MCP SDK types. Include a refresh DTO that reports `started/refreshed`, server counts, and redacted errors; do not include configuration mutation fields.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-client -am test</automated>
  </verify>
  <done>Admin clients can consume MCP governance and refresh responses without private runtime or SDK types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend GovernanceQueryService mapping for MCP</name>
  <files>pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/GovernanceQueryService.java, pi-agent-app/src/main/java/io/github/pi_java/agent/app/usecase/DefaultGovernanceQueryService.java, pi-agent-app/src/test/java/io/github/pi_java/agent/app/usecase/GovernanceQueryServiceMcpTest.java</files>
  <behavior>
    - `overview()` MCP status is derived from `McpGovernanceCatalog` instead of hardcoded `UNCONFIGURED` when a catalog is present per D-03.
    - Service exposes `mcp(context)` and `refreshMcp(context)` or equivalent methods per D-03 and D-20.
    - Mapping preserves unhealthy configured servers and redacted errors per D-21, D-24.
  </behavior>
  <action>Add MCP methods to `GovernanceQueryService`, update `DefaultGovernanceQueryService` constructor to accept `McpGovernanceCatalog` with an empty fallback in adapter wiring later, and map App port statuses to client DTOs. Keep policy/audit methods unchanged. Tests should use a fake catalog with one healthy server and one failed server and assert overview counts/status, MCP response tool metadata, refresh response, and absence of raw fake secret strings.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-app -Dtest=GovernanceQueryServiceMcpTest test</automated>
  </verify>
  <done>Governance use case has MCP read/refresh methods and overview status reflects real MCP catalog state.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-client,pi-agent-app -am test`.
</verification>

<success_criteria>
Admin MCP governance can be represented and refreshed through App/client contracts, with no Admin CRUD, no private SDK leakage, and visible failed/unhealthy server status implementing MCP-01/MCP-04 and D-03, D-11, D-20 through D-24, and D-28.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-02-SUMMARY.md`.
</output>
