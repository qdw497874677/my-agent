---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 06
type: execute
wave: 5
depends_on: [07-02, 07-04, 07-05]
files_modified:
  - pi-agent-adapter-web/pom.xml
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/McpGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java
  - pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpGovernanceApiTest.java
  - pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpToolRegistryWiringTest.java
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04]
must_haves:
  truths:
    - "Cloud Server composes configured MCP registry into the same ToolRegistry used by ToolExecutionGateway."
    - "Admin REST can read MCP status and trigger refresh without server config mutation."
    - "MCP discovered tools appear in `/api/tools`/registry paths with MCP provenance."
  artifacts:
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/McpGovernanceBeanConfiguration.java"
      provides: "Spring composition root for MCP config, discovery, registry, and governance"
    - path: "pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java"
      provides: "MCP governance GET and refresh endpoint"
    - path: "pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpToolRegistryWiringTest.java"
      provides: "Product wiring proof for MCP registry through ToolRegistry"
  key_links:
    - from: "ToolGovernanceBeanConfiguration"
      to: "McpToolRegistry"
      via: "CompositeToolRegistry includes MCP registry after built-ins/extensions"
      pattern: "McpToolRegistry"
---

<objective>
Wire MCP registry and governance into the Cloud Server composition root and Admin REST API.

Purpose: Make Phase 7 MCP tools product-visible and gateway-routable without breaking the public API/read-only governance boundary.
Output: Adapter Web dependency/configuration, composite tool registry integration, Admin MCP endpoints, and wiring tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-02-SUMMARY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-04-SUMMARY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-05-SUMMARY.md
@docs/phase-04-governed-tool-contracts.md
@docs/phase-05-web-console.md

<interfaces>
Existing composition root to extend:
```java
@Bean
@ConditionalOnMissingBean
ToolRegistry toolRegistry(BuiltinToolCatalog builtinToolCatalog,
                          Optional&lt;DefaultExtensionContributionRegistry&gt; extensionContributions) {
    ToolRegistry builtins = builtinToolCatalog.registry();
    return extensionContributions
        .&lt;ToolRegistry&gt;map(contributions -> new CompositeToolRegistry(List.of(builtins, new ExtensionToolRegistry(contributions))))
        .orElse(builtins);
}
```
Existing controller prefix:
```java
@RequestMapping("/api/admin/governance")
public class AdminGovernanceController { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Adapter Web MCP bean configuration</name>
  <files>pi-agent-adapter-web/pom.xml, pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/McpGovernanceBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpToolRegistryWiringTest.java</files>
  <behavior>
    - Adapter Web depends on `pi-agent-infrastructure-mcp`; no Domain/App/client MCP SDK leakage per D-28.
    - Typed properties bind under `pi.mcp.servers` and feed `McpServerRegistry` per D-01.
    - Discovery can run at startup when enabled and can be disabled in tests; manual refresh remains available per D-20.
  </behavior>
  <action>Add the infrastructure MCP module dependency to `pi-agent-adapter-web`. Create `McpGovernanceBeanConfiguration` with `@ConfigurationProperties` binding for MCP server config, beans for `McpServerRegistry`, `McpToolRegistry`, and `McpGovernanceCatalog`. Provide safe empty catalog/registry when no MCP servers are configured. Keep raw secret resolution injected through the infrastructure seam from Plan 03. Tests should start a minimal Spring context with fake MCP discovery and assert beans exist without network/model keys.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=McpToolRegistryWiringTest test</automated>
  </verify>
  <done>Cloud Server has MCP registry/governance beans with safe empty fallback and no external service requirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose MCP tools into the governed ToolRegistry</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/config/ToolGovernanceBeanConfiguration.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpToolRegistryWiringTest.java</files>
  <behavior>
    - Composite registry order is built-ins first, extensions second, MCP third unless existing duplicate policy requires otherwise; no silent override of built-ins per Phase 6 pattern.
    - MCP tools resolve through the same `ToolRegistry` bean consumed by `DefaultToolExecutionGateway` per D-12 and MCP-03.
    - `/api/tools` sees MCP descriptors with `SourceKind.MCP` after fake discovery per MCP-02.
  </behavior>
  <action>Update `ToolGovernanceBeanConfiguration.toolRegistry(...)` to accept `Optional&lt;McpToolRegistry&gt;` and include it in `CompositeToolRegistry`. Preserve current extension registry behavior. If duplicate IDs occur, first registry wins and MCP duplicates are governance-visible but not overriding. Tests should assert a fake `mcp.fake.echo` descriptor is listed/resolvable through the final `ToolRegistry` and retains MCP provenance.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=McpToolRegistryWiringTest test</automated>
  </verify>
  <done>MCP tools are product-composed into the exact registry path used by ToolExecutionGateway and public tool catalog APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Admin MCP governance REST endpoints</name>
  <files>pi-agent-adapter-web/src/main/java/io/github/pi_java/agent/adapter/web/controller/AdminGovernanceController.java, pi-agent-adapter-web/src/test/java/io/github/pi_java/agent/adapter/web/McpGovernanceApiTest.java</files>
  <behavior>
    - Authenticated `GET /api/admin/governance/mcp` returns configured server/tool status per D-03 and MCP-04.
    - Authenticated refresh endpoint (use `POST /api/admin/governance/mcp/refresh` unless existing controller style suggests otherwise) triggers rediscovery only, not config mutation per D-03.
    - Endpoint responses use `pi-agent-client` DTOs and redacted errors only per D-19 and D-24.
  </behavior>
  <action>Extend `AdminGovernanceController` to delegate to `GovernanceQueryService.mcp(...)` and `refreshMcp(...)`. Keep controller thin: build `RequestContext` and return DTO. Tests should use dev/test auth headers and fake catalog to assert overview MCP no longer placeholder, GET status works, refresh works, and fake secret text is absent from JSON.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -Dtest=McpGovernanceApiTest test</automated>
  </verify>
  <done>Admin REST exposes real MCP read-only status plus refresh action without server configuration CRUD.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-adapter-web -am -Dtest=McpToolRegistryWiringTest,McpGovernanceApiTest test`.
</verification>

<success_criteria>
Cloud Server wiring composes MCP tools into the governed registry, surfaces MCP status/refresh through public Admin REST, and preserves read-only configuration boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-06-SUMMARY.md`.
</output>
