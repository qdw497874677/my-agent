---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactory.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientHandle.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpTransportFactory.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpSecretHeaderResolver.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientErrorSanitizer.java
  - pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactoryTest.java
autonomous: true
requirements: [MCP-01, MCP-05]
must_haves:
  truths:
    - "Configured MCP servers can produce clients for streamable HTTP, stdio, and legacy SSE."
    - "Credential refs are resolved only inside MCP infrastructure."
    - "Connection/auth/transport errors are normalized and sanitized before leaving infrastructure."
  artifacts:
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactory.java"
      provides: "Infrastructure MCP client creation seam"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpTransportFactory.java"
      provides: "Transport-specific factory for HTTP/SSE/stdio"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientErrorSanitizer.java"
      provides: "Sanitized error boundary"
  key_links:
    - from: "McpClientFactory"
      to: "McpServerProperties"
      via: "validated config creates transport/client"
      pattern: "McpSafetyValidator"
---

<objective>
Implement the MCP client/transport factory seam with static credential injection and sanitized error handling.

Purpose: Turn Phase 7 configuration into safe infrastructure-only MCP clients while keeping stdio constrained and secrets inside the adapter boundary.
Output: Client factory, transport factory, secret/header resolver, sanitizer, and focused tests using fakes/seams rather than real MCP services.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-DISCOVERY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-01-SUMMARY.md

<interfaces>
MCP Java SDK discovery findings:
```java
McpSyncClient client = McpClient.sync(transport)
    .requestTimeout(Duration.ofSeconds(10))
    .capabilities(ClientCapabilities.builder().build())
    .build();
client.initialize();
client.closeGracefully();
```
Transport examples:
```java
HttpClientStreamableHttpTransport.builder("http://server").endpoint("/mcp").build();
new HttpClientSseClientTransport("http://server");
new StdioClientTransport(ServerParameters.builder("command").args(...).build());
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create client and transport factory seams</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactory.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientHandle.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpTransportFactory.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactoryTest.java</files>
  <behavior>
    - Streamable HTTP uses configured base URL and endpoint per D-05.
    - SSE uses explicit legacy transport only when configured per D-05.
    - Stdio uses only configured command/args/env refs per D-06.
    - Client capabilities are Tools-only/minimal; resources/prompts/sampling/elicitation are not enabled per D-09.
  </behavior>
  <action>Implement a `McpClientFactory` that validates `McpServerProperties`, builds a `McpTransport` via `McpTransportFactory`, creates a sync client with request timeout, initializes it, and returns a closeable `McpClientHandle`. Keep MCP SDK types package-private where possible. Add test seams so unit tests can assert selected transport kind without launching real processes or connecting to network.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpClientFactoryTest test</automated>
  </verify>
  <done>Infrastructure can create initialized MCP client handles for all configured transports without exposing MCP SDK types above infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve static credential refs inside MCP infrastructure</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpSecretHeaderResolver.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactoryTest.java</files>
  <behavior>
    - Bearer/API-key/custom-header values are resolved from ref-based config only inside this infrastructure package per D-07.
    - Raw values are applied to HTTP client/transport setup or stdio env only at the final boundary.
    - Test fake secret marker never appears in `toString()`, thrown exception messages, redacted summaries, or public return values per D-19 and D-24.
  </behavior>
  <action>Create `McpSecretHeaderResolver` that accepts a minimal secret lookup function/adapter compatible with existing `SecretResolver` concepts. It should return transport header/env maps for HTTP/SSE/stdio but expose only redacted metadata outward. If existing Phase 3 `SecretResolver` is App-level and not directly injectable here, use an adapter interface in infrastructure-mcp and wire it later in Adapter Web. Do not add raw secret fields to `McpServerProperties`.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpClientFactoryTest test</automated>
  </verify>
  <done>MCP auth supports static refs for headers/tokens/env without secret leakage outside the infrastructure call boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize and sanitize MCP connection errors</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientErrorSanitizer.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/client/McpClientFactoryTest.java</files>
  <behavior>
    - Auth failures, timeout, server unavailable, discovery failure, and invalid config map to stable categories per D-23.
    - Sanitized messages include server id, category, and user-action hint but exclude raw remote bodies, headers, tokens, env values, and credentials per D-24.
    - Factory wraps initialization failures in sanitized infrastructure exceptions/status objects.
  </behavior>
  <action>Implement `McpClientErrorSanitizer` and use it in `McpClientFactory`. Keep categories small: `CONFIG_INVALID`, `AUTH_FAILED`, `SERVER_UNAVAILABLE`, `TIMEOUT`, `TRANSPORT_ERROR`, `UNKNOWN`. Tests should construct fake exceptions containing `PI_PHASE7_FAKE_SECRET_DO_NOT_LEAK` and assert sanitized outputs do not contain it.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpClientFactoryTest test</automated>
  </verify>
  <done>Connection initialization failures become redacted, categorized infrastructure errors suitable for governance status.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -am -Dtest=McpClientFactoryTest test`.
</verification>

<success_criteria>
MCP clients can be constructed for all Phase 7 transports from validated configuration, static credentials are resolved only inside infrastructure, and errors are sanitized before governance/registry consumers see them.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-03-SUMMARY.md`.
</output>
