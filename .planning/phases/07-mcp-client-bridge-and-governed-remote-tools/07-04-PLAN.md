---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpServerRegistry.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistry.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolDescriptorMapper.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpGovernanceCatalogAdapter.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpDiscoveryResult.java
  - pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistryTest.java
autonomous: true
requirements: [MCP-02, MCP-04, MCP-05]
must_haves:
  truths:
    - "Platform discovers MCP tools and lists them as Pi ToolDescriptors with server-qualified IDs."
    - "Discovery failures leave configured server status visible instead of hiding the server."
    - "MCP input schemas pass through as JSON Schema and metadata preserves read-only/destructive/open-world hints."
  artifacts:
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistry.java"
      provides: "ToolRegistry adapter for discovered MCP tools"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolDescriptorMapper.java"
      provides: "MCP tool to ToolDescriptor normalization"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpGovernanceCatalogAdapter.java"
      provides: "MCP governance status adapter"
  key_links:
    - from: "McpToolRegistry"
      to: "ToolRegistry"
      via: "implements App port and returns ToolResolution"
      pattern: "implements ToolRegistry"
---

<objective>
Implement MCP tool discovery, descriptor normalization, registry state, and governance status adaptation.

Purpose: Make remote MCP tools appear as ordinary governed Pi tools while retaining per-server health/discovery visibility.
Output: MCP server/tool registry, descriptor mapper, governance catalog adapter, and registry tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-DISCOVERY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-02-SUMMARY.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-03-SUMMARY.md
@docs/phase-04-governed-tool-contracts.md
@docs/phase-06-extension-surface.md

<interfaces>
Canonical target contracts:
```java
public interface ToolRegistry {
    List<ToolDescriptor> listTools();
    Optional<ToolResolution> resolve(String toolId);
}
public record ToolDescriptor(String id, String name, String description, ToolSchema inputSchema,
        Optional<ToolSchema> outputSchema, ToolProvenance provenance, String version,
        Set<String> scopes, ToolRiskLevel riskLevel, ToolSideEffect sideEffect,
        Duration defaultTimeout, Map<String, Object> metadata) {}
public record ToolSchema(String dialect, Map<String,Object> document, Set<String> sensitiveFields, int payloadLimitBytes) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MCP discovery registry state</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpServerRegistry.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpDiscoveryResult.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistryTest.java</files>
  <behavior>
    - Startup/manual refresh discovers tools from all enabled configured servers per D-20.
    - Failed discovery records server status, redacted error, and zero/unavailable tools without removing the configured server per D-21.
    - Refresh is deterministic and replace-all per server; stale tools for a failed server become unavailable rather than silently executable per D-22.
  </behavior>
  <action>Create `McpServerRegistry` that holds configured server properties, current discovery snapshots, and refresh results. Use `McpClientFactory` from Plan 03 to initialize/list tools through a seam. Store sanitized categories/messages only. Keep implementation thread-safe enough for manual refresh and registry reads using immutable snapshots or synchronization; do not add periodic polling beyond a simple refresh method.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolRegistryTest test</automated>
  </verify>
  <done>Configured MCP server statuses survive success and failure refreshes with redacted diagnostic state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map MCP tools into canonical ToolDescriptor records</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolDescriptorMapper.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistryTest.java</files>
  <behavior>
    - Tool IDs are `mcp.&lt;serverId&gt;.&lt;toolName&gt;` per D-13.
    - `inputSchema` is passed through as `ToolSchema` with dialect `json-schema` and no custom schema conversion per D-14.
    - Provenance uses `ToolProvenance.SourceKind.MCP`, server id as source id, and binding ref containing server id/tool name per D-10 through D-12.
    - Read-only annotations map to lower risk/read-only; destructive/open-world/unknown tools default to conservative remote/external metadata per D-17.
  </behavior>
  <action>Implement `McpToolDescriptorMapper` from MCP SDK tool metadata or an internal test-friendly snapshot record. Preserve annotations such as `readOnlyHint`, `destructiveHint`, `idempotentHint`, and `openWorldHint` in descriptor metadata. Set required scopes to include `tool:mcp`, `mcp:server:&lt;serverId&gt;`, and `mcp:tool:&lt;serverId&gt;:&lt;toolName&gt;` so Agent allowlists can be explicit per D-16. Default timeout comes from server config/tool metadata.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolRegistryTest test</automated>
  </verify>
  <done>Discovered MCP tools normalize into collision-free, policy-hint-rich Pi descriptors without schema conversion or MCP SDK leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose MCP ToolRegistry and governance catalog adapters</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistry.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpGovernanceCatalogAdapter.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/registry/McpToolRegistryTest.java</files>
  <behavior>
    - `McpToolRegistry.listTools()` returns available descriptors from healthy discoveries.
    - `resolve(id)` returns a `ToolResolution` for available tools and no resolution/unavailable-safe behavior for unhealthy tools per D-22.
    - `McpGovernanceCatalogAdapter` maps server/tool snapshots to App `McpServerStatus` and `McpToolStatus` records with redacted errors per D-11, D-21, D-24.
  </behavior>
  <action>Implement `McpToolRegistry` as a Pi `ToolRegistry` adapter backed by `McpServerRegistry`; the executor binding can be a placeholder seam to be completed in Plan 05 but must not bypass `ToolExecutionGateway`. Implement `McpGovernanceCatalogAdapter` using the App port from Plan 02, including refresh delegation. Tests should prove list/resolve behavior, failed server visible in governance, and no raw fake secret in statuses.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpToolRegistryTest test</automated>
  </verify>
  <done>MCP discovery state feeds both governed tool registry resolution and Admin governance status consistently.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -am -Dtest=McpToolRegistryTest test`.
</verification>

<success_criteria>
MCP discovery produces server-qualified ToolDescriptors with JSON Schema passthrough, conservative policy metadata, persistent failed-server visibility, and an App governance catalog adapter.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-04-SUMMARY.md`.
</output>
