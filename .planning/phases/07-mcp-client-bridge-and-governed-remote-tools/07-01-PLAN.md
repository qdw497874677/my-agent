---
phase: 07-mcp-client-bridge-and-governed-remote-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - pi-agent-infrastructure-mcp/pom.xml
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerProperties.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpTransportKind.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpAuthProperties.java
  - pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpSafetyValidator.java
  - pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerPropertiesTest.java
autonomous: true
requirements: [MCP-01, MCP-05]
must_haves:
  truths:
    - "Operator can define trusted MCP servers from configuration, not Admin CRUD."
    - "Configured transport is explicit: streamable HTTP, stdio, or legacy SSE."
    - "Unsafe URL schemes, unconfigured commands, and secret-bearing public summaries are rejected or redacted."
  artifacts:
    - path: "pi-agent-infrastructure-mcp/pom.xml"
      provides: "Dedicated MCP infrastructure module with isolated MCP dependencies"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerProperties.java"
      provides: "Typed configuration-file-first trusted MCP server model"
    - path: "pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpSafetyValidator.java"
      provides: "Transport, URL, stdio, redirect/header, and credential-reference safety validation"
  key_links:
    - from: "pi-agent-infrastructure-mcp"
      to: "MCP SDK"
      via: "module-scoped dependencies only"
      pattern: "io.modelcontextprotocol|spring-ai.*mcp"
---

<objective>
Create the isolated MCP infrastructure module and configuration-file-first trusted server contract.

Purpose: Establish Phase 7's MCP boundary without leaking MCP SDK or Spring AI MCP types into Domain/App/client/starter modules, while honoring D-01 through D-08 and D-15.
Output: `pi-agent-infrastructure-mcp` Maven module with typed server/auth/transport properties and safety validation tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-CONTEXT.md
@.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-DISCOVERY.md

<interfaces>
MCP SDK/Spring AI MCP dependencies are allowed only inside this new infrastructure module. Existing parent versions include:
```xml
<spring-ai.version>1.1.5</spring-ai.version>
```

Existing secret boundary to reference in config names but not expose raw values:
```java
public record CredentialRef(String value) {}
public record SecretRef(String value) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isolated MCP infrastructure Maven module</name>
  <files>pom.xml, pi-agent-infrastructure-mcp/pom.xml</files>
  <behavior>
    - Parent reactor includes `pi-agent-infrastructure-mcp` after `pi-agent-infrastructure-extension` and before adapter modules.
    - MCP SDK/Spring AI MCP dependencies are declared only in `pi-agent-infrastructure-mcp` per D-28.
    - Domain, App, client, extension API, and Spring starter POMs remain unchanged by MCP dependencies.
  </behavior>
  <action>Add `pi-agent-infrastructure-mcp` to the parent `<modules>` list. Create the module POM with parent coordinates, dependencies on `pi-agent-domain`, `pi-agent-app`, `pi-agent-client` only if needed for redacted DTO helpers, Spring Boot configuration processor/test support as test or optional only, Resilience4j if needed, and MCP Java SDK/Spring AI MCP client artifacts isolated here. Do not add MCP SDK dependencies to root dependencyManagement unless the Spring AI BOM already manages them; if a version is required, add a module-local property/comment explaining the pin. Use D-10: this module owns independent MCP registry/adapter behavior, not Java `ExtensionSource` behavior.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -am test</automated>
  </verify>
  <done>New module compiles in reactor and MCP dependency scope is isolated to the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define typed MCP server, transport, and auth configuration</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerProperties.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpTransportKind.java, pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpAuthProperties.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerPropertiesTest.java</files>
  <behavior>
    - `McpServerProperties` supports server id, enabled flag, display name, transport kind, base URL/endpoint for HTTP/SSE, command/args/env refs for stdio, timeout, credential refs, static bearer/API-key/custom-header refs, and allowlist/network metadata per D-01, D-05, D-07.
    - Defaults recommend `STREAMABLE_HTTP`; stdio and SSE require explicit transport selection per D-05 and D-06.
    - Public/redacted summary methods expose only refs and metadata, never raw secret/header values per D-07 and D-19.
  </behavior>
  <action>Create plain Java records/classes in the config package. Use string ref fields such as `credentialRef`, `bearerTokenRef`, and `customHeaderSecretRefs` rather than raw secret values. Include a helper that returns redacted metadata for governance (`auth=credential-ref`, `headers=count`, `transport=STREAMABLE_HTTP`) without secrets. Keep OAuth/DCR fields out of this phase except optional future extension comments; do not implement OAuth per D-08.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpServerPropertiesTest test</automated>
  </verify>
  <done>Config contract can represent Streamable HTTP, stdio, and SSE trusted servers with static credential refs and redacted summaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MCP configuration safety validation</name>
  <files>pi-agent-infrastructure-mcp/src/main/java/io/github/pi_java/agent/infrastructure/mcp/config/McpSafetyValidator.java, pi-agent-infrastructure-mcp/src/test/java/io/github/pi_java/agent/infrastructure/mcp/config/McpServerPropertiesTest.java</files>
  <behavior>
    - Reject HTTP URLs with schemes other than `http`/`https`; reject blank host and credentials embedded in URL per D-15.
    - Reject stdio config unless command is present and the transport is explicitly `STDIO` per D-06.
    - Reject custom headers that directly contain raw secret-looking values; require secret/ref fields per D-07 and D-19.
    - Produce sanitized validation errors that do not echo full auth headers, tokens, or raw env values per D-24.
  </behavior>
  <action>Implement `McpSafetyValidator.validate(server)` returning a small result record or throwing a sanitized `IllegalArgumentException`. Cover SSRF-sensitive defaults conservatively: no `file:`, `jar:`, `ftp:`, `gopher:`, or credential-in-URL; redirects must default to disabled or same-host only in later transport code. Do not attempt full network egress policy in this plan; document remaining production network controls as Phase 9 hardening.</action>
  <verify>
    <automated>JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -Dtest=McpServerPropertiesTest test</automated>
  </verify>
  <done>Unsafe transport/auth configuration fails before any MCP connection is attempted, with redacted error text.</done>
</task>

</tasks>

<verification>
Run `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 mvn -q -pl pi-agent-infrastructure-mcp -am test`.
</verification>

<success_criteria>
Phase 7 has an isolated, compilable MCP infrastructure module and a validated configuration-file-first trusted server model implementing D-01, D-05, D-06, D-07, D-08, D-10, D-15, D-19, and MCP-01/MCP-05 without Admin CRUD or OAuth scope creep.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mcp-client-bridge-and-governed-remote-tools/07-01-SUMMARY.md`.
</output>
